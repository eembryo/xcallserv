/*
 * Generated by gdbus-codegen 2.32.4. DO NOT EDIT.
 *
 * The license of this code is the same as for the source it was derived from.
 */

#ifdef HAVE_CONFIG_H
#  include "config.h"
#endif

#include "dbus_mmus_interface.h"

#include <string.h>
#ifdef G_OS_UNIX
#  include <gio/gunixfdlist.h>
#endif

typedef struct
{
  GDBusArgInfo parent_struct;
  gboolean use_gvariant;
} _ExtendedGDBusArgInfo;

typedef struct
{
  GDBusMethodInfo parent_struct;
  const gchar *signal_name;
  gboolean pass_fdlist;
} _ExtendedGDBusMethodInfo;

typedef struct
{
  GDBusSignalInfo parent_struct;
  const gchar *signal_name;
} _ExtendedGDBusSignalInfo;

typedef struct
{
  GDBusPropertyInfo parent_struct;
  const gchar *hyphen_name;
  gboolean use_gvariant;
} _ExtendedGDBusPropertyInfo;

typedef struct
{
  GDBusInterfaceInfo parent_struct;
  const gchar *hyphen_name;
} _ExtendedGDBusInterfaceInfo;

typedef struct
{
  const _ExtendedGDBusPropertyInfo *info;
  guint prop_id;
  GValue orig_value; /* the value before the change */
} ChangedProperty;

static void
_changed_property_free (ChangedProperty *data)
{
  g_value_unset (&data->orig_value);
  g_free (data);
}

static gboolean
_g_strv_equal0 (gchar **a, gchar **b)
{
  gboolean ret = FALSE;
  guint n;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  if (g_strv_length (a) != g_strv_length (b))
    goto out;
  for (n = 0; a[n] != NULL; n++)
    if (g_strcmp0 (a[n], b[n]) != 0)
      goto out;
  ret = TRUE;
out:
  return ret;
}

static gboolean
_g_variant_equal0 (GVariant *a, GVariant *b)
{
  gboolean ret = FALSE;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  ret = g_variant_equal (a, b);
out:
  return ret;
}

G_GNUC_UNUSED static gboolean
_g_value_equal (const GValue *a, const GValue *b)
{
  gboolean ret = FALSE;
  g_assert (G_VALUE_TYPE (a) == G_VALUE_TYPE (b));
  switch (G_VALUE_TYPE (a))
    {
      case G_TYPE_BOOLEAN:
        ret = (g_value_get_boolean (a) == g_value_get_boolean (b));
        break;
      case G_TYPE_UCHAR:
        ret = (g_value_get_uchar (a) == g_value_get_uchar (b));
        break;
      case G_TYPE_INT:
        ret = (g_value_get_int (a) == g_value_get_int (b));
        break;
      case G_TYPE_UINT:
        ret = (g_value_get_uint (a) == g_value_get_uint (b));
        break;
      case G_TYPE_INT64:
        ret = (g_value_get_int64 (a) == g_value_get_int64 (b));
        break;
      case G_TYPE_UINT64:
        ret = (g_value_get_uint64 (a) == g_value_get_uint64 (b));
        break;
      case G_TYPE_DOUBLE:
        {
          /* Avoid -Wfloat-equal warnings by doing a direct bit compare */
          gdouble da = g_value_get_double (a);
          gdouble db = g_value_get_double (b);
          ret = memcmp (&da, &db, sizeof (gdouble)) == 0;
        }
        break;
      case G_TYPE_STRING:
        ret = (g_strcmp0 (g_value_get_string (a), g_value_get_string (b)) == 0);
        break;
      case G_TYPE_VARIANT:
        ret = _g_variant_equal0 (g_value_get_variant (a), g_value_get_variant (b));
        break;
      default:
        if (G_VALUE_TYPE (a) == G_TYPE_STRV)
          ret = _g_strv_equal0 (g_value_get_boxed (a), g_value_get_boxed (b));
        else
          g_critical ("_g_value_equal() does not handle type %s", g_type_name (G_VALUE_TYPE (a)));
        break;
    }
  return ret;
}

/* ------------------------------------------------------------------------
 * Code for interface com.lge.car.micom.vddm
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:ComLgeCarMicomVddm
 * @title: ComLgeCarMicomVddm
 * @short_description: Generated C code for the com.lge.car.micom.vddm D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-com-lge-car-micom-vddm.top_of_page">com.lge.car.micom.vddm</link> D-Bus interface in C.
 */

/* ---- Introspection data for com.lge.car.micom.vddm ---- */

static const _ExtendedGDBusArgInfo _com_lge_car_micom_vddm_signal_info_notify_gear_indcn_rec_ARG_gearIndcnRec =
{
  {
    -1,
    "gearIndcnRec",
    "ay",
    NULL
  },
  TRUE
};

static const _ExtendedGDBusArgInfo * const _com_lge_car_micom_vddm_signal_info_notify_gear_indcn_rec_ARG_pointers[] =
{
  &_com_lge_car_micom_vddm_signal_info_notify_gear_indcn_rec_ARG_gearIndcnRec,
  NULL
};

static const _ExtendedGDBusSignalInfo _com_lge_car_micom_vddm_signal_info_notify_gear_indcn_rec =
{
  {
    -1,
    "NotifyGearIndcnRec",
    (GDBusArgInfo **) &_com_lge_car_micom_vddm_signal_info_notify_gear_indcn_rec_ARG_pointers,
    NULL
  },
  "notify-gear-indcn-rec"
};

static const _ExtendedGDBusArgInfo _com_lge_car_micom_vddm_signal_info_notify_whl_rot_tooth_cntr_ARG_whlRotToothCntr =
{
  {
    -1,
    "whlRotToothCntr",
    "ay",
    NULL
  },
  TRUE
};

static const _ExtendedGDBusArgInfo * const _com_lge_car_micom_vddm_signal_info_notify_whl_rot_tooth_cntr_ARG_pointers[] =
{
  &_com_lge_car_micom_vddm_signal_info_notify_whl_rot_tooth_cntr_ARG_whlRotToothCntr,
  NULL
};

static const _ExtendedGDBusSignalInfo _com_lge_car_micom_vddm_signal_info_notify_whl_rot_tooth_cntr =
{
  {
    -1,
    "NotifyWhlRotToothCntr",
    (GDBusArgInfo **) &_com_lge_car_micom_vddm_signal_info_notify_whl_rot_tooth_cntr_ARG_pointers,
    NULL
  },
  "notify-whl-rot-tooth-cntr"
};

static const _ExtendedGDBusSignalInfo * const _com_lge_car_micom_vddm_signal_info_pointers[] =
{
  &_com_lge_car_micom_vddm_signal_info_notify_gear_indcn_rec,
  &_com_lge_car_micom_vddm_signal_info_notify_whl_rot_tooth_cntr,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _com_lge_car_micom_vddm_interface_info =
{
  {
    -1,
    "com.lge.car.micom.vddm",
    NULL,
    (GDBusSignalInfo **) &_com_lge_car_micom_vddm_signal_info_pointers,
    NULL,
    NULL
  },
  "com-lge-car-micom-vddm",
};


/**
 * com_lge_car_micom_vddm_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-com-lge-car-micom-vddm.top_of_page">com.lge.car.micom.vddm</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
com_lge_car_micom_vddm_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_com_lge_car_micom_vddm_interface_info;
}

/**
 * com_lge_car_micom_vddm_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #ComLgeCarMicomVddm interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
com_lge_car_micom_vddm_override_properties (GObjectClass *klass, guint property_id_begin)
{
  return property_id_begin - 1;
}



/**
 * ComLgeCarMicomVddm:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-com-lge-car-micom-vddm.top_of_page">com.lge.car.micom.vddm</link>.
 */

/**
 * ComLgeCarMicomVddmIface:
 * @parent_iface: The parent interface.
 * @notify_gear_indcn_rec: Handler for the #ComLgeCarMicomVddm::notify-gear-indcn-rec signal.
 * @notify_whl_rot_tooth_cntr: Handler for the #ComLgeCarMicomVddm::notify-whl-rot-tooth-cntr signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-com-lge-car-micom-vddm.top_of_page">com.lge.car.micom.vddm</link>.
 */

static void
com_lge_car_micom_vddm_default_init (ComLgeCarMicomVddmIface *iface)
{
  /* GObject signals for received D-Bus signals: */
  /**
   * ComLgeCarMicomVddm::notify-gear-indcn-rec:
   * @object: A #ComLgeCarMicomVddm.
   * @arg_gearIndcnRec: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-lge-car-micom-vddm.NotifyGearIndcnRec">"NotifyGearIndcnRec"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("notify-gear-indcn-rec",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (ComLgeCarMicomVddmIface, notify_gear_indcn_rec),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    1, G_TYPE_VARIANT);

  /**
   * ComLgeCarMicomVddm::notify-whl-rot-tooth-cntr:
   * @object: A #ComLgeCarMicomVddm.
   * @arg_whlRotToothCntr: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-lge-car-micom-vddm.NotifyWhlRotToothCntr">"NotifyWhlRotToothCntr"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("notify-whl-rot-tooth-cntr",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (ComLgeCarMicomVddmIface, notify_whl_rot_tooth_cntr),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    1, G_TYPE_VARIANT);

}

typedef ComLgeCarMicomVddmIface ComLgeCarMicomVddmInterface;
G_DEFINE_INTERFACE (ComLgeCarMicomVddm, com_lge_car_micom_vddm, G_TYPE_OBJECT);

/**
 * com_lge_car_micom_vddm_emit_notify_gear_indcn_rec:
 * @object: A #ComLgeCarMicomVddm.
 * @arg_gearIndcnRec: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-com-lge-car-micom-vddm.NotifyGearIndcnRec">"NotifyGearIndcnRec"</link> D-Bus signal.
 */
void
com_lge_car_micom_vddm_emit_notify_gear_indcn_rec (
    ComLgeCarMicomVddm *object,
    GVariant *arg_gearIndcnRec)
{
  g_signal_emit_by_name (object, "notify-gear-indcn-rec", arg_gearIndcnRec);
}

/**
 * com_lge_car_micom_vddm_emit_notify_whl_rot_tooth_cntr:
 * @object: A #ComLgeCarMicomVddm.
 * @arg_whlRotToothCntr: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-com-lge-car-micom-vddm.NotifyWhlRotToothCntr">"NotifyWhlRotToothCntr"</link> D-Bus signal.
 */
void
com_lge_car_micom_vddm_emit_notify_whl_rot_tooth_cntr (
    ComLgeCarMicomVddm *object,
    GVariant *arg_whlRotToothCntr)
{
  g_signal_emit_by_name (object, "notify-whl-rot-tooth-cntr", arg_whlRotToothCntr);
}

/* ------------------------------------------------------------------------ */

/**
 * ComLgeCarMicomVddmProxy:
 *
 * The #ComLgeCarMicomVddmProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * ComLgeCarMicomVddmProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #ComLgeCarMicomVddmProxy.
 */

struct _ComLgeCarMicomVddmProxyPrivate
{
  GData *qdata;
};

static void com_lge_car_micom_vddm_proxy_iface_init (ComLgeCarMicomVddmIface *iface);

G_DEFINE_TYPE_WITH_CODE (ComLgeCarMicomVddmProxy, com_lge_car_micom_vddm_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (TYPE_COM_LGE_CAR_MICOM_VDDM, com_lge_car_micom_vddm_proxy_iface_init));

static void
com_lge_car_micom_vddm_proxy_finalize (GObject *object)
{
  ComLgeCarMicomVddmProxy *proxy = COM_LGE_CAR_MICOM_VDDM_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (com_lge_car_micom_vddm_proxy_parent_class)->finalize (object);
}

static void
com_lge_car_micom_vddm_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec)
{
}

static void
com_lge_car_micom_vddm_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
}

static void
com_lge_car_micom_vddm_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_com_lge_car_micom_vddm_interface_info, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], TYPE_COM_LGE_CAR_MICOM_VDDM);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_COM_LGE_CAR_MICOM_VDDM);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
com_lge_car_micom_vddm_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  ComLgeCarMicomVddmProxy *proxy = COM_LGE_CAR_MICOM_VDDM_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_car_micom_vddm_interface_info, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_car_micom_vddm_interface_info, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static void
com_lge_car_micom_vddm_proxy_init (ComLgeCarMicomVddmProxy *proxy)
{
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, TYPE_COM_LGE_CAR_MICOM_VDDM_PROXY, ComLgeCarMicomVddmProxyPrivate);
  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), com_lge_car_micom_vddm_interface_info ());
}

static void
com_lge_car_micom_vddm_proxy_class_init (ComLgeCarMicomVddmProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  g_type_class_add_private (klass, sizeof (ComLgeCarMicomVddmProxyPrivate));

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = com_lge_car_micom_vddm_proxy_finalize;
  gobject_class->get_property = com_lge_car_micom_vddm_proxy_get_property;
  gobject_class->set_property = com_lge_car_micom_vddm_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = com_lge_car_micom_vddm_proxy_g_signal;
  proxy_class->g_properties_changed = com_lge_car_micom_vddm_proxy_g_properties_changed;

}

static void
com_lge_car_micom_vddm_proxy_iface_init (ComLgeCarMicomVddmIface *iface)
{
}

/**
 * com_lge_car_micom_vddm_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-lge-car-micom-vddm.top_of_page">com.lge.car.micom.vddm</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_car_micom_vddm_proxy_new_finish() to get the result of the operation.
 *
 * See com_lge_car_micom_vddm_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
com_lge_car_micom_vddm_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_COM_LGE_CAR_MICOM_VDDM_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.lge.car.micom.vddm", NULL);
}

/**
 * com_lge_car_micom_vddm_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_car_micom_vddm_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with com_lge_car_micom_vddm_proxy_new().
 *
 * Returns: (transfer full) (type ComLgeCarMicomVddmProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeCarMicomVddm *
com_lge_car_micom_vddm_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return COM_LGE_CAR_MICOM_VDDM (ret);
  else
    return NULL;
}

/**
 * com_lge_car_micom_vddm_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-lge-car-micom-vddm.top_of_page">com.lge.car.micom.vddm</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See com_lge_car_micom_vddm_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type ComLgeCarMicomVddmProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeCarMicomVddm *
com_lge_car_micom_vddm_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_COM_LGE_CAR_MICOM_VDDM_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.lge.car.micom.vddm", NULL);
  if (ret != NULL)
    return COM_LGE_CAR_MICOM_VDDM (ret);
  else
    return NULL;
}


/**
 * com_lge_car_micom_vddm_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like com_lge_car_micom_vddm_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_car_micom_vddm_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See com_lge_car_micom_vddm_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
com_lge_car_micom_vddm_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_COM_LGE_CAR_MICOM_VDDM_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.lge.car.micom.vddm", NULL);
}

/**
 * com_lge_car_micom_vddm_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_car_micom_vddm_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with com_lge_car_micom_vddm_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type ComLgeCarMicomVddmProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeCarMicomVddm *
com_lge_car_micom_vddm_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return COM_LGE_CAR_MICOM_VDDM (ret);
  else
    return NULL;
}

/**
 * com_lge_car_micom_vddm_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like com_lge_car_micom_vddm_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See com_lge_car_micom_vddm_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type ComLgeCarMicomVddmProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeCarMicomVddm *
com_lge_car_micom_vddm_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_COM_LGE_CAR_MICOM_VDDM_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.lge.car.micom.vddm", NULL);
  if (ret != NULL)
    return COM_LGE_CAR_MICOM_VDDM (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * ComLgeCarMicomVddmSkeleton:
 *
 * The #ComLgeCarMicomVddmSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * ComLgeCarMicomVddmSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #ComLgeCarMicomVddmSkeleton.
 */

struct _ComLgeCarMicomVddmSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_com_lge_car_micom_vddm_skeleton_handle_method_call (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  ComLgeCarMicomVddmSkeleton *skeleton = COM_LGE_CAR_MICOM_VDDM_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], TYPE_COM_LGE_CAR_MICOM_VDDM);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_COM_LGE_CAR_MICOM_VDDM);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_com_lge_car_micom_vddm_skeleton_handle_get_property (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  ComLgeCarMicomVddmSkeleton *skeleton = COM_LGE_CAR_MICOM_VDDM_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_car_micom_vddm_interface_info, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_com_lge_car_micom_vddm_skeleton_handle_set_property (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  ComLgeCarMicomVddmSkeleton *skeleton = COM_LGE_CAR_MICOM_VDDM_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_car_micom_vddm_interface_info, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _com_lge_car_micom_vddm_skeleton_vtable =
{
  _com_lge_car_micom_vddm_skeleton_handle_method_call,
  _com_lge_car_micom_vddm_skeleton_handle_get_property,
  _com_lge_car_micom_vddm_skeleton_handle_set_property
};

static GDBusInterfaceInfo *
com_lge_car_micom_vddm_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton)
{
  return com_lge_car_micom_vddm_interface_info ();
}

static GDBusInterfaceVTable *
com_lge_car_micom_vddm_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton)
{
  return (GDBusInterfaceVTable *) &_com_lge_car_micom_vddm_skeleton_vtable;
}

static GVariant *
com_lge_car_micom_vddm_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  ComLgeCarMicomVddmSkeleton *skeleton = COM_LGE_CAR_MICOM_VDDM_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_com_lge_car_micom_vddm_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _com_lge_car_micom_vddm_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _com_lge_car_micom_vddm_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _com_lge_car_micom_vddm_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.lge.car.micom.vddm", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static void
com_lge_car_micom_vddm_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
}

static void
_com_lge_car_micom_vddm_on_signal_notify_gear_indcn_rec (
    ComLgeCarMicomVddm *object,
    GVariant *arg_gearIndcnRec)
{
  ComLgeCarMicomVddmSkeleton *skeleton = COM_LGE_CAR_MICOM_VDDM_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(@ay)",
                   arg_gearIndcnRec));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.lge.car.micom.vddm", "NotifyGearIndcnRec",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_com_lge_car_micom_vddm_on_signal_notify_whl_rot_tooth_cntr (
    ComLgeCarMicomVddm *object,
    GVariant *arg_whlRotToothCntr)
{
  ComLgeCarMicomVddmSkeleton *skeleton = COM_LGE_CAR_MICOM_VDDM_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(@ay)",
                   arg_whlRotToothCntr));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.lge.car.micom.vddm", "NotifyWhlRotToothCntr",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void com_lge_car_micom_vddm_skeleton_iface_init (ComLgeCarMicomVddmIface *iface);
G_DEFINE_TYPE_WITH_CODE (ComLgeCarMicomVddmSkeleton, com_lge_car_micom_vddm_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (TYPE_COM_LGE_CAR_MICOM_VDDM, com_lge_car_micom_vddm_skeleton_iface_init));

static void
com_lge_car_micom_vddm_skeleton_finalize (GObject *object)
{
  ComLgeCarMicomVddmSkeleton *skeleton = COM_LGE_CAR_MICOM_VDDM_SKELETON (object);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (com_lge_car_micom_vddm_skeleton_parent_class)->finalize (object);
}

static void
com_lge_car_micom_vddm_skeleton_init (ComLgeCarMicomVddmSkeleton *skeleton)
{
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, TYPE_COM_LGE_CAR_MICOM_VDDM_SKELETON, ComLgeCarMicomVddmSkeletonPrivate);
  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
}

static void
com_lge_car_micom_vddm_skeleton_class_init (ComLgeCarMicomVddmSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  g_type_class_add_private (klass, sizeof (ComLgeCarMicomVddmSkeletonPrivate));

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = com_lge_car_micom_vddm_skeleton_finalize;

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = com_lge_car_micom_vddm_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = com_lge_car_micom_vddm_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = com_lge_car_micom_vddm_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = com_lge_car_micom_vddm_skeleton_dbus_interface_get_vtable;
}

static void
com_lge_car_micom_vddm_skeleton_iface_init (ComLgeCarMicomVddmIface *iface)
{
  iface->notify_gear_indcn_rec = _com_lge_car_micom_vddm_on_signal_notify_gear_indcn_rec;
  iface->notify_whl_rot_tooth_cntr = _com_lge_car_micom_vddm_on_signal_notify_whl_rot_tooth_cntr;
}

/**
 * com_lge_car_micom_vddm_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-com-lge-car-micom-vddm.top_of_page">com.lge.car.micom.vddm</link>.
 *
 * Returns: (transfer full) (type ComLgeCarMicomVddmSkeleton): The skeleton object.
 */
ComLgeCarMicomVddm *
com_lge_car_micom_vddm_skeleton_new (void)
{
  return COM_LGE_CAR_MICOM_VDDM (g_object_new (TYPE_COM_LGE_CAR_MICOM_VDDM_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface com.lge.car.micom.ihu
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:ComLgeCarMicomIhu
 * @title: ComLgeCarMicomIhu
 * @short_description: Generated C code for the com.lge.car.micom.ihu D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-com-lge-car-micom-ihu.top_of_page">com.lge.car.micom.ihu</link> D-Bus interface in C.
 */

/* ---- Introspection data for com.lge.car.micom.ihu ---- */

static const _ExtendedGDBusArgInfo _com_lge_car_micom_ihu_method_info_ask_sys_last_status_OUT_ARG_SysLastStatus =
{
  {
    -1,
    "SysLastStatus",
    "ay",
    NULL
  },
  TRUE
};

static const _ExtendedGDBusArgInfo * const _com_lge_car_micom_ihu_method_info_ask_sys_last_status_OUT_ARG_pointers[] =
{
  &_com_lge_car_micom_ihu_method_info_ask_sys_last_status_OUT_ARG_SysLastStatus,
  NULL
};

static const _ExtendedGDBusMethodInfo _com_lge_car_micom_ihu_method_info_ask_sys_last_status =
{
  {
    -1,
    "AskSysLastStatus",
    NULL,
    (GDBusArgInfo **) &_com_lge_car_micom_ihu_method_info_ask_sys_last_status_OUT_ARG_pointers,
    NULL
  },
  "handle-ask-sys-last-status",
  FALSE
};

static const _ExtendedGDBusMethodInfo * const _com_lge_car_micom_ihu_method_info_pointers[] =
{
  &_com_lge_car_micom_ihu_method_info_ask_sys_last_status,
  NULL
};

static const _ExtendedGDBusArgInfo _com_lge_car_micom_ihu_signal_info_notify_car_ifstatus_ARG_CarIFStatus =
{
  {
    -1,
    "CarIFStatus",
    "ay",
    NULL
  },
  TRUE
};

static const _ExtendedGDBusArgInfo * const _com_lge_car_micom_ihu_signal_info_notify_car_ifstatus_ARG_pointers[] =
{
  &_com_lge_car_micom_ihu_signal_info_notify_car_ifstatus_ARG_CarIFStatus,
  NULL
};

static const _ExtendedGDBusSignalInfo _com_lge_car_micom_ihu_signal_info_notify_car_ifstatus =
{
  {
    -1,
    "NotifyCarIFStatus",
    (GDBusArgInfo **) &_com_lge_car_micom_ihu_signal_info_notify_car_ifstatus_ARG_pointers,
    NULL
  },
  "notify-car-ifstatus"
};

static const _ExtendedGDBusSignalInfo * const _com_lge_car_micom_ihu_signal_info_pointers[] =
{
  &_com_lge_car_micom_ihu_signal_info_notify_car_ifstatus,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _com_lge_car_micom_ihu_interface_info =
{
  {
    -1,
    "com.lge.car.micom.ihu",
    (GDBusMethodInfo **) &_com_lge_car_micom_ihu_method_info_pointers,
    (GDBusSignalInfo **) &_com_lge_car_micom_ihu_signal_info_pointers,
    NULL,
    NULL
  },
  "com-lge-car-micom-ihu",
};


/**
 * com_lge_car_micom_ihu_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-com-lge-car-micom-ihu.top_of_page">com.lge.car.micom.ihu</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
com_lge_car_micom_ihu_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_com_lge_car_micom_ihu_interface_info;
}

/**
 * com_lge_car_micom_ihu_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #ComLgeCarMicomIhu interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
com_lge_car_micom_ihu_override_properties (GObjectClass *klass, guint property_id_begin)
{
  return property_id_begin - 1;
}



/**
 * ComLgeCarMicomIhu:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-com-lge-car-micom-ihu.top_of_page">com.lge.car.micom.ihu</link>.
 */

/**
 * ComLgeCarMicomIhuIface:
 * @parent_iface: The parent interface.
 * @handle_ask_sys_last_status: Handler for the #ComLgeCarMicomIhu::handle-ask-sys-last-status signal.
 * @notify_car_ifstatus: Handler for the #ComLgeCarMicomIhu::notify-car-ifstatus signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-com-lge-car-micom-ihu.top_of_page">com.lge.car.micom.ihu</link>.
 */

static void
com_lge_car_micom_ihu_default_init (ComLgeCarMicomIhuIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * ComLgeCarMicomIhu::handle-ask-sys-last-status:
   * @object: A #ComLgeCarMicomIhu.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-lge-car-micom-ihu.AskSysLastStatus">AskSysLastStatus()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call com_lge_car_micom_ihu_complete_ask_sys_last_status() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-ask-sys-last-status",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (ComLgeCarMicomIhuIface, handle_ask_sys_last_status),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /* GObject signals for received D-Bus signals: */
  /**
   * ComLgeCarMicomIhu::notify-car-ifstatus:
   * @object: A #ComLgeCarMicomIhu.
   * @arg_CarIFStatus: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-lge-car-micom-ihu.NotifyCarIFStatus">"NotifyCarIFStatus"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("notify-car-ifstatus",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (ComLgeCarMicomIhuIface, notify_car_ifstatus),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    1, G_TYPE_VARIANT);

}

typedef ComLgeCarMicomIhuIface ComLgeCarMicomIhuInterface;
G_DEFINE_INTERFACE (ComLgeCarMicomIhu, com_lge_car_micom_ihu, G_TYPE_OBJECT);

/**
 * com_lge_car_micom_ihu_emit_notify_car_ifstatus:
 * @object: A #ComLgeCarMicomIhu.
 * @arg_CarIFStatus: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-com-lge-car-micom-ihu.NotifyCarIFStatus">"NotifyCarIFStatus"</link> D-Bus signal.
 */
void
com_lge_car_micom_ihu_emit_notify_car_ifstatus (
    ComLgeCarMicomIhu *object,
    GVariant *arg_CarIFStatus)
{
  g_signal_emit_by_name (object, "notify-car-ifstatus", arg_CarIFStatus);
}

/**
 * com_lge_car_micom_ihu_call_ask_sys_last_status:
 * @proxy: A #ComLgeCarMicomIhuProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-lge-car-micom-ihu.AskSysLastStatus">AskSysLastStatus()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_car_micom_ihu_call_ask_sys_last_status_finish() to get the result of the operation.
 *
 * See com_lge_car_micom_ihu_call_ask_sys_last_status_sync() for the synchronous, blocking version of this method.
 */
void
com_lge_car_micom_ihu_call_ask_sys_last_status (
    ComLgeCarMicomIhu *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "AskSysLastStatus",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * com_lge_car_micom_ihu_call_ask_sys_last_status_finish:
 * @proxy: A #ComLgeCarMicomIhuProxy.
 * @out_SysLastStatus: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_car_micom_ihu_call_ask_sys_last_status().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with com_lge_car_micom_ihu_call_ask_sys_last_status().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_car_micom_ihu_call_ask_sys_last_status_finish (
    ComLgeCarMicomIhu *proxy,
    GVariant **out_SysLastStatus,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@ay)",
                 out_SysLastStatus);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_car_micom_ihu_call_ask_sys_last_status_sync:
 * @proxy: A #ComLgeCarMicomIhuProxy.
 * @out_SysLastStatus: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-lge-car-micom-ihu.AskSysLastStatus">AskSysLastStatus()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See com_lge_car_micom_ihu_call_ask_sys_last_status() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_car_micom_ihu_call_ask_sys_last_status_sync (
    ComLgeCarMicomIhu *proxy,
    GVariant **out_SysLastStatus,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "AskSysLastStatus",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@ay)",
                 out_SysLastStatus);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_car_micom_ihu_complete_ask_sys_last_status:
 * @object: A #ComLgeCarMicomIhu.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @SysLastStatus: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-lge-car-micom-ihu.AskSysLastStatus">AskSysLastStatus()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
com_lge_car_micom_ihu_complete_ask_sys_last_status (
    ComLgeCarMicomIhu *object,
    GDBusMethodInvocation *invocation,
    GVariant *SysLastStatus)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(@ay)",
                   SysLastStatus));
}

/* ------------------------------------------------------------------------ */

/**
 * ComLgeCarMicomIhuProxy:
 *
 * The #ComLgeCarMicomIhuProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * ComLgeCarMicomIhuProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #ComLgeCarMicomIhuProxy.
 */

struct _ComLgeCarMicomIhuProxyPrivate
{
  GData *qdata;
};

static void com_lge_car_micom_ihu_proxy_iface_init (ComLgeCarMicomIhuIface *iface);

G_DEFINE_TYPE_WITH_CODE (ComLgeCarMicomIhuProxy, com_lge_car_micom_ihu_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (TYPE_COM_LGE_CAR_MICOM_IHU, com_lge_car_micom_ihu_proxy_iface_init));

static void
com_lge_car_micom_ihu_proxy_finalize (GObject *object)
{
  ComLgeCarMicomIhuProxy *proxy = COM_LGE_CAR_MICOM_IHU_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (com_lge_car_micom_ihu_proxy_parent_class)->finalize (object);
}

static void
com_lge_car_micom_ihu_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec)
{
}

static void
com_lge_car_micom_ihu_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
}

static void
com_lge_car_micom_ihu_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_com_lge_car_micom_ihu_interface_info, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], TYPE_COM_LGE_CAR_MICOM_IHU);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_COM_LGE_CAR_MICOM_IHU);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
com_lge_car_micom_ihu_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  ComLgeCarMicomIhuProxy *proxy = COM_LGE_CAR_MICOM_IHU_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_car_micom_ihu_interface_info, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_car_micom_ihu_interface_info, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static void
com_lge_car_micom_ihu_proxy_init (ComLgeCarMicomIhuProxy *proxy)
{
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, TYPE_COM_LGE_CAR_MICOM_IHU_PROXY, ComLgeCarMicomIhuProxyPrivate);
  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), com_lge_car_micom_ihu_interface_info ());
}

static void
com_lge_car_micom_ihu_proxy_class_init (ComLgeCarMicomIhuProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  g_type_class_add_private (klass, sizeof (ComLgeCarMicomIhuProxyPrivate));

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = com_lge_car_micom_ihu_proxy_finalize;
  gobject_class->get_property = com_lge_car_micom_ihu_proxy_get_property;
  gobject_class->set_property = com_lge_car_micom_ihu_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = com_lge_car_micom_ihu_proxy_g_signal;
  proxy_class->g_properties_changed = com_lge_car_micom_ihu_proxy_g_properties_changed;

}

static void
com_lge_car_micom_ihu_proxy_iface_init (ComLgeCarMicomIhuIface *iface)
{
}

/**
 * com_lge_car_micom_ihu_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-lge-car-micom-ihu.top_of_page">com.lge.car.micom.ihu</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_car_micom_ihu_proxy_new_finish() to get the result of the operation.
 *
 * See com_lge_car_micom_ihu_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
com_lge_car_micom_ihu_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_COM_LGE_CAR_MICOM_IHU_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.lge.car.micom.ihu", NULL);
}

/**
 * com_lge_car_micom_ihu_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_car_micom_ihu_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with com_lge_car_micom_ihu_proxy_new().
 *
 * Returns: (transfer full) (type ComLgeCarMicomIhuProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeCarMicomIhu *
com_lge_car_micom_ihu_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return COM_LGE_CAR_MICOM_IHU (ret);
  else
    return NULL;
}

/**
 * com_lge_car_micom_ihu_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-lge-car-micom-ihu.top_of_page">com.lge.car.micom.ihu</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See com_lge_car_micom_ihu_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type ComLgeCarMicomIhuProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeCarMicomIhu *
com_lge_car_micom_ihu_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_COM_LGE_CAR_MICOM_IHU_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.lge.car.micom.ihu", NULL);
  if (ret != NULL)
    return COM_LGE_CAR_MICOM_IHU (ret);
  else
    return NULL;
}


/**
 * com_lge_car_micom_ihu_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like com_lge_car_micom_ihu_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_car_micom_ihu_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See com_lge_car_micom_ihu_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
com_lge_car_micom_ihu_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_COM_LGE_CAR_MICOM_IHU_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.lge.car.micom.ihu", NULL);
}

/**
 * com_lge_car_micom_ihu_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_car_micom_ihu_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with com_lge_car_micom_ihu_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type ComLgeCarMicomIhuProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeCarMicomIhu *
com_lge_car_micom_ihu_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return COM_LGE_CAR_MICOM_IHU (ret);
  else
    return NULL;
}

/**
 * com_lge_car_micom_ihu_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like com_lge_car_micom_ihu_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See com_lge_car_micom_ihu_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type ComLgeCarMicomIhuProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeCarMicomIhu *
com_lge_car_micom_ihu_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_COM_LGE_CAR_MICOM_IHU_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.lge.car.micom.ihu", NULL);
  if (ret != NULL)
    return COM_LGE_CAR_MICOM_IHU (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * ComLgeCarMicomIhuSkeleton:
 *
 * The #ComLgeCarMicomIhuSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * ComLgeCarMicomIhuSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #ComLgeCarMicomIhuSkeleton.
 */

struct _ComLgeCarMicomIhuSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_com_lge_car_micom_ihu_skeleton_handle_method_call (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  ComLgeCarMicomIhuSkeleton *skeleton = COM_LGE_CAR_MICOM_IHU_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], TYPE_COM_LGE_CAR_MICOM_IHU);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_COM_LGE_CAR_MICOM_IHU);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_com_lge_car_micom_ihu_skeleton_handle_get_property (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  ComLgeCarMicomIhuSkeleton *skeleton = COM_LGE_CAR_MICOM_IHU_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_car_micom_ihu_interface_info, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_com_lge_car_micom_ihu_skeleton_handle_set_property (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  ComLgeCarMicomIhuSkeleton *skeleton = COM_LGE_CAR_MICOM_IHU_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_car_micom_ihu_interface_info, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _com_lge_car_micom_ihu_skeleton_vtable =
{
  _com_lge_car_micom_ihu_skeleton_handle_method_call,
  _com_lge_car_micom_ihu_skeleton_handle_get_property,
  _com_lge_car_micom_ihu_skeleton_handle_set_property
};

static GDBusInterfaceInfo *
com_lge_car_micom_ihu_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton)
{
  return com_lge_car_micom_ihu_interface_info ();
}

static GDBusInterfaceVTable *
com_lge_car_micom_ihu_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton)
{
  return (GDBusInterfaceVTable *) &_com_lge_car_micom_ihu_skeleton_vtable;
}

static GVariant *
com_lge_car_micom_ihu_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  ComLgeCarMicomIhuSkeleton *skeleton = COM_LGE_CAR_MICOM_IHU_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_com_lge_car_micom_ihu_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _com_lge_car_micom_ihu_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _com_lge_car_micom_ihu_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _com_lge_car_micom_ihu_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.lge.car.micom.ihu", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static void
com_lge_car_micom_ihu_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
}

static void
_com_lge_car_micom_ihu_on_signal_notify_car_ifstatus (
    ComLgeCarMicomIhu *object,
    GVariant *arg_CarIFStatus)
{
  ComLgeCarMicomIhuSkeleton *skeleton = COM_LGE_CAR_MICOM_IHU_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(@ay)",
                   arg_CarIFStatus));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.lge.car.micom.ihu", "NotifyCarIFStatus",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void com_lge_car_micom_ihu_skeleton_iface_init (ComLgeCarMicomIhuIface *iface);
G_DEFINE_TYPE_WITH_CODE (ComLgeCarMicomIhuSkeleton, com_lge_car_micom_ihu_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (TYPE_COM_LGE_CAR_MICOM_IHU, com_lge_car_micom_ihu_skeleton_iface_init));

static void
com_lge_car_micom_ihu_skeleton_finalize (GObject *object)
{
  ComLgeCarMicomIhuSkeleton *skeleton = COM_LGE_CAR_MICOM_IHU_SKELETON (object);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (com_lge_car_micom_ihu_skeleton_parent_class)->finalize (object);
}

static void
com_lge_car_micom_ihu_skeleton_init (ComLgeCarMicomIhuSkeleton *skeleton)
{
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, TYPE_COM_LGE_CAR_MICOM_IHU_SKELETON, ComLgeCarMicomIhuSkeletonPrivate);
  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
}

static void
com_lge_car_micom_ihu_skeleton_class_init (ComLgeCarMicomIhuSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  g_type_class_add_private (klass, sizeof (ComLgeCarMicomIhuSkeletonPrivate));

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = com_lge_car_micom_ihu_skeleton_finalize;

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = com_lge_car_micom_ihu_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = com_lge_car_micom_ihu_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = com_lge_car_micom_ihu_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = com_lge_car_micom_ihu_skeleton_dbus_interface_get_vtable;
}

static void
com_lge_car_micom_ihu_skeleton_iface_init (ComLgeCarMicomIhuIface *iface)
{
  iface->notify_car_ifstatus = _com_lge_car_micom_ihu_on_signal_notify_car_ifstatus;
}

/**
 * com_lge_car_micom_ihu_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-com-lge-car-micom-ihu.top_of_page">com.lge.car.micom.ihu</link>.
 *
 * Returns: (transfer full) (type ComLgeCarMicomIhuSkeleton): The skeleton object.
 */
ComLgeCarMicomIhu *
com_lge_car_micom_ihu_skeleton_new (void)
{
  return COM_LGE_CAR_MICOM_IHU (g_object_new (TYPE_COM_LGE_CAR_MICOM_IHU_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface com.lge.car.micom.cem
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:ComLgeCarMicomCem
 * @title: ComLgeCarMicomCem
 * @short_description: Generated C code for the com.lge.car.micom.cem D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-com-lge-car-micom-cem.top_of_page">com.lge.car.micom.cem</link> D-Bus interface in C.
 */

/* ---- Introspection data for com.lge.car.micom.cem ---- */

static const _ExtendedGDBusArgInfo _com_lge_car_micom_cem_signal_info_notify_emgy_assc_sts_ARG_EmgyAsscSts =
{
  {
    -1,
    "EmgyAsscSts",
    "ay",
    NULL
  },
  TRUE
};

static const _ExtendedGDBusArgInfo * const _com_lge_car_micom_cem_signal_info_notify_emgy_assc_sts_ARG_pointers[] =
{
  &_com_lge_car_micom_cem_signal_info_notify_emgy_assc_sts_ARG_EmgyAsscSts,
  NULL
};

static const _ExtendedGDBusSignalInfo _com_lge_car_micom_cem_signal_info_notify_emgy_assc_sts =
{
  {
    -1,
    "NotifyEmgyAsscSts",
    (GDBusArgInfo **) &_com_lge_car_micom_cem_signal_info_notify_emgy_assc_sts_ARG_pointers,
    NULL
  },
  "notify-emgy-assc-sts"
};

static const _ExtendedGDBusArgInfo _com_lge_car_micom_cem_signal_info_notify_telm_sts_ARG_TelmSts =
{
  {
    -1,
    "TelmSts",
    "ay",
    NULL
  },
  TRUE
};

static const _ExtendedGDBusArgInfo * const _com_lge_car_micom_cem_signal_info_notify_telm_sts_ARG_pointers[] =
{
  &_com_lge_car_micom_cem_signal_info_notify_telm_sts_ARG_TelmSts,
  NULL
};

static const _ExtendedGDBusSignalInfo _com_lge_car_micom_cem_signal_info_notify_telm_sts =
{
  {
    -1,
    "NotifyTelmSts",
    (GDBusArgInfo **) &_com_lge_car_micom_cem_signal_info_notify_telm_sts_ARG_pointers,
    NULL
  },
  "notify-telm-sts"
};

static const _ExtendedGDBusSignalInfo * const _com_lge_car_micom_cem_signal_info_pointers[] =
{
  &_com_lge_car_micom_cem_signal_info_notify_emgy_assc_sts,
  &_com_lge_car_micom_cem_signal_info_notify_telm_sts,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _com_lge_car_micom_cem_interface_info =
{
  {
    -1,
    "com.lge.car.micom.cem",
    NULL,
    (GDBusSignalInfo **) &_com_lge_car_micom_cem_signal_info_pointers,
    NULL,
    NULL
  },
  "com-lge-car-micom-cem",
};


/**
 * com_lge_car_micom_cem_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-com-lge-car-micom-cem.top_of_page">com.lge.car.micom.cem</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
com_lge_car_micom_cem_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_com_lge_car_micom_cem_interface_info;
}

/**
 * com_lge_car_micom_cem_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #ComLgeCarMicomCem interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
com_lge_car_micom_cem_override_properties (GObjectClass *klass, guint property_id_begin)
{
  return property_id_begin - 1;
}



/**
 * ComLgeCarMicomCem:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-com-lge-car-micom-cem.top_of_page">com.lge.car.micom.cem</link>.
 */

/**
 * ComLgeCarMicomCemIface:
 * @parent_iface: The parent interface.
 * @notify_emgy_assc_sts: Handler for the #ComLgeCarMicomCem::notify-emgy-assc-sts signal.
 * @notify_telm_sts: Handler for the #ComLgeCarMicomCem::notify-telm-sts signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-com-lge-car-micom-cem.top_of_page">com.lge.car.micom.cem</link>.
 */

static void
com_lge_car_micom_cem_default_init (ComLgeCarMicomCemIface *iface)
{
  /* GObject signals for received D-Bus signals: */
  /**
   * ComLgeCarMicomCem::notify-emgy-assc-sts:
   * @object: A #ComLgeCarMicomCem.
   * @arg_EmgyAsscSts: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-lge-car-micom-cem.NotifyEmgyAsscSts">"NotifyEmgyAsscSts"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("notify-emgy-assc-sts",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (ComLgeCarMicomCemIface, notify_emgy_assc_sts),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    1, G_TYPE_VARIANT);

  /**
   * ComLgeCarMicomCem::notify-telm-sts:
   * @object: A #ComLgeCarMicomCem.
   * @arg_TelmSts: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-lge-car-micom-cem.NotifyTelmSts">"NotifyTelmSts"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("notify-telm-sts",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (ComLgeCarMicomCemIface, notify_telm_sts),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    1, G_TYPE_VARIANT);

}

typedef ComLgeCarMicomCemIface ComLgeCarMicomCemInterface;
G_DEFINE_INTERFACE (ComLgeCarMicomCem, com_lge_car_micom_cem, G_TYPE_OBJECT);

/**
 * com_lge_car_micom_cem_emit_notify_emgy_assc_sts:
 * @object: A #ComLgeCarMicomCem.
 * @arg_EmgyAsscSts: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-com-lge-car-micom-cem.NotifyEmgyAsscSts">"NotifyEmgyAsscSts"</link> D-Bus signal.
 */
void
com_lge_car_micom_cem_emit_notify_emgy_assc_sts (
    ComLgeCarMicomCem *object,
    GVariant *arg_EmgyAsscSts)
{
  g_signal_emit_by_name (object, "notify-emgy-assc-sts", arg_EmgyAsscSts);
}

/**
 * com_lge_car_micom_cem_emit_notify_telm_sts:
 * @object: A #ComLgeCarMicomCem.
 * @arg_TelmSts: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-com-lge-car-micom-cem.NotifyTelmSts">"NotifyTelmSts"</link> D-Bus signal.
 */
void
com_lge_car_micom_cem_emit_notify_telm_sts (
    ComLgeCarMicomCem *object,
    GVariant *arg_TelmSts)
{
  g_signal_emit_by_name (object, "notify-telm-sts", arg_TelmSts);
}

/* ------------------------------------------------------------------------ */

/**
 * ComLgeCarMicomCemProxy:
 *
 * The #ComLgeCarMicomCemProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * ComLgeCarMicomCemProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #ComLgeCarMicomCemProxy.
 */

struct _ComLgeCarMicomCemProxyPrivate
{
  GData *qdata;
};

static void com_lge_car_micom_cem_proxy_iface_init (ComLgeCarMicomCemIface *iface);

G_DEFINE_TYPE_WITH_CODE (ComLgeCarMicomCemProxy, com_lge_car_micom_cem_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (TYPE_COM_LGE_CAR_MICOM_CEM, com_lge_car_micom_cem_proxy_iface_init));

static void
com_lge_car_micom_cem_proxy_finalize (GObject *object)
{
  ComLgeCarMicomCemProxy *proxy = COM_LGE_CAR_MICOM_CEM_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (com_lge_car_micom_cem_proxy_parent_class)->finalize (object);
}

static void
com_lge_car_micom_cem_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec)
{
}

static void
com_lge_car_micom_cem_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
}

static void
com_lge_car_micom_cem_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_com_lge_car_micom_cem_interface_info, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], TYPE_COM_LGE_CAR_MICOM_CEM);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_COM_LGE_CAR_MICOM_CEM);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
com_lge_car_micom_cem_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  ComLgeCarMicomCemProxy *proxy = COM_LGE_CAR_MICOM_CEM_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_car_micom_cem_interface_info, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_car_micom_cem_interface_info, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static void
com_lge_car_micom_cem_proxy_init (ComLgeCarMicomCemProxy *proxy)
{
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, TYPE_COM_LGE_CAR_MICOM_CEM_PROXY, ComLgeCarMicomCemProxyPrivate);
  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), com_lge_car_micom_cem_interface_info ());
}

static void
com_lge_car_micom_cem_proxy_class_init (ComLgeCarMicomCemProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  g_type_class_add_private (klass, sizeof (ComLgeCarMicomCemProxyPrivate));

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = com_lge_car_micom_cem_proxy_finalize;
  gobject_class->get_property = com_lge_car_micom_cem_proxy_get_property;
  gobject_class->set_property = com_lge_car_micom_cem_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = com_lge_car_micom_cem_proxy_g_signal;
  proxy_class->g_properties_changed = com_lge_car_micom_cem_proxy_g_properties_changed;

}

static void
com_lge_car_micom_cem_proxy_iface_init (ComLgeCarMicomCemIface *iface)
{
}

/**
 * com_lge_car_micom_cem_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-lge-car-micom-cem.top_of_page">com.lge.car.micom.cem</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_car_micom_cem_proxy_new_finish() to get the result of the operation.
 *
 * See com_lge_car_micom_cem_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
com_lge_car_micom_cem_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_COM_LGE_CAR_MICOM_CEM_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.lge.car.micom.cem", NULL);
}

/**
 * com_lge_car_micom_cem_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_car_micom_cem_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with com_lge_car_micom_cem_proxy_new().
 *
 * Returns: (transfer full) (type ComLgeCarMicomCemProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeCarMicomCem *
com_lge_car_micom_cem_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return COM_LGE_CAR_MICOM_CEM (ret);
  else
    return NULL;
}

/**
 * com_lge_car_micom_cem_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-lge-car-micom-cem.top_of_page">com.lge.car.micom.cem</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See com_lge_car_micom_cem_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type ComLgeCarMicomCemProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeCarMicomCem *
com_lge_car_micom_cem_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_COM_LGE_CAR_MICOM_CEM_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.lge.car.micom.cem", NULL);
  if (ret != NULL)
    return COM_LGE_CAR_MICOM_CEM (ret);
  else
    return NULL;
}


/**
 * com_lge_car_micom_cem_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like com_lge_car_micom_cem_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_car_micom_cem_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See com_lge_car_micom_cem_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
com_lge_car_micom_cem_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_COM_LGE_CAR_MICOM_CEM_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.lge.car.micom.cem", NULL);
}

/**
 * com_lge_car_micom_cem_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_car_micom_cem_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with com_lge_car_micom_cem_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type ComLgeCarMicomCemProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeCarMicomCem *
com_lge_car_micom_cem_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return COM_LGE_CAR_MICOM_CEM (ret);
  else
    return NULL;
}

/**
 * com_lge_car_micom_cem_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like com_lge_car_micom_cem_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See com_lge_car_micom_cem_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type ComLgeCarMicomCemProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeCarMicomCem *
com_lge_car_micom_cem_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_COM_LGE_CAR_MICOM_CEM_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.lge.car.micom.cem", NULL);
  if (ret != NULL)
    return COM_LGE_CAR_MICOM_CEM (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * ComLgeCarMicomCemSkeleton:
 *
 * The #ComLgeCarMicomCemSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * ComLgeCarMicomCemSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #ComLgeCarMicomCemSkeleton.
 */

struct _ComLgeCarMicomCemSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_com_lge_car_micom_cem_skeleton_handle_method_call (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  ComLgeCarMicomCemSkeleton *skeleton = COM_LGE_CAR_MICOM_CEM_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], TYPE_COM_LGE_CAR_MICOM_CEM);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_COM_LGE_CAR_MICOM_CEM);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_com_lge_car_micom_cem_skeleton_handle_get_property (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  ComLgeCarMicomCemSkeleton *skeleton = COM_LGE_CAR_MICOM_CEM_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_car_micom_cem_interface_info, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_com_lge_car_micom_cem_skeleton_handle_set_property (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  ComLgeCarMicomCemSkeleton *skeleton = COM_LGE_CAR_MICOM_CEM_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_car_micom_cem_interface_info, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _com_lge_car_micom_cem_skeleton_vtable =
{
  _com_lge_car_micom_cem_skeleton_handle_method_call,
  _com_lge_car_micom_cem_skeleton_handle_get_property,
  _com_lge_car_micom_cem_skeleton_handle_set_property
};

static GDBusInterfaceInfo *
com_lge_car_micom_cem_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton)
{
  return com_lge_car_micom_cem_interface_info ();
}

static GDBusInterfaceVTable *
com_lge_car_micom_cem_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton)
{
  return (GDBusInterfaceVTable *) &_com_lge_car_micom_cem_skeleton_vtable;
}

static GVariant *
com_lge_car_micom_cem_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  ComLgeCarMicomCemSkeleton *skeleton = COM_LGE_CAR_MICOM_CEM_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_com_lge_car_micom_cem_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _com_lge_car_micom_cem_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _com_lge_car_micom_cem_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _com_lge_car_micom_cem_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.lge.car.micom.cem", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static void
com_lge_car_micom_cem_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
}

static void
_com_lge_car_micom_cem_on_signal_notify_emgy_assc_sts (
    ComLgeCarMicomCem *object,
    GVariant *arg_EmgyAsscSts)
{
  ComLgeCarMicomCemSkeleton *skeleton = COM_LGE_CAR_MICOM_CEM_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(@ay)",
                   arg_EmgyAsscSts));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.lge.car.micom.cem", "NotifyEmgyAsscSts",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_com_lge_car_micom_cem_on_signal_notify_telm_sts (
    ComLgeCarMicomCem *object,
    GVariant *arg_TelmSts)
{
  ComLgeCarMicomCemSkeleton *skeleton = COM_LGE_CAR_MICOM_CEM_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(@ay)",
                   arg_TelmSts));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.lge.car.micom.cem", "NotifyTelmSts",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void com_lge_car_micom_cem_skeleton_iface_init (ComLgeCarMicomCemIface *iface);
G_DEFINE_TYPE_WITH_CODE (ComLgeCarMicomCemSkeleton, com_lge_car_micom_cem_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (TYPE_COM_LGE_CAR_MICOM_CEM, com_lge_car_micom_cem_skeleton_iface_init));

static void
com_lge_car_micom_cem_skeleton_finalize (GObject *object)
{
  ComLgeCarMicomCemSkeleton *skeleton = COM_LGE_CAR_MICOM_CEM_SKELETON (object);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (com_lge_car_micom_cem_skeleton_parent_class)->finalize (object);
}

static void
com_lge_car_micom_cem_skeleton_init (ComLgeCarMicomCemSkeleton *skeleton)
{
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, TYPE_COM_LGE_CAR_MICOM_CEM_SKELETON, ComLgeCarMicomCemSkeletonPrivate);
  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
}

static void
com_lge_car_micom_cem_skeleton_class_init (ComLgeCarMicomCemSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  g_type_class_add_private (klass, sizeof (ComLgeCarMicomCemSkeletonPrivate));

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = com_lge_car_micom_cem_skeleton_finalize;

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = com_lge_car_micom_cem_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = com_lge_car_micom_cem_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = com_lge_car_micom_cem_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = com_lge_car_micom_cem_skeleton_dbus_interface_get_vtable;
}

static void
com_lge_car_micom_cem_skeleton_iface_init (ComLgeCarMicomCemIface *iface)
{
  iface->notify_emgy_assc_sts = _com_lge_car_micom_cem_on_signal_notify_emgy_assc_sts;
  iface->notify_telm_sts = _com_lge_car_micom_cem_on_signal_notify_telm_sts;
}

/**
 * com_lge_car_micom_cem_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-com-lge-car-micom-cem.top_of_page">com.lge.car.micom.cem</link>.
 *
 * Returns: (transfer full) (type ComLgeCarMicomCemSkeleton): The skeleton object.
 */
ComLgeCarMicomCem *
com_lge_car_micom_cem_skeleton_new (void)
{
  return COM_LGE_CAR_MICOM_CEM (g_object_new (TYPE_COM_LGE_CAR_MICOM_CEM_SKELETON, NULL));
}

