/*
 * Generated by gdbus-codegen 2.32.4. DO NOT EDIT.
 *
 * The license of this code is the same as for the source it was derived from.
 */

#ifdef HAVE_CONFIG_H
#  include "config.h"
#endif

#include "Connectivity.h"

#include <string.h>
#ifdef G_OS_UNIX
#  include <gio/gunixfdlist.h>
#endif

typedef struct
{
  GDBusArgInfo parent_struct;
  gboolean use_gvariant;
} _ExtendedGDBusArgInfo;

typedef struct
{
  GDBusMethodInfo parent_struct;
  const gchar *signal_name;
  gboolean pass_fdlist;
} _ExtendedGDBusMethodInfo;

typedef struct
{
  GDBusSignalInfo parent_struct;
  const gchar *signal_name;
} _ExtendedGDBusSignalInfo;

typedef struct
{
  GDBusPropertyInfo parent_struct;
  const gchar *hyphen_name;
  gboolean use_gvariant;
} _ExtendedGDBusPropertyInfo;

typedef struct
{
  GDBusInterfaceInfo parent_struct;
  const gchar *hyphen_name;
} _ExtendedGDBusInterfaceInfo;

typedef struct
{
  const _ExtendedGDBusPropertyInfo *info;
  guint prop_id;
  GValue orig_value; /* the value before the change */
} ChangedProperty;

static void
_changed_property_free (ChangedProperty *data)
{
  g_value_unset (&data->orig_value);
  g_free (data);
}

static gboolean
_g_strv_equal0 (gchar **a, gchar **b)
{
  gboolean ret = FALSE;
  guint n;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  if (g_strv_length (a) != g_strv_length (b))
    goto out;
  for (n = 0; a[n] != NULL; n++)
    if (g_strcmp0 (a[n], b[n]) != 0)
      goto out;
  ret = TRUE;
out:
  return ret;
}

static gboolean
_g_variant_equal0 (GVariant *a, GVariant *b)
{
  gboolean ret = FALSE;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  ret = g_variant_equal (a, b);
out:
  return ret;
}

G_GNUC_UNUSED static gboolean
_g_value_equal (const GValue *a, const GValue *b)
{
  gboolean ret = FALSE;
  g_assert (G_VALUE_TYPE (a) == G_VALUE_TYPE (b));
  switch (G_VALUE_TYPE (a))
    {
      case G_TYPE_BOOLEAN:
        ret = (g_value_get_boolean (a) == g_value_get_boolean (b));
        break;
      case G_TYPE_UCHAR:
        ret = (g_value_get_uchar (a) == g_value_get_uchar (b));
        break;
      case G_TYPE_INT:
        ret = (g_value_get_int (a) == g_value_get_int (b));
        break;
      case G_TYPE_UINT:
        ret = (g_value_get_uint (a) == g_value_get_uint (b));
        break;
      case G_TYPE_INT64:
        ret = (g_value_get_int64 (a) == g_value_get_int64 (b));
        break;
      case G_TYPE_UINT64:
        ret = (g_value_get_uint64 (a) == g_value_get_uint64 (b));
        break;
      case G_TYPE_DOUBLE:
        {
          /* Avoid -Wfloat-equal warnings by doing a direct bit compare */
          gdouble da = g_value_get_double (a);
          gdouble db = g_value_get_double (b);
          ret = memcmp (&da, &db, sizeof (gdouble)) == 0;
        }
        break;
      case G_TYPE_STRING:
        ret = (g_strcmp0 (g_value_get_string (a), g_value_get_string (b)) == 0);
        break;
      case G_TYPE_VARIANT:
        ret = _g_variant_equal0 (g_value_get_variant (a), g_value_get_variant (b));
        break;
      default:
        if (G_VALUE_TYPE (a) == G_TYPE_STRV)
          ret = _g_strv_equal0 (g_value_get_boxed (a), g_value_get_boxed (b));
        else
          g_critical ("_g_value_equal() does not handle type %s", g_type_name (G_VALUE_TYPE (a)));
        break;
    }
  return ret;
}

/* ------------------------------------------------------------------------
 * Code for interface com.lge.temil.Connectivity.SIMConnectionStatus
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:ComLgeTemilConnectivitySIMConnectionStatus
 * @title: ComLgeTemilConnectivitySIMConnectionStatus
 * @short_description: Generated C code for the com.lge.temil.Connectivity.SIMConnectionStatus D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-com-lge-temil-Connectivity-SIMConnectionStatus.top_of_page">com.lge.temil.Connectivity.SIMConnectionStatus</link> D-Bus interface in C.
 */

/* ---- Introspection data for com.lge.temil.Connectivity.SIMConnectionStatus ---- */

static const _ExtendedGDBusArgInfo _com_lge_temil_connectivity_simconnection_status_method_info_request_OUT_ARG_status =
{
  {
    -1,
    "status",
    "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _com_lge_temil_connectivity_simconnection_status_method_info_request_OUT_ARG_technology =
{
  {
    -1,
    "technology",
    "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _com_lge_temil_connectivity_simconnection_status_method_info_request_OUT_ARG_pointers[] =
{
  &_com_lge_temil_connectivity_simconnection_status_method_info_request_OUT_ARG_status,
  &_com_lge_temil_connectivity_simconnection_status_method_info_request_OUT_ARG_technology,
  NULL
};

static const _ExtendedGDBusMethodInfo _com_lge_temil_connectivity_simconnection_status_method_info_request =
{
  {
    -1,
    "Request",
    NULL,
    (GDBusArgInfo **) &_com_lge_temil_connectivity_simconnection_status_method_info_request_OUT_ARG_pointers,
    NULL
  },
  "handle-request",
  FALSE
};

static const _ExtendedGDBusArgInfo _com_lge_temil_connectivity_simconnection_status_method_info_notification_request_IN_ARG_subscribed =
{
  {
    -1,
    "subscribed",
    "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _com_lge_temil_connectivity_simconnection_status_method_info_notification_request_IN_ARG_pointers[] =
{
  &_com_lge_temil_connectivity_simconnection_status_method_info_notification_request_IN_ARG_subscribed,
  NULL
};

static const _ExtendedGDBusMethodInfo _com_lge_temil_connectivity_simconnection_status_method_info_notification_request =
{
  {
    -1,
    "NotificationRequest",
    (GDBusArgInfo **) &_com_lge_temil_connectivity_simconnection_status_method_info_notification_request_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-notification-request",
  FALSE
};

static const _ExtendedGDBusMethodInfo * const _com_lge_temil_connectivity_simconnection_status_method_info_pointers[] =
{
  &_com_lge_temil_connectivity_simconnection_status_method_info_request,
  &_com_lge_temil_connectivity_simconnection_status_method_info_notification_request,
  NULL
};

static const _ExtendedGDBusArgInfo _com_lge_temil_connectivity_simconnection_status_signal_info_notification_ARG_status =
{
  {
    -1,
    "status",
    "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _com_lge_temil_connectivity_simconnection_status_signal_info_notification_ARG_technology =
{
  {
    -1,
    "technology",
    "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _com_lge_temil_connectivity_simconnection_status_signal_info_notification_ARG_pointers[] =
{
  &_com_lge_temil_connectivity_simconnection_status_signal_info_notification_ARG_status,
  &_com_lge_temil_connectivity_simconnection_status_signal_info_notification_ARG_technology,
  NULL
};

static const _ExtendedGDBusSignalInfo _com_lge_temil_connectivity_simconnection_status_signal_info_notification =
{
  {
    -1,
    "Notification",
    (GDBusArgInfo **) &_com_lge_temil_connectivity_simconnection_status_signal_info_notification_ARG_pointers,
    NULL
  },
  "notification"
};

static const _ExtendedGDBusSignalInfo * const _com_lge_temil_connectivity_simconnection_status_signal_info_pointers[] =
{
  &_com_lge_temil_connectivity_simconnection_status_signal_info_notification,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _com_lge_temil_connectivity_simconnection_status_interface_info =
{
  {
    -1,
    "com.lge.temil.Connectivity.SIMConnectionStatus",
    (GDBusMethodInfo **) &_com_lge_temil_connectivity_simconnection_status_method_info_pointers,
    (GDBusSignalInfo **) &_com_lge_temil_connectivity_simconnection_status_signal_info_pointers,
    NULL,
    NULL
  },
  "com-lge-temil-connectivity-simconnection-status",
};


/**
 * com_lge_temil_connectivity_simconnection_status_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-com-lge-temil-Connectivity-SIMConnectionStatus.top_of_page">com.lge.temil.Connectivity.SIMConnectionStatus</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
com_lge_temil_connectivity_simconnection_status_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_com_lge_temil_connectivity_simconnection_status_interface_info;
}

/**
 * com_lge_temil_connectivity_simconnection_status_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #ComLgeTemilConnectivitySIMConnectionStatus interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
com_lge_temil_connectivity_simconnection_status_override_properties (GObjectClass *klass, guint property_id_begin)
{
  return property_id_begin - 1;
}



/**
 * ComLgeTemilConnectivitySIMConnectionStatus:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Connectivity-SIMConnectionStatus.top_of_page">com.lge.temil.Connectivity.SIMConnectionStatus</link>.
 */

/**
 * ComLgeTemilConnectivitySIMConnectionStatusIface:
 * @parent_iface: The parent interface.
 * @handle_notification_request: Handler for the #ComLgeTemilConnectivitySIMConnectionStatus::handle-notification-request signal.
 * @handle_request: Handler for the #ComLgeTemilConnectivitySIMConnectionStatus::handle-request signal.
 * @notification: Handler for the #ComLgeTemilConnectivitySIMConnectionStatus::notification signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Connectivity-SIMConnectionStatus.top_of_page">com.lge.temil.Connectivity.SIMConnectionStatus</link>.
 */

static void
com_lge_temil_connectivity_simconnection_status_default_init (ComLgeTemilConnectivitySIMConnectionStatusIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * ComLgeTemilConnectivitySIMConnectionStatus::handle-request:
   * @object: A #ComLgeTemilConnectivitySIMConnectionStatus.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-lge-temil-Connectivity-SIMConnectionStatus.Request">Request()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call com_lge_temil_connectivity_simconnection_status_complete_request() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-request",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (ComLgeTemilConnectivitySIMConnectionStatusIface, handle_request),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * ComLgeTemilConnectivitySIMConnectionStatus::handle-notification-request:
   * @object: A #ComLgeTemilConnectivitySIMConnectionStatus.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_subscribed: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-lge-temil-Connectivity-SIMConnectionStatus.NotificationRequest">NotificationRequest()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call com_lge_temil_connectivity_simconnection_status_complete_notification_request() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-notification-request",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (ComLgeTemilConnectivitySIMConnectionStatusIface, handle_notification_request),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_BOOLEAN);

  /* GObject signals for received D-Bus signals: */
  /**
   * ComLgeTemilConnectivitySIMConnectionStatus::notification:
   * @object: A #ComLgeTemilConnectivitySIMConnectionStatus.
   * @arg_status: Argument.
   * @arg_technology: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-lge-temil-Connectivity-SIMConnectionStatus.Notification">"Notification"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("notification",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (ComLgeTemilConnectivitySIMConnectionStatusIface, notification),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    2, G_TYPE_UINT, G_TYPE_UINT);

}

typedef ComLgeTemilConnectivitySIMConnectionStatusIface ComLgeTemilConnectivitySIMConnectionStatusInterface;
G_DEFINE_INTERFACE (ComLgeTemilConnectivitySIMConnectionStatus, com_lge_temil_connectivity_simconnection_status, G_TYPE_OBJECT);

/**
 * com_lge_temil_connectivity_simconnection_status_emit_notification:
 * @object: A #ComLgeTemilConnectivitySIMConnectionStatus.
 * @arg_status: Argument to pass with the signal.
 * @arg_technology: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-com-lge-temil-Connectivity-SIMConnectionStatus.Notification">"Notification"</link> D-Bus signal.
 */
void
com_lge_temil_connectivity_simconnection_status_emit_notification (
    ComLgeTemilConnectivitySIMConnectionStatus *object,
    guint arg_status,
    guint arg_technology)
{
  g_signal_emit_by_name (object, "notification", arg_status, arg_technology);
}

/**
 * com_lge_temil_connectivity_simconnection_status_call_request:
 * @proxy: A #ComLgeTemilConnectivitySIMConnectionStatusProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-lge-temil-Connectivity-SIMConnectionStatus.Request">Request()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_connectivity_simconnection_status_call_request_finish() to get the result of the operation.
 *
 * See com_lge_temil_connectivity_simconnection_status_call_request_sync() for the synchronous, blocking version of this method.
 */
void
com_lge_temil_connectivity_simconnection_status_call_request (
    ComLgeTemilConnectivitySIMConnectionStatus *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Request",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * com_lge_temil_connectivity_simconnection_status_call_request_finish:
 * @proxy: A #ComLgeTemilConnectivitySIMConnectionStatusProxy.
 * @out_status: (out): Return location for return parameter or %NULL to ignore.
 * @out_technology: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_connectivity_simconnection_status_call_request().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with com_lge_temil_connectivity_simconnection_status_call_request().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_connectivity_simconnection_status_call_request_finish (
    ComLgeTemilConnectivitySIMConnectionStatus *proxy,
    guint *out_status,
    guint *out_technology,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(uu)",
                 out_status,
                 out_technology);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_connectivity_simconnection_status_call_request_sync:
 * @proxy: A #ComLgeTemilConnectivitySIMConnectionStatusProxy.
 * @out_status: (out): Return location for return parameter or %NULL to ignore.
 * @out_technology: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-lge-temil-Connectivity-SIMConnectionStatus.Request">Request()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_connectivity_simconnection_status_call_request() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_connectivity_simconnection_status_call_request_sync (
    ComLgeTemilConnectivitySIMConnectionStatus *proxy,
    guint *out_status,
    guint *out_technology,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Request",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(uu)",
                 out_status,
                 out_technology);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_connectivity_simconnection_status_call_notification_request:
 * @proxy: A #ComLgeTemilConnectivitySIMConnectionStatusProxy.
 * @arg_subscribed: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-lge-temil-Connectivity-SIMConnectionStatus.NotificationRequest">NotificationRequest()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_connectivity_simconnection_status_call_notification_request_finish() to get the result of the operation.
 *
 * See com_lge_temil_connectivity_simconnection_status_call_notification_request_sync() for the synchronous, blocking version of this method.
 */
void
com_lge_temil_connectivity_simconnection_status_call_notification_request (
    ComLgeTemilConnectivitySIMConnectionStatus *proxy,
    gboolean arg_subscribed,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "NotificationRequest",
    g_variant_new ("(b)",
                   arg_subscribed),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * com_lge_temil_connectivity_simconnection_status_call_notification_request_finish:
 * @proxy: A #ComLgeTemilConnectivitySIMConnectionStatusProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_connectivity_simconnection_status_call_notification_request().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with com_lge_temil_connectivity_simconnection_status_call_notification_request().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_connectivity_simconnection_status_call_notification_request_finish (
    ComLgeTemilConnectivitySIMConnectionStatus *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_connectivity_simconnection_status_call_notification_request_sync:
 * @proxy: A #ComLgeTemilConnectivitySIMConnectionStatusProxy.
 * @arg_subscribed: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-lge-temil-Connectivity-SIMConnectionStatus.NotificationRequest">NotificationRequest()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_connectivity_simconnection_status_call_notification_request() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_connectivity_simconnection_status_call_notification_request_sync (
    ComLgeTemilConnectivitySIMConnectionStatus *proxy,
    gboolean arg_subscribed,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "NotificationRequest",
    g_variant_new ("(b)",
                   arg_subscribed),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_connectivity_simconnection_status_complete_request:
 * @object: A #ComLgeTemilConnectivitySIMConnectionStatus.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @status: Parameter to return.
 * @technology: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-lge-temil-Connectivity-SIMConnectionStatus.Request">Request()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
com_lge_temil_connectivity_simconnection_status_complete_request (
    ComLgeTemilConnectivitySIMConnectionStatus *object,
    GDBusMethodInvocation *invocation,
    guint status,
    guint technology)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(uu)",
                   status,
                   technology));
}

/**
 * com_lge_temil_connectivity_simconnection_status_complete_notification_request:
 * @object: A #ComLgeTemilConnectivitySIMConnectionStatus.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-lge-temil-Connectivity-SIMConnectionStatus.NotificationRequest">NotificationRequest()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
com_lge_temil_connectivity_simconnection_status_complete_notification_request (
    ComLgeTemilConnectivitySIMConnectionStatus *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * ComLgeTemilConnectivitySIMConnectionStatusProxy:
 *
 * The #ComLgeTemilConnectivitySIMConnectionStatusProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * ComLgeTemilConnectivitySIMConnectionStatusProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #ComLgeTemilConnectivitySIMConnectionStatusProxy.
 */

struct _ComLgeTemilConnectivitySIMConnectionStatusProxyPrivate
{
  GData *qdata;
};

static void com_lge_temil_connectivity_simconnection_status_proxy_iface_init (ComLgeTemilConnectivitySIMConnectionStatusIface *iface);

G_DEFINE_TYPE_WITH_CODE (ComLgeTemilConnectivitySIMConnectionStatusProxy, com_lge_temil_connectivity_simconnection_status_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (TYPE_COM_LGE_TEMIL_CONNECTIVITY_SIMCONNECTION_STATUS, com_lge_temil_connectivity_simconnection_status_proxy_iface_init));

static void
com_lge_temil_connectivity_simconnection_status_proxy_finalize (GObject *object)
{
  ComLgeTemilConnectivitySIMConnectionStatusProxy *proxy = COM_LGE_TEMIL_CONNECTIVITY_SIMCONNECTION_STATUS_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (com_lge_temil_connectivity_simconnection_status_proxy_parent_class)->finalize (object);
}

static void
com_lge_temil_connectivity_simconnection_status_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec)
{
}

static void
com_lge_temil_connectivity_simconnection_status_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
}

static void
com_lge_temil_connectivity_simconnection_status_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_com_lge_temil_connectivity_simconnection_status_interface_info, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], TYPE_COM_LGE_TEMIL_CONNECTIVITY_SIMCONNECTION_STATUS);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_COM_LGE_TEMIL_CONNECTIVITY_SIMCONNECTION_STATUS);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
com_lge_temil_connectivity_simconnection_status_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  ComLgeTemilConnectivitySIMConnectionStatusProxy *proxy = COM_LGE_TEMIL_CONNECTIVITY_SIMCONNECTION_STATUS_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_connectivity_simconnection_status_interface_info, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_connectivity_simconnection_status_interface_info, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static void
com_lge_temil_connectivity_simconnection_status_proxy_init (ComLgeTemilConnectivitySIMConnectionStatusProxy *proxy)
{
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, TYPE_COM_LGE_TEMIL_CONNECTIVITY_SIMCONNECTION_STATUS_PROXY, ComLgeTemilConnectivitySIMConnectionStatusProxyPrivate);
  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), com_lge_temil_connectivity_simconnection_status_interface_info ());
}

static void
com_lge_temil_connectivity_simconnection_status_proxy_class_init (ComLgeTemilConnectivitySIMConnectionStatusProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  g_type_class_add_private (klass, sizeof (ComLgeTemilConnectivitySIMConnectionStatusProxyPrivate));

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = com_lge_temil_connectivity_simconnection_status_proxy_finalize;
  gobject_class->get_property = com_lge_temil_connectivity_simconnection_status_proxy_get_property;
  gobject_class->set_property = com_lge_temil_connectivity_simconnection_status_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = com_lge_temil_connectivity_simconnection_status_proxy_g_signal;
  proxy_class->g_properties_changed = com_lge_temil_connectivity_simconnection_status_proxy_g_properties_changed;

}

static void
com_lge_temil_connectivity_simconnection_status_proxy_iface_init (ComLgeTemilConnectivitySIMConnectionStatusIface *iface)
{
}

/**
 * com_lge_temil_connectivity_simconnection_status_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Connectivity-SIMConnectionStatus.top_of_page">com.lge.temil.Connectivity.SIMConnectionStatus</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_connectivity_simconnection_status_proxy_new_finish() to get the result of the operation.
 *
 * See com_lge_temil_connectivity_simconnection_status_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
com_lge_temil_connectivity_simconnection_status_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_COM_LGE_TEMIL_CONNECTIVITY_SIMCONNECTION_STATUS_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Connectivity.SIMConnectionStatus", NULL);
}

/**
 * com_lge_temil_connectivity_simconnection_status_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_connectivity_simconnection_status_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with com_lge_temil_connectivity_simconnection_status_proxy_new().
 *
 * Returns: (transfer full) (type ComLgeTemilConnectivitySIMConnectionStatusProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilConnectivitySIMConnectionStatus *
com_lge_temil_connectivity_simconnection_status_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return COM_LGE_TEMIL_CONNECTIVITY_SIMCONNECTION_STATUS (ret);
  else
    return NULL;
}

/**
 * com_lge_temil_connectivity_simconnection_status_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Connectivity-SIMConnectionStatus.top_of_page">com.lge.temil.Connectivity.SIMConnectionStatus</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_connectivity_simconnection_status_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type ComLgeTemilConnectivitySIMConnectionStatusProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilConnectivitySIMConnectionStatus *
com_lge_temil_connectivity_simconnection_status_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_COM_LGE_TEMIL_CONNECTIVITY_SIMCONNECTION_STATUS_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Connectivity.SIMConnectionStatus", NULL);
  if (ret != NULL)
    return COM_LGE_TEMIL_CONNECTIVITY_SIMCONNECTION_STATUS (ret);
  else
    return NULL;
}


/**
 * com_lge_temil_connectivity_simconnection_status_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like com_lge_temil_connectivity_simconnection_status_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_connectivity_simconnection_status_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See com_lge_temil_connectivity_simconnection_status_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
com_lge_temil_connectivity_simconnection_status_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_COM_LGE_TEMIL_CONNECTIVITY_SIMCONNECTION_STATUS_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Connectivity.SIMConnectionStatus", NULL);
}

/**
 * com_lge_temil_connectivity_simconnection_status_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_connectivity_simconnection_status_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with com_lge_temil_connectivity_simconnection_status_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type ComLgeTemilConnectivitySIMConnectionStatusProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilConnectivitySIMConnectionStatus *
com_lge_temil_connectivity_simconnection_status_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return COM_LGE_TEMIL_CONNECTIVITY_SIMCONNECTION_STATUS (ret);
  else
    return NULL;
}

/**
 * com_lge_temil_connectivity_simconnection_status_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like com_lge_temil_connectivity_simconnection_status_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_connectivity_simconnection_status_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type ComLgeTemilConnectivitySIMConnectionStatusProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilConnectivitySIMConnectionStatus *
com_lge_temil_connectivity_simconnection_status_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_COM_LGE_TEMIL_CONNECTIVITY_SIMCONNECTION_STATUS_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Connectivity.SIMConnectionStatus", NULL);
  if (ret != NULL)
    return COM_LGE_TEMIL_CONNECTIVITY_SIMCONNECTION_STATUS (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * ComLgeTemilConnectivitySIMConnectionStatusSkeleton:
 *
 * The #ComLgeTemilConnectivitySIMConnectionStatusSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * ComLgeTemilConnectivitySIMConnectionStatusSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #ComLgeTemilConnectivitySIMConnectionStatusSkeleton.
 */

struct _ComLgeTemilConnectivitySIMConnectionStatusSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_com_lge_temil_connectivity_simconnection_status_skeleton_handle_method_call (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  ComLgeTemilConnectivitySIMConnectionStatusSkeleton *skeleton = COM_LGE_TEMIL_CONNECTIVITY_SIMCONNECTION_STATUS_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], TYPE_COM_LGE_TEMIL_CONNECTIVITY_SIMCONNECTION_STATUS);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_COM_LGE_TEMIL_CONNECTIVITY_SIMCONNECTION_STATUS);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_com_lge_temil_connectivity_simconnection_status_skeleton_handle_get_property (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  ComLgeTemilConnectivitySIMConnectionStatusSkeleton *skeleton = COM_LGE_TEMIL_CONNECTIVITY_SIMCONNECTION_STATUS_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_connectivity_simconnection_status_interface_info, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_com_lge_temil_connectivity_simconnection_status_skeleton_handle_set_property (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  ComLgeTemilConnectivitySIMConnectionStatusSkeleton *skeleton = COM_LGE_TEMIL_CONNECTIVITY_SIMCONNECTION_STATUS_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_connectivity_simconnection_status_interface_info, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _com_lge_temil_connectivity_simconnection_status_skeleton_vtable =
{
  _com_lge_temil_connectivity_simconnection_status_skeleton_handle_method_call,
  _com_lge_temil_connectivity_simconnection_status_skeleton_handle_get_property,
  _com_lge_temil_connectivity_simconnection_status_skeleton_handle_set_property
};

static GDBusInterfaceInfo *
com_lge_temil_connectivity_simconnection_status_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton)
{
  return com_lge_temil_connectivity_simconnection_status_interface_info ();
}

static GDBusInterfaceVTable *
com_lge_temil_connectivity_simconnection_status_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton)
{
  return (GDBusInterfaceVTable *) &_com_lge_temil_connectivity_simconnection_status_skeleton_vtable;
}

static GVariant *
com_lge_temil_connectivity_simconnection_status_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  ComLgeTemilConnectivitySIMConnectionStatusSkeleton *skeleton = COM_LGE_TEMIL_CONNECTIVITY_SIMCONNECTION_STATUS_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_com_lge_temil_connectivity_simconnection_status_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _com_lge_temil_connectivity_simconnection_status_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _com_lge_temil_connectivity_simconnection_status_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _com_lge_temil_connectivity_simconnection_status_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.lge.temil.Connectivity.SIMConnectionStatus", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static void
com_lge_temil_connectivity_simconnection_status_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
}

static void
_com_lge_temil_connectivity_simconnection_status_on_signal_notification (
    ComLgeTemilConnectivitySIMConnectionStatus *object,
    guint arg_status,
    guint arg_technology)
{
  ComLgeTemilConnectivitySIMConnectionStatusSkeleton *skeleton = COM_LGE_TEMIL_CONNECTIVITY_SIMCONNECTION_STATUS_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(uu)",
                   arg_status,
                   arg_technology));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.lge.temil.Connectivity.SIMConnectionStatus", "Notification",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void com_lge_temil_connectivity_simconnection_status_skeleton_iface_init (ComLgeTemilConnectivitySIMConnectionStatusIface *iface);
G_DEFINE_TYPE_WITH_CODE (ComLgeTemilConnectivitySIMConnectionStatusSkeleton, com_lge_temil_connectivity_simconnection_status_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (TYPE_COM_LGE_TEMIL_CONNECTIVITY_SIMCONNECTION_STATUS, com_lge_temil_connectivity_simconnection_status_skeleton_iface_init));

static void
com_lge_temil_connectivity_simconnection_status_skeleton_finalize (GObject *object)
{
  ComLgeTemilConnectivitySIMConnectionStatusSkeleton *skeleton = COM_LGE_TEMIL_CONNECTIVITY_SIMCONNECTION_STATUS_SKELETON (object);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (com_lge_temil_connectivity_simconnection_status_skeleton_parent_class)->finalize (object);
}

static void
com_lge_temil_connectivity_simconnection_status_skeleton_init (ComLgeTemilConnectivitySIMConnectionStatusSkeleton *skeleton)
{
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, TYPE_COM_LGE_TEMIL_CONNECTIVITY_SIMCONNECTION_STATUS_SKELETON, ComLgeTemilConnectivitySIMConnectionStatusSkeletonPrivate);
  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
}

static void
com_lge_temil_connectivity_simconnection_status_skeleton_class_init (ComLgeTemilConnectivitySIMConnectionStatusSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  g_type_class_add_private (klass, sizeof (ComLgeTemilConnectivitySIMConnectionStatusSkeletonPrivate));

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = com_lge_temil_connectivity_simconnection_status_skeleton_finalize;

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = com_lge_temil_connectivity_simconnection_status_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = com_lge_temil_connectivity_simconnection_status_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = com_lge_temil_connectivity_simconnection_status_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = com_lge_temil_connectivity_simconnection_status_skeleton_dbus_interface_get_vtable;
}

static void
com_lge_temil_connectivity_simconnection_status_skeleton_iface_init (ComLgeTemilConnectivitySIMConnectionStatusIface *iface)
{
  iface->notification = _com_lge_temil_connectivity_simconnection_status_on_signal_notification;
}

/**
 * com_lge_temil_connectivity_simconnection_status_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Connectivity-SIMConnectionStatus.top_of_page">com.lge.temil.Connectivity.SIMConnectionStatus</link>.
 *
 * Returns: (transfer full) (type ComLgeTemilConnectivitySIMConnectionStatusSkeleton): The skeleton object.
 */
ComLgeTemilConnectivitySIMConnectionStatus *
com_lge_temil_connectivity_simconnection_status_skeleton_new (void)
{
  return COM_LGE_TEMIL_CONNECTIVITY_SIMCONNECTION_STATUS (g_object_new (TYPE_COM_LGE_TEMIL_CONNECTIVITY_SIMCONNECTION_STATUS_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface com.lge.temil.Connectivity.SOSStatus
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:ComLgeTemilConnectivitySOSStatus
 * @title: ComLgeTemilConnectivitySOSStatus
 * @short_description: Generated C code for the com.lge.temil.Connectivity.SOSStatus D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-com-lge-temil-Connectivity-SOSStatus.top_of_page">com.lge.temil.Connectivity.SOSStatus</link> D-Bus interface in C.
 */

/* ---- Introspection data for com.lge.temil.Connectivity.SOSStatus ---- */

static const _ExtendedGDBusArgInfo _com_lge_temil_connectivity_sosstatus_method_info_request_OUT_ARG_status =
{
  {
    -1,
    "status",
    "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _com_lge_temil_connectivity_sosstatus_method_info_request_OUT_ARG_pointers[] =
{
  &_com_lge_temil_connectivity_sosstatus_method_info_request_OUT_ARG_status,
  NULL
};

static const _ExtendedGDBusMethodInfo _com_lge_temil_connectivity_sosstatus_method_info_request =
{
  {
    -1,
    "Request",
    NULL,
    (GDBusArgInfo **) &_com_lge_temil_connectivity_sosstatus_method_info_request_OUT_ARG_pointers,
    NULL
  },
  "handle-request",
  FALSE
};

static const _ExtendedGDBusArgInfo _com_lge_temil_connectivity_sosstatus_method_info_notification_request_IN_ARG_subscribed =
{
  {
    -1,
    "subscribed",
    "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _com_lge_temil_connectivity_sosstatus_method_info_notification_request_IN_ARG_pointers[] =
{
  &_com_lge_temil_connectivity_sosstatus_method_info_notification_request_IN_ARG_subscribed,
  NULL
};

static const _ExtendedGDBusMethodInfo _com_lge_temil_connectivity_sosstatus_method_info_notification_request =
{
  {
    -1,
    "NotificationRequest",
    (GDBusArgInfo **) &_com_lge_temil_connectivity_sosstatus_method_info_notification_request_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-notification-request",
  FALSE
};

static const _ExtendedGDBusMethodInfo * const _com_lge_temil_connectivity_sosstatus_method_info_pointers[] =
{
  &_com_lge_temil_connectivity_sosstatus_method_info_request,
  &_com_lge_temil_connectivity_sosstatus_method_info_notification_request,
  NULL
};

static const _ExtendedGDBusArgInfo _com_lge_temil_connectivity_sosstatus_signal_info_notification_ARG_status =
{
  {
    -1,
    "status",
    "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _com_lge_temil_connectivity_sosstatus_signal_info_notification_ARG_pointers[] =
{
  &_com_lge_temil_connectivity_sosstatus_signal_info_notification_ARG_status,
  NULL
};

static const _ExtendedGDBusSignalInfo _com_lge_temil_connectivity_sosstatus_signal_info_notification =
{
  {
    -1,
    "Notification",
    (GDBusArgInfo **) &_com_lge_temil_connectivity_sosstatus_signal_info_notification_ARG_pointers,
    NULL
  },
  "notification"
};

static const _ExtendedGDBusSignalInfo * const _com_lge_temil_connectivity_sosstatus_signal_info_pointers[] =
{
  &_com_lge_temil_connectivity_sosstatus_signal_info_notification,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _com_lge_temil_connectivity_sosstatus_interface_info =
{
  {
    -1,
    "com.lge.temil.Connectivity.SOSStatus",
    (GDBusMethodInfo **) &_com_lge_temil_connectivity_sosstatus_method_info_pointers,
    (GDBusSignalInfo **) &_com_lge_temil_connectivity_sosstatus_signal_info_pointers,
    NULL,
    NULL
  },
  "com-lge-temil-connectivity-sosstatus",
};


/**
 * com_lge_temil_connectivity_sosstatus_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-com-lge-temil-Connectivity-SOSStatus.top_of_page">com.lge.temil.Connectivity.SOSStatus</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
com_lge_temil_connectivity_sosstatus_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_com_lge_temil_connectivity_sosstatus_interface_info;
}

/**
 * com_lge_temil_connectivity_sosstatus_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #ComLgeTemilConnectivitySOSStatus interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
com_lge_temil_connectivity_sosstatus_override_properties (GObjectClass *klass, guint property_id_begin)
{
  return property_id_begin - 1;
}



/**
 * ComLgeTemilConnectivitySOSStatus:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Connectivity-SOSStatus.top_of_page">com.lge.temil.Connectivity.SOSStatus</link>.
 */

/**
 * ComLgeTemilConnectivitySOSStatusIface:
 * @parent_iface: The parent interface.
 * @handle_notification_request: Handler for the #ComLgeTemilConnectivitySOSStatus::handle-notification-request signal.
 * @handle_request: Handler for the #ComLgeTemilConnectivitySOSStatus::handle-request signal.
 * @notification: Handler for the #ComLgeTemilConnectivitySOSStatus::notification signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Connectivity-SOSStatus.top_of_page">com.lge.temil.Connectivity.SOSStatus</link>.
 */

static void
com_lge_temil_connectivity_sosstatus_default_init (ComLgeTemilConnectivitySOSStatusIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * ComLgeTemilConnectivitySOSStatus::handle-request:
   * @object: A #ComLgeTemilConnectivitySOSStatus.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-lge-temil-Connectivity-SOSStatus.Request">Request()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call com_lge_temil_connectivity_sosstatus_complete_request() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-request",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (ComLgeTemilConnectivitySOSStatusIface, handle_request),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * ComLgeTemilConnectivitySOSStatus::handle-notification-request:
   * @object: A #ComLgeTemilConnectivitySOSStatus.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_subscribed: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-lge-temil-Connectivity-SOSStatus.NotificationRequest">NotificationRequest()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call com_lge_temil_connectivity_sosstatus_complete_notification_request() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-notification-request",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (ComLgeTemilConnectivitySOSStatusIface, handle_notification_request),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_BOOLEAN);

  /* GObject signals for received D-Bus signals: */
  /**
   * ComLgeTemilConnectivitySOSStatus::notification:
   * @object: A #ComLgeTemilConnectivitySOSStatus.
   * @arg_status: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-lge-temil-Connectivity-SOSStatus.Notification">"Notification"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("notification",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (ComLgeTemilConnectivitySOSStatusIface, notification),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    1, G_TYPE_UINT);

}

typedef ComLgeTemilConnectivitySOSStatusIface ComLgeTemilConnectivitySOSStatusInterface;
G_DEFINE_INTERFACE (ComLgeTemilConnectivitySOSStatus, com_lge_temil_connectivity_sosstatus, G_TYPE_OBJECT);

/**
 * com_lge_temil_connectivity_sosstatus_emit_notification:
 * @object: A #ComLgeTemilConnectivitySOSStatus.
 * @arg_status: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-com-lge-temil-Connectivity-SOSStatus.Notification">"Notification"</link> D-Bus signal.
 */
void
com_lge_temil_connectivity_sosstatus_emit_notification (
    ComLgeTemilConnectivitySOSStatus *object,
    guint arg_status)
{
  g_signal_emit_by_name (object, "notification", arg_status);
}

/**
 * com_lge_temil_connectivity_sosstatus_call_request:
 * @proxy: A #ComLgeTemilConnectivitySOSStatusProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-lge-temil-Connectivity-SOSStatus.Request">Request()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_connectivity_sosstatus_call_request_finish() to get the result of the operation.
 *
 * See com_lge_temil_connectivity_sosstatus_call_request_sync() for the synchronous, blocking version of this method.
 */
void
com_lge_temil_connectivity_sosstatus_call_request (
    ComLgeTemilConnectivitySOSStatus *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Request",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * com_lge_temil_connectivity_sosstatus_call_request_finish:
 * @proxy: A #ComLgeTemilConnectivitySOSStatusProxy.
 * @out_status: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_connectivity_sosstatus_call_request().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with com_lge_temil_connectivity_sosstatus_call_request().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_connectivity_sosstatus_call_request_finish (
    ComLgeTemilConnectivitySOSStatus *proxy,
    guint *out_status,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(u)",
                 out_status);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_connectivity_sosstatus_call_request_sync:
 * @proxy: A #ComLgeTemilConnectivitySOSStatusProxy.
 * @out_status: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-lge-temil-Connectivity-SOSStatus.Request">Request()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_connectivity_sosstatus_call_request() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_connectivity_sosstatus_call_request_sync (
    ComLgeTemilConnectivitySOSStatus *proxy,
    guint *out_status,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Request",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(u)",
                 out_status);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_connectivity_sosstatus_call_notification_request:
 * @proxy: A #ComLgeTemilConnectivitySOSStatusProxy.
 * @arg_subscribed: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-lge-temil-Connectivity-SOSStatus.NotificationRequest">NotificationRequest()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_connectivity_sosstatus_call_notification_request_finish() to get the result of the operation.
 *
 * See com_lge_temil_connectivity_sosstatus_call_notification_request_sync() for the synchronous, blocking version of this method.
 */
void
com_lge_temil_connectivity_sosstatus_call_notification_request (
    ComLgeTemilConnectivitySOSStatus *proxy,
    gboolean arg_subscribed,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "NotificationRequest",
    g_variant_new ("(b)",
                   arg_subscribed),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * com_lge_temil_connectivity_sosstatus_call_notification_request_finish:
 * @proxy: A #ComLgeTemilConnectivitySOSStatusProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_connectivity_sosstatus_call_notification_request().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with com_lge_temil_connectivity_sosstatus_call_notification_request().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_connectivity_sosstatus_call_notification_request_finish (
    ComLgeTemilConnectivitySOSStatus *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_connectivity_sosstatus_call_notification_request_sync:
 * @proxy: A #ComLgeTemilConnectivitySOSStatusProxy.
 * @arg_subscribed: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-lge-temil-Connectivity-SOSStatus.NotificationRequest">NotificationRequest()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_connectivity_sosstatus_call_notification_request() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_connectivity_sosstatus_call_notification_request_sync (
    ComLgeTemilConnectivitySOSStatus *proxy,
    gboolean arg_subscribed,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "NotificationRequest",
    g_variant_new ("(b)",
                   arg_subscribed),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_connectivity_sosstatus_complete_request:
 * @object: A #ComLgeTemilConnectivitySOSStatus.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @status: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-lge-temil-Connectivity-SOSStatus.Request">Request()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
com_lge_temil_connectivity_sosstatus_complete_request (
    ComLgeTemilConnectivitySOSStatus *object,
    GDBusMethodInvocation *invocation,
    guint status)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(u)",
                   status));
}

/**
 * com_lge_temil_connectivity_sosstatus_complete_notification_request:
 * @object: A #ComLgeTemilConnectivitySOSStatus.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-lge-temil-Connectivity-SOSStatus.NotificationRequest">NotificationRequest()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
com_lge_temil_connectivity_sosstatus_complete_notification_request (
    ComLgeTemilConnectivitySOSStatus *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * ComLgeTemilConnectivitySOSStatusProxy:
 *
 * The #ComLgeTemilConnectivitySOSStatusProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * ComLgeTemilConnectivitySOSStatusProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #ComLgeTemilConnectivitySOSStatusProxy.
 */

struct _ComLgeTemilConnectivitySOSStatusProxyPrivate
{
  GData *qdata;
};

static void com_lge_temil_connectivity_sosstatus_proxy_iface_init (ComLgeTemilConnectivitySOSStatusIface *iface);

G_DEFINE_TYPE_WITH_CODE (ComLgeTemilConnectivitySOSStatusProxy, com_lge_temil_connectivity_sosstatus_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (TYPE_COM_LGE_TEMIL_CONNECTIVITY_SOSSTATUS, com_lge_temil_connectivity_sosstatus_proxy_iface_init));

static void
com_lge_temil_connectivity_sosstatus_proxy_finalize (GObject *object)
{
  ComLgeTemilConnectivitySOSStatusProxy *proxy = COM_LGE_TEMIL_CONNECTIVITY_SOSSTATUS_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (com_lge_temil_connectivity_sosstatus_proxy_parent_class)->finalize (object);
}

static void
com_lge_temil_connectivity_sosstatus_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec)
{
}

static void
com_lge_temil_connectivity_sosstatus_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
}

static void
com_lge_temil_connectivity_sosstatus_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_com_lge_temil_connectivity_sosstatus_interface_info, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], TYPE_COM_LGE_TEMIL_CONNECTIVITY_SOSSTATUS);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_COM_LGE_TEMIL_CONNECTIVITY_SOSSTATUS);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
com_lge_temil_connectivity_sosstatus_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  ComLgeTemilConnectivitySOSStatusProxy *proxy = COM_LGE_TEMIL_CONNECTIVITY_SOSSTATUS_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_connectivity_sosstatus_interface_info, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_connectivity_sosstatus_interface_info, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static void
com_lge_temil_connectivity_sosstatus_proxy_init (ComLgeTemilConnectivitySOSStatusProxy *proxy)
{
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, TYPE_COM_LGE_TEMIL_CONNECTIVITY_SOSSTATUS_PROXY, ComLgeTemilConnectivitySOSStatusProxyPrivate);
  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), com_lge_temil_connectivity_sosstatus_interface_info ());
}

static void
com_lge_temil_connectivity_sosstatus_proxy_class_init (ComLgeTemilConnectivitySOSStatusProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  g_type_class_add_private (klass, sizeof (ComLgeTemilConnectivitySOSStatusProxyPrivate));

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = com_lge_temil_connectivity_sosstatus_proxy_finalize;
  gobject_class->get_property = com_lge_temil_connectivity_sosstatus_proxy_get_property;
  gobject_class->set_property = com_lge_temil_connectivity_sosstatus_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = com_lge_temil_connectivity_sosstatus_proxy_g_signal;
  proxy_class->g_properties_changed = com_lge_temil_connectivity_sosstatus_proxy_g_properties_changed;

}

static void
com_lge_temil_connectivity_sosstatus_proxy_iface_init (ComLgeTemilConnectivitySOSStatusIface *iface)
{
}

/**
 * com_lge_temil_connectivity_sosstatus_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Connectivity-SOSStatus.top_of_page">com.lge.temil.Connectivity.SOSStatus</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_connectivity_sosstatus_proxy_new_finish() to get the result of the operation.
 *
 * See com_lge_temil_connectivity_sosstatus_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
com_lge_temil_connectivity_sosstatus_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_COM_LGE_TEMIL_CONNECTIVITY_SOSSTATUS_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Connectivity.SOSStatus", NULL);
}

/**
 * com_lge_temil_connectivity_sosstatus_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_connectivity_sosstatus_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with com_lge_temil_connectivity_sosstatus_proxy_new().
 *
 * Returns: (transfer full) (type ComLgeTemilConnectivitySOSStatusProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilConnectivitySOSStatus *
com_lge_temil_connectivity_sosstatus_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return COM_LGE_TEMIL_CONNECTIVITY_SOSSTATUS (ret);
  else
    return NULL;
}

/**
 * com_lge_temil_connectivity_sosstatus_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Connectivity-SOSStatus.top_of_page">com.lge.temil.Connectivity.SOSStatus</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_connectivity_sosstatus_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type ComLgeTemilConnectivitySOSStatusProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilConnectivitySOSStatus *
com_lge_temil_connectivity_sosstatus_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_COM_LGE_TEMIL_CONNECTIVITY_SOSSTATUS_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Connectivity.SOSStatus", NULL);
  if (ret != NULL)
    return COM_LGE_TEMIL_CONNECTIVITY_SOSSTATUS (ret);
  else
    return NULL;
}


/**
 * com_lge_temil_connectivity_sosstatus_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like com_lge_temil_connectivity_sosstatus_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_connectivity_sosstatus_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See com_lge_temil_connectivity_sosstatus_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
com_lge_temil_connectivity_sosstatus_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_COM_LGE_TEMIL_CONNECTIVITY_SOSSTATUS_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Connectivity.SOSStatus", NULL);
}

/**
 * com_lge_temil_connectivity_sosstatus_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_connectivity_sosstatus_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with com_lge_temil_connectivity_sosstatus_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type ComLgeTemilConnectivitySOSStatusProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilConnectivitySOSStatus *
com_lge_temil_connectivity_sosstatus_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return COM_LGE_TEMIL_CONNECTIVITY_SOSSTATUS (ret);
  else
    return NULL;
}

/**
 * com_lge_temil_connectivity_sosstatus_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like com_lge_temil_connectivity_sosstatus_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_connectivity_sosstatus_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type ComLgeTemilConnectivitySOSStatusProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilConnectivitySOSStatus *
com_lge_temil_connectivity_sosstatus_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_COM_LGE_TEMIL_CONNECTIVITY_SOSSTATUS_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Connectivity.SOSStatus", NULL);
  if (ret != NULL)
    return COM_LGE_TEMIL_CONNECTIVITY_SOSSTATUS (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * ComLgeTemilConnectivitySOSStatusSkeleton:
 *
 * The #ComLgeTemilConnectivitySOSStatusSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * ComLgeTemilConnectivitySOSStatusSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #ComLgeTemilConnectivitySOSStatusSkeleton.
 */

struct _ComLgeTemilConnectivitySOSStatusSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_com_lge_temil_connectivity_sosstatus_skeleton_handle_method_call (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  ComLgeTemilConnectivitySOSStatusSkeleton *skeleton = COM_LGE_TEMIL_CONNECTIVITY_SOSSTATUS_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], TYPE_COM_LGE_TEMIL_CONNECTIVITY_SOSSTATUS);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_COM_LGE_TEMIL_CONNECTIVITY_SOSSTATUS);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_com_lge_temil_connectivity_sosstatus_skeleton_handle_get_property (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  ComLgeTemilConnectivitySOSStatusSkeleton *skeleton = COM_LGE_TEMIL_CONNECTIVITY_SOSSTATUS_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_connectivity_sosstatus_interface_info, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_com_lge_temil_connectivity_sosstatus_skeleton_handle_set_property (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  ComLgeTemilConnectivitySOSStatusSkeleton *skeleton = COM_LGE_TEMIL_CONNECTIVITY_SOSSTATUS_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_connectivity_sosstatus_interface_info, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _com_lge_temil_connectivity_sosstatus_skeleton_vtable =
{
  _com_lge_temil_connectivity_sosstatus_skeleton_handle_method_call,
  _com_lge_temil_connectivity_sosstatus_skeleton_handle_get_property,
  _com_lge_temil_connectivity_sosstatus_skeleton_handle_set_property
};

static GDBusInterfaceInfo *
com_lge_temil_connectivity_sosstatus_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton)
{
  return com_lge_temil_connectivity_sosstatus_interface_info ();
}

static GDBusInterfaceVTable *
com_lge_temil_connectivity_sosstatus_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton)
{
  return (GDBusInterfaceVTable *) &_com_lge_temil_connectivity_sosstatus_skeleton_vtable;
}

static GVariant *
com_lge_temil_connectivity_sosstatus_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  ComLgeTemilConnectivitySOSStatusSkeleton *skeleton = COM_LGE_TEMIL_CONNECTIVITY_SOSSTATUS_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_com_lge_temil_connectivity_sosstatus_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _com_lge_temil_connectivity_sosstatus_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _com_lge_temil_connectivity_sosstatus_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _com_lge_temil_connectivity_sosstatus_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.lge.temil.Connectivity.SOSStatus", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static void
com_lge_temil_connectivity_sosstatus_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
}

static void
_com_lge_temil_connectivity_sosstatus_on_signal_notification (
    ComLgeTemilConnectivitySOSStatus *object,
    guint arg_status)
{
  ComLgeTemilConnectivitySOSStatusSkeleton *skeleton = COM_LGE_TEMIL_CONNECTIVITY_SOSSTATUS_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(u)",
                   arg_status));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.lge.temil.Connectivity.SOSStatus", "Notification",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void com_lge_temil_connectivity_sosstatus_skeleton_iface_init (ComLgeTemilConnectivitySOSStatusIface *iface);
G_DEFINE_TYPE_WITH_CODE (ComLgeTemilConnectivitySOSStatusSkeleton, com_lge_temil_connectivity_sosstatus_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (TYPE_COM_LGE_TEMIL_CONNECTIVITY_SOSSTATUS, com_lge_temil_connectivity_sosstatus_skeleton_iface_init));

static void
com_lge_temil_connectivity_sosstatus_skeleton_finalize (GObject *object)
{
  ComLgeTemilConnectivitySOSStatusSkeleton *skeleton = COM_LGE_TEMIL_CONNECTIVITY_SOSSTATUS_SKELETON (object);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (com_lge_temil_connectivity_sosstatus_skeleton_parent_class)->finalize (object);
}

static void
com_lge_temil_connectivity_sosstatus_skeleton_init (ComLgeTemilConnectivitySOSStatusSkeleton *skeleton)
{
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, TYPE_COM_LGE_TEMIL_CONNECTIVITY_SOSSTATUS_SKELETON, ComLgeTemilConnectivitySOSStatusSkeletonPrivate);
  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
}

static void
com_lge_temil_connectivity_sosstatus_skeleton_class_init (ComLgeTemilConnectivitySOSStatusSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  g_type_class_add_private (klass, sizeof (ComLgeTemilConnectivitySOSStatusSkeletonPrivate));

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = com_lge_temil_connectivity_sosstatus_skeleton_finalize;

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = com_lge_temil_connectivity_sosstatus_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = com_lge_temil_connectivity_sosstatus_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = com_lge_temil_connectivity_sosstatus_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = com_lge_temil_connectivity_sosstatus_skeleton_dbus_interface_get_vtable;
}

static void
com_lge_temil_connectivity_sosstatus_skeleton_iface_init (ComLgeTemilConnectivitySOSStatusIface *iface)
{
  iface->notification = _com_lge_temil_connectivity_sosstatus_on_signal_notification;
}

/**
 * com_lge_temil_connectivity_sosstatus_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Connectivity-SOSStatus.top_of_page">com.lge.temil.Connectivity.SOSStatus</link>.
 *
 * Returns: (transfer full) (type ComLgeTemilConnectivitySOSStatusSkeleton): The skeleton object.
 */
ComLgeTemilConnectivitySOSStatus *
com_lge_temil_connectivity_sosstatus_skeleton_new (void)
{
  return COM_LGE_TEMIL_CONNECTIVITY_SOSSTATUS (g_object_new (TYPE_COM_LGE_TEMIL_CONNECTIVITY_SOSSTATUS_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface com.lge.temil.Connectivity.ConnectivityInhibition
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:ComLgeTemilConnectivityConnectivityInhibition
 * @title: ComLgeTemilConnectivityConnectivityInhibition
 * @short_description: Generated C code for the com.lge.temil.Connectivity.ConnectivityInhibition D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-com-lge-temil-Connectivity-ConnectivityInhibition.top_of_page">com.lge.temil.Connectivity.ConnectivityInhibition</link> D-Bus interface in C.
 */

/* ---- Introspection data for com.lge.temil.Connectivity.ConnectivityInhibition ---- */

static const _ExtendedGDBusArgInfo _com_lge_temil_connectivity_connectivity_inhibition_method_info_set_request_IN_ARG_ConnectivityInhibition =
{
  {
    -1,
    "ConnectivityInhibition",
    "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _com_lge_temil_connectivity_connectivity_inhibition_method_info_set_request_IN_ARG_pointers[] =
{
  &_com_lge_temil_connectivity_connectivity_inhibition_method_info_set_request_IN_ARG_ConnectivityInhibition,
  NULL
};

static const _ExtendedGDBusArgInfo _com_lge_temil_connectivity_connectivity_inhibition_method_info_set_request_OUT_ARG_ConnectivityInhibitionResult =
{
  {
    -1,
    "ConnectivityInhibitionResult",
    "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _com_lge_temil_connectivity_connectivity_inhibition_method_info_set_request_OUT_ARG_pointers[] =
{
  &_com_lge_temil_connectivity_connectivity_inhibition_method_info_set_request_OUT_ARG_ConnectivityInhibitionResult,
  NULL
};

static const _ExtendedGDBusMethodInfo _com_lge_temil_connectivity_connectivity_inhibition_method_info_set_request =
{
  {
    -1,
    "SetRequest",
    (GDBusArgInfo **) &_com_lge_temil_connectivity_connectivity_inhibition_method_info_set_request_IN_ARG_pointers,
    (GDBusArgInfo **) &_com_lge_temil_connectivity_connectivity_inhibition_method_info_set_request_OUT_ARG_pointers,
    NULL
  },
  "handle-set-request",
  FALSE
};

static const _ExtendedGDBusArgInfo _com_lge_temil_connectivity_connectivity_inhibition_method_info_request_OUT_ARG_ConnectivityInhibitionResult =
{
  {
    -1,
    "ConnectivityInhibitionResult",
    "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _com_lge_temil_connectivity_connectivity_inhibition_method_info_request_OUT_ARG_pointers[] =
{
  &_com_lge_temil_connectivity_connectivity_inhibition_method_info_request_OUT_ARG_ConnectivityInhibitionResult,
  NULL
};

static const _ExtendedGDBusMethodInfo _com_lge_temil_connectivity_connectivity_inhibition_method_info_request =
{
  {
    -1,
    "Request",
    NULL,
    (GDBusArgInfo **) &_com_lge_temil_connectivity_connectivity_inhibition_method_info_request_OUT_ARG_pointers,
    NULL
  },
  "handle-request",
  FALSE
};

static const _ExtendedGDBusArgInfo _com_lge_temil_connectivity_connectivity_inhibition_method_info_notification_request_IN_ARG_subscribed =
{
  {
    -1,
    "subscribed",
    "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _com_lge_temil_connectivity_connectivity_inhibition_method_info_notification_request_IN_ARG_pointers[] =
{
  &_com_lge_temil_connectivity_connectivity_inhibition_method_info_notification_request_IN_ARG_subscribed,
  NULL
};

static const _ExtendedGDBusMethodInfo _com_lge_temil_connectivity_connectivity_inhibition_method_info_notification_request =
{
  {
    -1,
    "NotificationRequest",
    (GDBusArgInfo **) &_com_lge_temil_connectivity_connectivity_inhibition_method_info_notification_request_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-notification-request",
  FALSE
};

static const _ExtendedGDBusMethodInfo * const _com_lge_temil_connectivity_connectivity_inhibition_method_info_pointers[] =
{
  &_com_lge_temil_connectivity_connectivity_inhibition_method_info_set_request,
  &_com_lge_temil_connectivity_connectivity_inhibition_method_info_request,
  &_com_lge_temil_connectivity_connectivity_inhibition_method_info_notification_request,
  NULL
};

static const _ExtendedGDBusArgInfo _com_lge_temil_connectivity_connectivity_inhibition_signal_info_notification_ARG_ConnectivityInhibitionResult =
{
  {
    -1,
    "ConnectivityInhibitionResult",
    "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _com_lge_temil_connectivity_connectivity_inhibition_signal_info_notification_ARG_pointers[] =
{
  &_com_lge_temil_connectivity_connectivity_inhibition_signal_info_notification_ARG_ConnectivityInhibitionResult,
  NULL
};

static const _ExtendedGDBusSignalInfo _com_lge_temil_connectivity_connectivity_inhibition_signal_info_notification =
{
  {
    -1,
    "Notification",
    (GDBusArgInfo **) &_com_lge_temil_connectivity_connectivity_inhibition_signal_info_notification_ARG_pointers,
    NULL
  },
  "notification"
};

static const _ExtendedGDBusSignalInfo * const _com_lge_temil_connectivity_connectivity_inhibition_signal_info_pointers[] =
{
  &_com_lge_temil_connectivity_connectivity_inhibition_signal_info_notification,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _com_lge_temil_connectivity_connectivity_inhibition_interface_info =
{
  {
    -1,
    "com.lge.temil.Connectivity.ConnectivityInhibition",
    (GDBusMethodInfo **) &_com_lge_temil_connectivity_connectivity_inhibition_method_info_pointers,
    (GDBusSignalInfo **) &_com_lge_temil_connectivity_connectivity_inhibition_signal_info_pointers,
    NULL,
    NULL
  },
  "com-lge-temil-connectivity-connectivity-inhibition",
};


/**
 * com_lge_temil_connectivity_connectivity_inhibition_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-com-lge-temil-Connectivity-ConnectivityInhibition.top_of_page">com.lge.temil.Connectivity.ConnectivityInhibition</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
com_lge_temil_connectivity_connectivity_inhibition_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_com_lge_temil_connectivity_connectivity_inhibition_interface_info;
}

/**
 * com_lge_temil_connectivity_connectivity_inhibition_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #ComLgeTemilConnectivityConnectivityInhibition interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
com_lge_temil_connectivity_connectivity_inhibition_override_properties (GObjectClass *klass, guint property_id_begin)
{
  return property_id_begin - 1;
}



/**
 * ComLgeTemilConnectivityConnectivityInhibition:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Connectivity-ConnectivityInhibition.top_of_page">com.lge.temil.Connectivity.ConnectivityInhibition</link>.
 */

/**
 * ComLgeTemilConnectivityConnectivityInhibitionIface:
 * @parent_iface: The parent interface.
 * @handle_notification_request: Handler for the #ComLgeTemilConnectivityConnectivityInhibition::handle-notification-request signal.
 * @handle_request: Handler for the #ComLgeTemilConnectivityConnectivityInhibition::handle-request signal.
 * @handle_set_request: Handler for the #ComLgeTemilConnectivityConnectivityInhibition::handle-set-request signal.
 * @notification: Handler for the #ComLgeTemilConnectivityConnectivityInhibition::notification signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Connectivity-ConnectivityInhibition.top_of_page">com.lge.temil.Connectivity.ConnectivityInhibition</link>.
 */

static void
com_lge_temil_connectivity_connectivity_inhibition_default_init (ComLgeTemilConnectivityConnectivityInhibitionIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * ComLgeTemilConnectivityConnectivityInhibition::handle-set-request:
   * @object: A #ComLgeTemilConnectivityConnectivityInhibition.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_ConnectivityInhibition: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-lge-temil-Connectivity-ConnectivityInhibition.SetRequest">SetRequest()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call com_lge_temil_connectivity_connectivity_inhibition_complete_set_request() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-set-request",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (ComLgeTemilConnectivityConnectivityInhibitionIface, handle_set_request),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_BOOLEAN);

  /**
   * ComLgeTemilConnectivityConnectivityInhibition::handle-request:
   * @object: A #ComLgeTemilConnectivityConnectivityInhibition.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-lge-temil-Connectivity-ConnectivityInhibition.Request">Request()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call com_lge_temil_connectivity_connectivity_inhibition_complete_request() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-request",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (ComLgeTemilConnectivityConnectivityInhibitionIface, handle_request),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * ComLgeTemilConnectivityConnectivityInhibition::handle-notification-request:
   * @object: A #ComLgeTemilConnectivityConnectivityInhibition.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_subscribed: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-lge-temil-Connectivity-ConnectivityInhibition.NotificationRequest">NotificationRequest()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call com_lge_temil_connectivity_connectivity_inhibition_complete_notification_request() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-notification-request",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (ComLgeTemilConnectivityConnectivityInhibitionIface, handle_notification_request),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_BOOLEAN);

  /* GObject signals for received D-Bus signals: */
  /**
   * ComLgeTemilConnectivityConnectivityInhibition::notification:
   * @object: A #ComLgeTemilConnectivityConnectivityInhibition.
   * @arg_ConnectivityInhibitionResult: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-lge-temil-Connectivity-ConnectivityInhibition.Notification">"Notification"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("notification",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (ComLgeTemilConnectivityConnectivityInhibitionIface, notification),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    1, G_TYPE_BOOLEAN);

}

typedef ComLgeTemilConnectivityConnectivityInhibitionIface ComLgeTemilConnectivityConnectivityInhibitionInterface;
G_DEFINE_INTERFACE (ComLgeTemilConnectivityConnectivityInhibition, com_lge_temil_connectivity_connectivity_inhibition, G_TYPE_OBJECT);

/**
 * com_lge_temil_connectivity_connectivity_inhibition_emit_notification:
 * @object: A #ComLgeTemilConnectivityConnectivityInhibition.
 * @arg_ConnectivityInhibitionResult: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-com-lge-temil-Connectivity-ConnectivityInhibition.Notification">"Notification"</link> D-Bus signal.
 */
void
com_lge_temil_connectivity_connectivity_inhibition_emit_notification (
    ComLgeTemilConnectivityConnectivityInhibition *object,
    gboolean arg_ConnectivityInhibitionResult)
{
  g_signal_emit_by_name (object, "notification", arg_ConnectivityInhibitionResult);
}

/**
 * com_lge_temil_connectivity_connectivity_inhibition_call_set_request:
 * @proxy: A #ComLgeTemilConnectivityConnectivityInhibitionProxy.
 * @arg_ConnectivityInhibition: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-lge-temil-Connectivity-ConnectivityInhibition.SetRequest">SetRequest()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_connectivity_connectivity_inhibition_call_set_request_finish() to get the result of the operation.
 *
 * See com_lge_temil_connectivity_connectivity_inhibition_call_set_request_sync() for the synchronous, blocking version of this method.
 */
void
com_lge_temil_connectivity_connectivity_inhibition_call_set_request (
    ComLgeTemilConnectivityConnectivityInhibition *proxy,
    gboolean arg_ConnectivityInhibition,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "SetRequest",
    g_variant_new ("(b)",
                   arg_ConnectivityInhibition),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * com_lge_temil_connectivity_connectivity_inhibition_call_set_request_finish:
 * @proxy: A #ComLgeTemilConnectivityConnectivityInhibitionProxy.
 * @out_ConnectivityInhibitionResult: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_connectivity_connectivity_inhibition_call_set_request().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with com_lge_temil_connectivity_connectivity_inhibition_call_set_request().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_connectivity_connectivity_inhibition_call_set_request_finish (
    ComLgeTemilConnectivityConnectivityInhibition *proxy,
    gboolean *out_ConnectivityInhibitionResult,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(b)",
                 out_ConnectivityInhibitionResult);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_connectivity_connectivity_inhibition_call_set_request_sync:
 * @proxy: A #ComLgeTemilConnectivityConnectivityInhibitionProxy.
 * @arg_ConnectivityInhibition: Argument to pass with the method invocation.
 * @out_ConnectivityInhibitionResult: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-lge-temil-Connectivity-ConnectivityInhibition.SetRequest">SetRequest()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_connectivity_connectivity_inhibition_call_set_request() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_connectivity_connectivity_inhibition_call_set_request_sync (
    ComLgeTemilConnectivityConnectivityInhibition *proxy,
    gboolean arg_ConnectivityInhibition,
    gboolean *out_ConnectivityInhibitionResult,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "SetRequest",
    g_variant_new ("(b)",
                   arg_ConnectivityInhibition),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(b)",
                 out_ConnectivityInhibitionResult);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_connectivity_connectivity_inhibition_call_request:
 * @proxy: A #ComLgeTemilConnectivityConnectivityInhibitionProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-lge-temil-Connectivity-ConnectivityInhibition.Request">Request()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_connectivity_connectivity_inhibition_call_request_finish() to get the result of the operation.
 *
 * See com_lge_temil_connectivity_connectivity_inhibition_call_request_sync() for the synchronous, blocking version of this method.
 */
void
com_lge_temil_connectivity_connectivity_inhibition_call_request (
    ComLgeTemilConnectivityConnectivityInhibition *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Request",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * com_lge_temil_connectivity_connectivity_inhibition_call_request_finish:
 * @proxy: A #ComLgeTemilConnectivityConnectivityInhibitionProxy.
 * @out_ConnectivityInhibitionResult: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_connectivity_connectivity_inhibition_call_request().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with com_lge_temil_connectivity_connectivity_inhibition_call_request().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_connectivity_connectivity_inhibition_call_request_finish (
    ComLgeTemilConnectivityConnectivityInhibition *proxy,
    gboolean *out_ConnectivityInhibitionResult,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(b)",
                 out_ConnectivityInhibitionResult);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_connectivity_connectivity_inhibition_call_request_sync:
 * @proxy: A #ComLgeTemilConnectivityConnectivityInhibitionProxy.
 * @out_ConnectivityInhibitionResult: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-lge-temil-Connectivity-ConnectivityInhibition.Request">Request()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_connectivity_connectivity_inhibition_call_request() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_connectivity_connectivity_inhibition_call_request_sync (
    ComLgeTemilConnectivityConnectivityInhibition *proxy,
    gboolean *out_ConnectivityInhibitionResult,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Request",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(b)",
                 out_ConnectivityInhibitionResult);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_connectivity_connectivity_inhibition_call_notification_request:
 * @proxy: A #ComLgeTemilConnectivityConnectivityInhibitionProxy.
 * @arg_subscribed: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-lge-temil-Connectivity-ConnectivityInhibition.NotificationRequest">NotificationRequest()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_connectivity_connectivity_inhibition_call_notification_request_finish() to get the result of the operation.
 *
 * See com_lge_temil_connectivity_connectivity_inhibition_call_notification_request_sync() for the synchronous, blocking version of this method.
 */
void
com_lge_temil_connectivity_connectivity_inhibition_call_notification_request (
    ComLgeTemilConnectivityConnectivityInhibition *proxy,
    gboolean arg_subscribed,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "NotificationRequest",
    g_variant_new ("(b)",
                   arg_subscribed),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * com_lge_temil_connectivity_connectivity_inhibition_call_notification_request_finish:
 * @proxy: A #ComLgeTemilConnectivityConnectivityInhibitionProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_connectivity_connectivity_inhibition_call_notification_request().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with com_lge_temil_connectivity_connectivity_inhibition_call_notification_request().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_connectivity_connectivity_inhibition_call_notification_request_finish (
    ComLgeTemilConnectivityConnectivityInhibition *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_connectivity_connectivity_inhibition_call_notification_request_sync:
 * @proxy: A #ComLgeTemilConnectivityConnectivityInhibitionProxy.
 * @arg_subscribed: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-lge-temil-Connectivity-ConnectivityInhibition.NotificationRequest">NotificationRequest()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_connectivity_connectivity_inhibition_call_notification_request() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_connectivity_connectivity_inhibition_call_notification_request_sync (
    ComLgeTemilConnectivityConnectivityInhibition *proxy,
    gboolean arg_subscribed,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "NotificationRequest",
    g_variant_new ("(b)",
                   arg_subscribed),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_connectivity_connectivity_inhibition_complete_set_request:
 * @object: A #ComLgeTemilConnectivityConnectivityInhibition.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @ConnectivityInhibitionResult: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-lge-temil-Connectivity-ConnectivityInhibition.SetRequest">SetRequest()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
com_lge_temil_connectivity_connectivity_inhibition_complete_set_request (
    ComLgeTemilConnectivityConnectivityInhibition *object,
    GDBusMethodInvocation *invocation,
    gboolean ConnectivityInhibitionResult)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(b)",
                   ConnectivityInhibitionResult));
}

/**
 * com_lge_temil_connectivity_connectivity_inhibition_complete_request:
 * @object: A #ComLgeTemilConnectivityConnectivityInhibition.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @ConnectivityInhibitionResult: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-lge-temil-Connectivity-ConnectivityInhibition.Request">Request()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
com_lge_temil_connectivity_connectivity_inhibition_complete_request (
    ComLgeTemilConnectivityConnectivityInhibition *object,
    GDBusMethodInvocation *invocation,
    gboolean ConnectivityInhibitionResult)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(b)",
                   ConnectivityInhibitionResult));
}

/**
 * com_lge_temil_connectivity_connectivity_inhibition_complete_notification_request:
 * @object: A #ComLgeTemilConnectivityConnectivityInhibition.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-lge-temil-Connectivity-ConnectivityInhibition.NotificationRequest">NotificationRequest()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
com_lge_temil_connectivity_connectivity_inhibition_complete_notification_request (
    ComLgeTemilConnectivityConnectivityInhibition *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * ComLgeTemilConnectivityConnectivityInhibitionProxy:
 *
 * The #ComLgeTemilConnectivityConnectivityInhibitionProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * ComLgeTemilConnectivityConnectivityInhibitionProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #ComLgeTemilConnectivityConnectivityInhibitionProxy.
 */

struct _ComLgeTemilConnectivityConnectivityInhibitionProxyPrivate
{
  GData *qdata;
};

static void com_lge_temil_connectivity_connectivity_inhibition_proxy_iface_init (ComLgeTemilConnectivityConnectivityInhibitionIface *iface);

G_DEFINE_TYPE_WITH_CODE (ComLgeTemilConnectivityConnectivityInhibitionProxy, com_lge_temil_connectivity_connectivity_inhibition_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (TYPE_COM_LGE_TEMIL_CONNECTIVITY_CONNECTIVITY_INHIBITION, com_lge_temil_connectivity_connectivity_inhibition_proxy_iface_init));

static void
com_lge_temil_connectivity_connectivity_inhibition_proxy_finalize (GObject *object)
{
  ComLgeTemilConnectivityConnectivityInhibitionProxy *proxy = COM_LGE_TEMIL_CONNECTIVITY_CONNECTIVITY_INHIBITION_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (com_lge_temil_connectivity_connectivity_inhibition_proxy_parent_class)->finalize (object);
}

static void
com_lge_temil_connectivity_connectivity_inhibition_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec)
{
}

static void
com_lge_temil_connectivity_connectivity_inhibition_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
}

static void
com_lge_temil_connectivity_connectivity_inhibition_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_com_lge_temil_connectivity_connectivity_inhibition_interface_info, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], TYPE_COM_LGE_TEMIL_CONNECTIVITY_CONNECTIVITY_INHIBITION);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_COM_LGE_TEMIL_CONNECTIVITY_CONNECTIVITY_INHIBITION);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
com_lge_temil_connectivity_connectivity_inhibition_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  ComLgeTemilConnectivityConnectivityInhibitionProxy *proxy = COM_LGE_TEMIL_CONNECTIVITY_CONNECTIVITY_INHIBITION_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_connectivity_connectivity_inhibition_interface_info, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_connectivity_connectivity_inhibition_interface_info, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static void
com_lge_temil_connectivity_connectivity_inhibition_proxy_init (ComLgeTemilConnectivityConnectivityInhibitionProxy *proxy)
{
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, TYPE_COM_LGE_TEMIL_CONNECTIVITY_CONNECTIVITY_INHIBITION_PROXY, ComLgeTemilConnectivityConnectivityInhibitionProxyPrivate);
  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), com_lge_temil_connectivity_connectivity_inhibition_interface_info ());
}

static void
com_lge_temil_connectivity_connectivity_inhibition_proxy_class_init (ComLgeTemilConnectivityConnectivityInhibitionProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  g_type_class_add_private (klass, sizeof (ComLgeTemilConnectivityConnectivityInhibitionProxyPrivate));

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = com_lge_temil_connectivity_connectivity_inhibition_proxy_finalize;
  gobject_class->get_property = com_lge_temil_connectivity_connectivity_inhibition_proxy_get_property;
  gobject_class->set_property = com_lge_temil_connectivity_connectivity_inhibition_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = com_lge_temil_connectivity_connectivity_inhibition_proxy_g_signal;
  proxy_class->g_properties_changed = com_lge_temil_connectivity_connectivity_inhibition_proxy_g_properties_changed;

}

static void
com_lge_temil_connectivity_connectivity_inhibition_proxy_iface_init (ComLgeTemilConnectivityConnectivityInhibitionIface *iface)
{
}

/**
 * com_lge_temil_connectivity_connectivity_inhibition_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Connectivity-ConnectivityInhibition.top_of_page">com.lge.temil.Connectivity.ConnectivityInhibition</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_connectivity_connectivity_inhibition_proxy_new_finish() to get the result of the operation.
 *
 * See com_lge_temil_connectivity_connectivity_inhibition_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
com_lge_temil_connectivity_connectivity_inhibition_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_COM_LGE_TEMIL_CONNECTIVITY_CONNECTIVITY_INHIBITION_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Connectivity.ConnectivityInhibition", NULL);
}

/**
 * com_lge_temil_connectivity_connectivity_inhibition_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_connectivity_connectivity_inhibition_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with com_lge_temil_connectivity_connectivity_inhibition_proxy_new().
 *
 * Returns: (transfer full) (type ComLgeTemilConnectivityConnectivityInhibitionProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilConnectivityConnectivityInhibition *
com_lge_temil_connectivity_connectivity_inhibition_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return COM_LGE_TEMIL_CONNECTIVITY_CONNECTIVITY_INHIBITION (ret);
  else
    return NULL;
}

/**
 * com_lge_temil_connectivity_connectivity_inhibition_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Connectivity-ConnectivityInhibition.top_of_page">com.lge.temil.Connectivity.ConnectivityInhibition</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_connectivity_connectivity_inhibition_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type ComLgeTemilConnectivityConnectivityInhibitionProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilConnectivityConnectivityInhibition *
com_lge_temil_connectivity_connectivity_inhibition_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_COM_LGE_TEMIL_CONNECTIVITY_CONNECTIVITY_INHIBITION_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Connectivity.ConnectivityInhibition", NULL);
  if (ret != NULL)
    return COM_LGE_TEMIL_CONNECTIVITY_CONNECTIVITY_INHIBITION (ret);
  else
    return NULL;
}


/**
 * com_lge_temil_connectivity_connectivity_inhibition_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like com_lge_temil_connectivity_connectivity_inhibition_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_connectivity_connectivity_inhibition_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See com_lge_temil_connectivity_connectivity_inhibition_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
com_lge_temil_connectivity_connectivity_inhibition_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_COM_LGE_TEMIL_CONNECTIVITY_CONNECTIVITY_INHIBITION_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Connectivity.ConnectivityInhibition", NULL);
}

/**
 * com_lge_temil_connectivity_connectivity_inhibition_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_connectivity_connectivity_inhibition_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with com_lge_temil_connectivity_connectivity_inhibition_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type ComLgeTemilConnectivityConnectivityInhibitionProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilConnectivityConnectivityInhibition *
com_lge_temil_connectivity_connectivity_inhibition_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return COM_LGE_TEMIL_CONNECTIVITY_CONNECTIVITY_INHIBITION (ret);
  else
    return NULL;
}

/**
 * com_lge_temil_connectivity_connectivity_inhibition_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like com_lge_temil_connectivity_connectivity_inhibition_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_connectivity_connectivity_inhibition_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type ComLgeTemilConnectivityConnectivityInhibitionProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilConnectivityConnectivityInhibition *
com_lge_temil_connectivity_connectivity_inhibition_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_COM_LGE_TEMIL_CONNECTIVITY_CONNECTIVITY_INHIBITION_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Connectivity.ConnectivityInhibition", NULL);
  if (ret != NULL)
    return COM_LGE_TEMIL_CONNECTIVITY_CONNECTIVITY_INHIBITION (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * ComLgeTemilConnectivityConnectivityInhibitionSkeleton:
 *
 * The #ComLgeTemilConnectivityConnectivityInhibitionSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * ComLgeTemilConnectivityConnectivityInhibitionSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #ComLgeTemilConnectivityConnectivityInhibitionSkeleton.
 */

struct _ComLgeTemilConnectivityConnectivityInhibitionSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_com_lge_temil_connectivity_connectivity_inhibition_skeleton_handle_method_call (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  ComLgeTemilConnectivityConnectivityInhibitionSkeleton *skeleton = COM_LGE_TEMIL_CONNECTIVITY_CONNECTIVITY_INHIBITION_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], TYPE_COM_LGE_TEMIL_CONNECTIVITY_CONNECTIVITY_INHIBITION);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_COM_LGE_TEMIL_CONNECTIVITY_CONNECTIVITY_INHIBITION);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_com_lge_temil_connectivity_connectivity_inhibition_skeleton_handle_get_property (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  ComLgeTemilConnectivityConnectivityInhibitionSkeleton *skeleton = COM_LGE_TEMIL_CONNECTIVITY_CONNECTIVITY_INHIBITION_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_connectivity_connectivity_inhibition_interface_info, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_com_lge_temil_connectivity_connectivity_inhibition_skeleton_handle_set_property (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  ComLgeTemilConnectivityConnectivityInhibitionSkeleton *skeleton = COM_LGE_TEMIL_CONNECTIVITY_CONNECTIVITY_INHIBITION_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_connectivity_connectivity_inhibition_interface_info, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _com_lge_temil_connectivity_connectivity_inhibition_skeleton_vtable =
{
  _com_lge_temil_connectivity_connectivity_inhibition_skeleton_handle_method_call,
  _com_lge_temil_connectivity_connectivity_inhibition_skeleton_handle_get_property,
  _com_lge_temil_connectivity_connectivity_inhibition_skeleton_handle_set_property
};

static GDBusInterfaceInfo *
com_lge_temil_connectivity_connectivity_inhibition_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton)
{
  return com_lge_temil_connectivity_connectivity_inhibition_interface_info ();
}

static GDBusInterfaceVTable *
com_lge_temil_connectivity_connectivity_inhibition_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton)
{
  return (GDBusInterfaceVTable *) &_com_lge_temil_connectivity_connectivity_inhibition_skeleton_vtable;
}

static GVariant *
com_lge_temil_connectivity_connectivity_inhibition_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  ComLgeTemilConnectivityConnectivityInhibitionSkeleton *skeleton = COM_LGE_TEMIL_CONNECTIVITY_CONNECTIVITY_INHIBITION_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_com_lge_temil_connectivity_connectivity_inhibition_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _com_lge_temil_connectivity_connectivity_inhibition_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _com_lge_temil_connectivity_connectivity_inhibition_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _com_lge_temil_connectivity_connectivity_inhibition_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.lge.temil.Connectivity.ConnectivityInhibition", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static void
com_lge_temil_connectivity_connectivity_inhibition_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
}

static void
_com_lge_temil_connectivity_connectivity_inhibition_on_signal_notification (
    ComLgeTemilConnectivityConnectivityInhibition *object,
    gboolean arg_ConnectivityInhibitionResult)
{
  ComLgeTemilConnectivityConnectivityInhibitionSkeleton *skeleton = COM_LGE_TEMIL_CONNECTIVITY_CONNECTIVITY_INHIBITION_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(b)",
                   arg_ConnectivityInhibitionResult));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.lge.temil.Connectivity.ConnectivityInhibition", "Notification",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void com_lge_temil_connectivity_connectivity_inhibition_skeleton_iface_init (ComLgeTemilConnectivityConnectivityInhibitionIface *iface);
G_DEFINE_TYPE_WITH_CODE (ComLgeTemilConnectivityConnectivityInhibitionSkeleton, com_lge_temil_connectivity_connectivity_inhibition_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (TYPE_COM_LGE_TEMIL_CONNECTIVITY_CONNECTIVITY_INHIBITION, com_lge_temil_connectivity_connectivity_inhibition_skeleton_iface_init));

static void
com_lge_temil_connectivity_connectivity_inhibition_skeleton_finalize (GObject *object)
{
  ComLgeTemilConnectivityConnectivityInhibitionSkeleton *skeleton = COM_LGE_TEMIL_CONNECTIVITY_CONNECTIVITY_INHIBITION_SKELETON (object);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (com_lge_temil_connectivity_connectivity_inhibition_skeleton_parent_class)->finalize (object);
}

static void
com_lge_temil_connectivity_connectivity_inhibition_skeleton_init (ComLgeTemilConnectivityConnectivityInhibitionSkeleton *skeleton)
{
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, TYPE_COM_LGE_TEMIL_CONNECTIVITY_CONNECTIVITY_INHIBITION_SKELETON, ComLgeTemilConnectivityConnectivityInhibitionSkeletonPrivate);
  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
}

static void
com_lge_temil_connectivity_connectivity_inhibition_skeleton_class_init (ComLgeTemilConnectivityConnectivityInhibitionSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  g_type_class_add_private (klass, sizeof (ComLgeTemilConnectivityConnectivityInhibitionSkeletonPrivate));

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = com_lge_temil_connectivity_connectivity_inhibition_skeleton_finalize;

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = com_lge_temil_connectivity_connectivity_inhibition_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = com_lge_temil_connectivity_connectivity_inhibition_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = com_lge_temil_connectivity_connectivity_inhibition_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = com_lge_temil_connectivity_connectivity_inhibition_skeleton_dbus_interface_get_vtable;
}

static void
com_lge_temil_connectivity_connectivity_inhibition_skeleton_iface_init (ComLgeTemilConnectivityConnectivityInhibitionIface *iface)
{
  iface->notification = _com_lge_temil_connectivity_connectivity_inhibition_on_signal_notification;
}

/**
 * com_lge_temil_connectivity_connectivity_inhibition_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Connectivity-ConnectivityInhibition.top_of_page">com.lge.temil.Connectivity.ConnectivityInhibition</link>.
 *
 * Returns: (transfer full) (type ComLgeTemilConnectivityConnectivityInhibitionSkeleton): The skeleton object.
 */
ComLgeTemilConnectivityConnectivityInhibition *
com_lge_temil_connectivity_connectivity_inhibition_skeleton_new (void)
{
  return COM_LGE_TEMIL_CONNECTIVITY_CONNECTIVITY_INHIBITION (g_object_new (TYPE_COM_LGE_TEMIL_CONNECTIVITY_CONNECTIVITY_INHIBITION_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface com.lge.temil.Connectivity.FactoryDefaultRestore
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:ComLgeTemilConnectivityFactoryDefaultRestore
 * @title: ComLgeTemilConnectivityFactoryDefaultRestore
 * @short_description: Generated C code for the com.lge.temil.Connectivity.FactoryDefaultRestore D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-com-lge-temil-Connectivity-FactoryDefaultRestore.top_of_page">com.lge.temil.Connectivity.FactoryDefaultRestore</link> D-Bus interface in C.
 */

/* ---- Introspection data for com.lge.temil.Connectivity.FactoryDefaultRestore ---- */

static const _ExtendedGDBusArgInfo _com_lge_temil_connectivity_factory_default_restore_method_info_set_request_IN_ARG_setRestoration =
{
  {
    -1,
    "setRestoration",
    "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _com_lge_temil_connectivity_factory_default_restore_method_info_set_request_IN_ARG_pointers[] =
{
  &_com_lge_temil_connectivity_factory_default_restore_method_info_set_request_IN_ARG_setRestoration,
  NULL
};

static const _ExtendedGDBusArgInfo _com_lge_temil_connectivity_factory_default_restore_method_info_set_request_OUT_ARG_restorationResult =
{
  {
    -1,
    "restorationResult",
    "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _com_lge_temil_connectivity_factory_default_restore_method_info_set_request_OUT_ARG_pointers[] =
{
  &_com_lge_temil_connectivity_factory_default_restore_method_info_set_request_OUT_ARG_restorationResult,
  NULL
};

static const _ExtendedGDBusMethodInfo _com_lge_temil_connectivity_factory_default_restore_method_info_set_request =
{
  {
    -1,
    "SetRequest",
    (GDBusArgInfo **) &_com_lge_temil_connectivity_factory_default_restore_method_info_set_request_IN_ARG_pointers,
    (GDBusArgInfo **) &_com_lge_temil_connectivity_factory_default_restore_method_info_set_request_OUT_ARG_pointers,
    NULL
  },
  "handle-set-request",
  FALSE
};

static const _ExtendedGDBusMethodInfo * const _com_lge_temil_connectivity_factory_default_restore_method_info_pointers[] =
{
  &_com_lge_temil_connectivity_factory_default_restore_method_info_set_request,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _com_lge_temil_connectivity_factory_default_restore_interface_info =
{
  {
    -1,
    "com.lge.temil.Connectivity.FactoryDefaultRestore",
    (GDBusMethodInfo **) &_com_lge_temil_connectivity_factory_default_restore_method_info_pointers,
    NULL,
    NULL,
    NULL
  },
  "com-lge-temil-connectivity-factory-default-restore",
};


/**
 * com_lge_temil_connectivity_factory_default_restore_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-com-lge-temil-Connectivity-FactoryDefaultRestore.top_of_page">com.lge.temil.Connectivity.FactoryDefaultRestore</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
com_lge_temil_connectivity_factory_default_restore_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_com_lge_temil_connectivity_factory_default_restore_interface_info;
}

/**
 * com_lge_temil_connectivity_factory_default_restore_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #ComLgeTemilConnectivityFactoryDefaultRestore interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
com_lge_temil_connectivity_factory_default_restore_override_properties (GObjectClass *klass, guint property_id_begin)
{
  return property_id_begin - 1;
}



/**
 * ComLgeTemilConnectivityFactoryDefaultRestore:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Connectivity-FactoryDefaultRestore.top_of_page">com.lge.temil.Connectivity.FactoryDefaultRestore</link>.
 */

/**
 * ComLgeTemilConnectivityFactoryDefaultRestoreIface:
 * @parent_iface: The parent interface.
 * @handle_set_request: Handler for the #ComLgeTemilConnectivityFactoryDefaultRestore::handle-set-request signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Connectivity-FactoryDefaultRestore.top_of_page">com.lge.temil.Connectivity.FactoryDefaultRestore</link>.
 */

static void
com_lge_temil_connectivity_factory_default_restore_default_init (ComLgeTemilConnectivityFactoryDefaultRestoreIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * ComLgeTemilConnectivityFactoryDefaultRestore::handle-set-request:
   * @object: A #ComLgeTemilConnectivityFactoryDefaultRestore.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_setRestoration: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-lge-temil-Connectivity-FactoryDefaultRestore.SetRequest">SetRequest()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call com_lge_temil_connectivity_factory_default_restore_complete_set_request() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-set-request",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (ComLgeTemilConnectivityFactoryDefaultRestoreIface, handle_set_request),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_BOOLEAN);

}

typedef ComLgeTemilConnectivityFactoryDefaultRestoreIface ComLgeTemilConnectivityFactoryDefaultRestoreInterface;
G_DEFINE_INTERFACE (ComLgeTemilConnectivityFactoryDefaultRestore, com_lge_temil_connectivity_factory_default_restore, G_TYPE_OBJECT);

/**
 * com_lge_temil_connectivity_factory_default_restore_call_set_request:
 * @proxy: A #ComLgeTemilConnectivityFactoryDefaultRestoreProxy.
 * @arg_setRestoration: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-lge-temil-Connectivity-FactoryDefaultRestore.SetRequest">SetRequest()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_connectivity_factory_default_restore_call_set_request_finish() to get the result of the operation.
 *
 * See com_lge_temil_connectivity_factory_default_restore_call_set_request_sync() for the synchronous, blocking version of this method.
 */
void
com_lge_temil_connectivity_factory_default_restore_call_set_request (
    ComLgeTemilConnectivityFactoryDefaultRestore *proxy,
    gboolean arg_setRestoration,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "SetRequest",
    g_variant_new ("(b)",
                   arg_setRestoration),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * com_lge_temil_connectivity_factory_default_restore_call_set_request_finish:
 * @proxy: A #ComLgeTemilConnectivityFactoryDefaultRestoreProxy.
 * @out_restorationResult: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_connectivity_factory_default_restore_call_set_request().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with com_lge_temil_connectivity_factory_default_restore_call_set_request().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_connectivity_factory_default_restore_call_set_request_finish (
    ComLgeTemilConnectivityFactoryDefaultRestore *proxy,
    gboolean *out_restorationResult,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(b)",
                 out_restorationResult);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_connectivity_factory_default_restore_call_set_request_sync:
 * @proxy: A #ComLgeTemilConnectivityFactoryDefaultRestoreProxy.
 * @arg_setRestoration: Argument to pass with the method invocation.
 * @out_restorationResult: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-lge-temil-Connectivity-FactoryDefaultRestore.SetRequest">SetRequest()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_connectivity_factory_default_restore_call_set_request() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_connectivity_factory_default_restore_call_set_request_sync (
    ComLgeTemilConnectivityFactoryDefaultRestore *proxy,
    gboolean arg_setRestoration,
    gboolean *out_restorationResult,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "SetRequest",
    g_variant_new ("(b)",
                   arg_setRestoration),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(b)",
                 out_restorationResult);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_connectivity_factory_default_restore_complete_set_request:
 * @object: A #ComLgeTemilConnectivityFactoryDefaultRestore.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @restorationResult: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-lge-temil-Connectivity-FactoryDefaultRestore.SetRequest">SetRequest()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
com_lge_temil_connectivity_factory_default_restore_complete_set_request (
    ComLgeTemilConnectivityFactoryDefaultRestore *object,
    GDBusMethodInvocation *invocation,
    gboolean restorationResult)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(b)",
                   restorationResult));
}

/* ------------------------------------------------------------------------ */

/**
 * ComLgeTemilConnectivityFactoryDefaultRestoreProxy:
 *
 * The #ComLgeTemilConnectivityFactoryDefaultRestoreProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * ComLgeTemilConnectivityFactoryDefaultRestoreProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #ComLgeTemilConnectivityFactoryDefaultRestoreProxy.
 */

struct _ComLgeTemilConnectivityFactoryDefaultRestoreProxyPrivate
{
  GData *qdata;
};

static void com_lge_temil_connectivity_factory_default_restore_proxy_iface_init (ComLgeTemilConnectivityFactoryDefaultRestoreIface *iface);

G_DEFINE_TYPE_WITH_CODE (ComLgeTemilConnectivityFactoryDefaultRestoreProxy, com_lge_temil_connectivity_factory_default_restore_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (TYPE_COM_LGE_TEMIL_CONNECTIVITY_FACTORY_DEFAULT_RESTORE, com_lge_temil_connectivity_factory_default_restore_proxy_iface_init));

static void
com_lge_temil_connectivity_factory_default_restore_proxy_finalize (GObject *object)
{
  ComLgeTemilConnectivityFactoryDefaultRestoreProxy *proxy = COM_LGE_TEMIL_CONNECTIVITY_FACTORY_DEFAULT_RESTORE_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (com_lge_temil_connectivity_factory_default_restore_proxy_parent_class)->finalize (object);
}

static void
com_lge_temil_connectivity_factory_default_restore_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec)
{
}

static void
com_lge_temil_connectivity_factory_default_restore_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
}

static void
com_lge_temil_connectivity_factory_default_restore_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_com_lge_temil_connectivity_factory_default_restore_interface_info, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], TYPE_COM_LGE_TEMIL_CONNECTIVITY_FACTORY_DEFAULT_RESTORE);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_COM_LGE_TEMIL_CONNECTIVITY_FACTORY_DEFAULT_RESTORE);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
com_lge_temil_connectivity_factory_default_restore_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  ComLgeTemilConnectivityFactoryDefaultRestoreProxy *proxy = COM_LGE_TEMIL_CONNECTIVITY_FACTORY_DEFAULT_RESTORE_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_connectivity_factory_default_restore_interface_info, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_connectivity_factory_default_restore_interface_info, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static void
com_lge_temil_connectivity_factory_default_restore_proxy_init (ComLgeTemilConnectivityFactoryDefaultRestoreProxy *proxy)
{
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, TYPE_COM_LGE_TEMIL_CONNECTIVITY_FACTORY_DEFAULT_RESTORE_PROXY, ComLgeTemilConnectivityFactoryDefaultRestoreProxyPrivate);
  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), com_lge_temil_connectivity_factory_default_restore_interface_info ());
}

static void
com_lge_temil_connectivity_factory_default_restore_proxy_class_init (ComLgeTemilConnectivityFactoryDefaultRestoreProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  g_type_class_add_private (klass, sizeof (ComLgeTemilConnectivityFactoryDefaultRestoreProxyPrivate));

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = com_lge_temil_connectivity_factory_default_restore_proxy_finalize;
  gobject_class->get_property = com_lge_temil_connectivity_factory_default_restore_proxy_get_property;
  gobject_class->set_property = com_lge_temil_connectivity_factory_default_restore_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = com_lge_temil_connectivity_factory_default_restore_proxy_g_signal;
  proxy_class->g_properties_changed = com_lge_temil_connectivity_factory_default_restore_proxy_g_properties_changed;

}

static void
com_lge_temil_connectivity_factory_default_restore_proxy_iface_init (ComLgeTemilConnectivityFactoryDefaultRestoreIface *iface)
{
}

/**
 * com_lge_temil_connectivity_factory_default_restore_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Connectivity-FactoryDefaultRestore.top_of_page">com.lge.temil.Connectivity.FactoryDefaultRestore</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_connectivity_factory_default_restore_proxy_new_finish() to get the result of the operation.
 *
 * See com_lge_temil_connectivity_factory_default_restore_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
com_lge_temil_connectivity_factory_default_restore_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_COM_LGE_TEMIL_CONNECTIVITY_FACTORY_DEFAULT_RESTORE_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Connectivity.FactoryDefaultRestore", NULL);
}

/**
 * com_lge_temil_connectivity_factory_default_restore_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_connectivity_factory_default_restore_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with com_lge_temil_connectivity_factory_default_restore_proxy_new().
 *
 * Returns: (transfer full) (type ComLgeTemilConnectivityFactoryDefaultRestoreProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilConnectivityFactoryDefaultRestore *
com_lge_temil_connectivity_factory_default_restore_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return COM_LGE_TEMIL_CONNECTIVITY_FACTORY_DEFAULT_RESTORE (ret);
  else
    return NULL;
}

/**
 * com_lge_temil_connectivity_factory_default_restore_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Connectivity-FactoryDefaultRestore.top_of_page">com.lge.temil.Connectivity.FactoryDefaultRestore</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_connectivity_factory_default_restore_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type ComLgeTemilConnectivityFactoryDefaultRestoreProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilConnectivityFactoryDefaultRestore *
com_lge_temil_connectivity_factory_default_restore_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_COM_LGE_TEMIL_CONNECTIVITY_FACTORY_DEFAULT_RESTORE_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Connectivity.FactoryDefaultRestore", NULL);
  if (ret != NULL)
    return COM_LGE_TEMIL_CONNECTIVITY_FACTORY_DEFAULT_RESTORE (ret);
  else
    return NULL;
}


/**
 * com_lge_temil_connectivity_factory_default_restore_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like com_lge_temil_connectivity_factory_default_restore_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_connectivity_factory_default_restore_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See com_lge_temil_connectivity_factory_default_restore_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
com_lge_temil_connectivity_factory_default_restore_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_COM_LGE_TEMIL_CONNECTIVITY_FACTORY_DEFAULT_RESTORE_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Connectivity.FactoryDefaultRestore", NULL);
}

/**
 * com_lge_temil_connectivity_factory_default_restore_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_connectivity_factory_default_restore_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with com_lge_temil_connectivity_factory_default_restore_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type ComLgeTemilConnectivityFactoryDefaultRestoreProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilConnectivityFactoryDefaultRestore *
com_lge_temil_connectivity_factory_default_restore_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return COM_LGE_TEMIL_CONNECTIVITY_FACTORY_DEFAULT_RESTORE (ret);
  else
    return NULL;
}

/**
 * com_lge_temil_connectivity_factory_default_restore_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like com_lge_temil_connectivity_factory_default_restore_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_connectivity_factory_default_restore_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type ComLgeTemilConnectivityFactoryDefaultRestoreProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilConnectivityFactoryDefaultRestore *
com_lge_temil_connectivity_factory_default_restore_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_COM_LGE_TEMIL_CONNECTIVITY_FACTORY_DEFAULT_RESTORE_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Connectivity.FactoryDefaultRestore", NULL);
  if (ret != NULL)
    return COM_LGE_TEMIL_CONNECTIVITY_FACTORY_DEFAULT_RESTORE (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * ComLgeTemilConnectivityFactoryDefaultRestoreSkeleton:
 *
 * The #ComLgeTemilConnectivityFactoryDefaultRestoreSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * ComLgeTemilConnectivityFactoryDefaultRestoreSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #ComLgeTemilConnectivityFactoryDefaultRestoreSkeleton.
 */

struct _ComLgeTemilConnectivityFactoryDefaultRestoreSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_com_lge_temil_connectivity_factory_default_restore_skeleton_handle_method_call (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  ComLgeTemilConnectivityFactoryDefaultRestoreSkeleton *skeleton = COM_LGE_TEMIL_CONNECTIVITY_FACTORY_DEFAULT_RESTORE_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], TYPE_COM_LGE_TEMIL_CONNECTIVITY_FACTORY_DEFAULT_RESTORE);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_COM_LGE_TEMIL_CONNECTIVITY_FACTORY_DEFAULT_RESTORE);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_com_lge_temil_connectivity_factory_default_restore_skeleton_handle_get_property (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  ComLgeTemilConnectivityFactoryDefaultRestoreSkeleton *skeleton = COM_LGE_TEMIL_CONNECTIVITY_FACTORY_DEFAULT_RESTORE_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_connectivity_factory_default_restore_interface_info, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_com_lge_temil_connectivity_factory_default_restore_skeleton_handle_set_property (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  ComLgeTemilConnectivityFactoryDefaultRestoreSkeleton *skeleton = COM_LGE_TEMIL_CONNECTIVITY_FACTORY_DEFAULT_RESTORE_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_connectivity_factory_default_restore_interface_info, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _com_lge_temil_connectivity_factory_default_restore_skeleton_vtable =
{
  _com_lge_temil_connectivity_factory_default_restore_skeleton_handle_method_call,
  _com_lge_temil_connectivity_factory_default_restore_skeleton_handle_get_property,
  _com_lge_temil_connectivity_factory_default_restore_skeleton_handle_set_property
};

static GDBusInterfaceInfo *
com_lge_temil_connectivity_factory_default_restore_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton)
{
  return com_lge_temil_connectivity_factory_default_restore_interface_info ();
}

static GDBusInterfaceVTable *
com_lge_temil_connectivity_factory_default_restore_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton)
{
  return (GDBusInterfaceVTable *) &_com_lge_temil_connectivity_factory_default_restore_skeleton_vtable;
}

static GVariant *
com_lge_temil_connectivity_factory_default_restore_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  ComLgeTemilConnectivityFactoryDefaultRestoreSkeleton *skeleton = COM_LGE_TEMIL_CONNECTIVITY_FACTORY_DEFAULT_RESTORE_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_com_lge_temil_connectivity_factory_default_restore_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _com_lge_temil_connectivity_factory_default_restore_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _com_lge_temil_connectivity_factory_default_restore_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _com_lge_temil_connectivity_factory_default_restore_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.lge.temil.Connectivity.FactoryDefaultRestore", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static void
com_lge_temil_connectivity_factory_default_restore_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
}

static void com_lge_temil_connectivity_factory_default_restore_skeleton_iface_init (ComLgeTemilConnectivityFactoryDefaultRestoreIface *iface);
G_DEFINE_TYPE_WITH_CODE (ComLgeTemilConnectivityFactoryDefaultRestoreSkeleton, com_lge_temil_connectivity_factory_default_restore_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (TYPE_COM_LGE_TEMIL_CONNECTIVITY_FACTORY_DEFAULT_RESTORE, com_lge_temil_connectivity_factory_default_restore_skeleton_iface_init));

static void
com_lge_temil_connectivity_factory_default_restore_skeleton_finalize (GObject *object)
{
  ComLgeTemilConnectivityFactoryDefaultRestoreSkeleton *skeleton = COM_LGE_TEMIL_CONNECTIVITY_FACTORY_DEFAULT_RESTORE_SKELETON (object);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (com_lge_temil_connectivity_factory_default_restore_skeleton_parent_class)->finalize (object);
}

static void
com_lge_temil_connectivity_factory_default_restore_skeleton_init (ComLgeTemilConnectivityFactoryDefaultRestoreSkeleton *skeleton)
{
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, TYPE_COM_LGE_TEMIL_CONNECTIVITY_FACTORY_DEFAULT_RESTORE_SKELETON, ComLgeTemilConnectivityFactoryDefaultRestoreSkeletonPrivate);
  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
}

static void
com_lge_temil_connectivity_factory_default_restore_skeleton_class_init (ComLgeTemilConnectivityFactoryDefaultRestoreSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  g_type_class_add_private (klass, sizeof (ComLgeTemilConnectivityFactoryDefaultRestoreSkeletonPrivate));

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = com_lge_temil_connectivity_factory_default_restore_skeleton_finalize;

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = com_lge_temil_connectivity_factory_default_restore_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = com_lge_temil_connectivity_factory_default_restore_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = com_lge_temil_connectivity_factory_default_restore_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = com_lge_temil_connectivity_factory_default_restore_skeleton_dbus_interface_get_vtable;
}

static void
com_lge_temil_connectivity_factory_default_restore_skeleton_iface_init (ComLgeTemilConnectivityFactoryDefaultRestoreIface *iface)
{
}

/**
 * com_lge_temil_connectivity_factory_default_restore_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Connectivity-FactoryDefaultRestore.top_of_page">com.lge.temil.Connectivity.FactoryDefaultRestore</link>.
 *
 * Returns: (transfer full) (type ComLgeTemilConnectivityFactoryDefaultRestoreSkeleton): The skeleton object.
 */
ComLgeTemilConnectivityFactoryDefaultRestore *
com_lge_temil_connectivity_factory_default_restore_skeleton_new (void)
{
  return COM_LGE_TEMIL_CONNECTIVITY_FACTORY_DEFAULT_RESTORE (g_object_new (TYPE_COM_LGE_TEMIL_CONNECTIVITY_FACTORY_DEFAULT_RESTORE_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface com.lge.temil.Connectivity.InternetGateway
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:ComLgeTemilConnectivityInternetGateway
 * @title: ComLgeTemilConnectivityInternetGateway
 * @short_description: Generated C code for the com.lge.temil.Connectivity.InternetGateway D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-com-lge-temil-Connectivity-InternetGateway.top_of_page">com.lge.temil.Connectivity.InternetGateway</link> D-Bus interface in C.
 */

/* ---- Introspection data for com.lge.temil.Connectivity.InternetGateway ---- */

static const _ExtendedGDBusArgInfo _com_lge_temil_connectivity_internet_gateway_method_info_set_request_IN_ARG_internetGateway =
{
  {
    -1,
    "internetGateway",
    "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _com_lge_temil_connectivity_internet_gateway_method_info_set_request_IN_ARG_pointers[] =
{
  &_com_lge_temil_connectivity_internet_gateway_method_info_set_request_IN_ARG_internetGateway,
  NULL
};

static const _ExtendedGDBusArgInfo _com_lge_temil_connectivity_internet_gateway_method_info_set_request_OUT_ARG_internetGatewayResult =
{
  {
    -1,
    "internetGatewayResult",
    "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _com_lge_temil_connectivity_internet_gateway_method_info_set_request_OUT_ARG_pointers[] =
{
  &_com_lge_temil_connectivity_internet_gateway_method_info_set_request_OUT_ARG_internetGatewayResult,
  NULL
};

static const _ExtendedGDBusMethodInfo _com_lge_temil_connectivity_internet_gateway_method_info_set_request =
{
  {
    -1,
    "SetRequest",
    (GDBusArgInfo **) &_com_lge_temil_connectivity_internet_gateway_method_info_set_request_IN_ARG_pointers,
    (GDBusArgInfo **) &_com_lge_temil_connectivity_internet_gateway_method_info_set_request_OUT_ARG_pointers,
    NULL
  },
  "handle-set-request",
  FALSE
};

static const _ExtendedGDBusArgInfo _com_lge_temil_connectivity_internet_gateway_method_info_request_OUT_ARG_internetGateway =
{
  {
    -1,
    "internetGateway",
    "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _com_lge_temil_connectivity_internet_gateway_method_info_request_OUT_ARG_pointers[] =
{
  &_com_lge_temil_connectivity_internet_gateway_method_info_request_OUT_ARG_internetGateway,
  NULL
};

static const _ExtendedGDBusMethodInfo _com_lge_temil_connectivity_internet_gateway_method_info_request =
{
  {
    -1,
    "Request",
    NULL,
    (GDBusArgInfo **) &_com_lge_temil_connectivity_internet_gateway_method_info_request_OUT_ARG_pointers,
    NULL
  },
  "handle-request",
  FALSE
};

static const _ExtendedGDBusMethodInfo * const _com_lge_temil_connectivity_internet_gateway_method_info_pointers[] =
{
  &_com_lge_temil_connectivity_internet_gateway_method_info_set_request,
  &_com_lge_temil_connectivity_internet_gateway_method_info_request,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _com_lge_temil_connectivity_internet_gateway_interface_info =
{
  {
    -1,
    "com.lge.temil.Connectivity.InternetGateway",
    (GDBusMethodInfo **) &_com_lge_temil_connectivity_internet_gateway_method_info_pointers,
    NULL,
    NULL,
    NULL
  },
  "com-lge-temil-connectivity-internet-gateway",
};


/**
 * com_lge_temil_connectivity_internet_gateway_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-com-lge-temil-Connectivity-InternetGateway.top_of_page">com.lge.temil.Connectivity.InternetGateway</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
com_lge_temil_connectivity_internet_gateway_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_com_lge_temil_connectivity_internet_gateway_interface_info;
}

/**
 * com_lge_temil_connectivity_internet_gateway_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #ComLgeTemilConnectivityInternetGateway interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
com_lge_temil_connectivity_internet_gateway_override_properties (GObjectClass *klass, guint property_id_begin)
{
  return property_id_begin - 1;
}



/**
 * ComLgeTemilConnectivityInternetGateway:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Connectivity-InternetGateway.top_of_page">com.lge.temil.Connectivity.InternetGateway</link>.
 */

/**
 * ComLgeTemilConnectivityInternetGatewayIface:
 * @parent_iface: The parent interface.
 * @handle_request: Handler for the #ComLgeTemilConnectivityInternetGateway::handle-request signal.
 * @handle_set_request: Handler for the #ComLgeTemilConnectivityInternetGateway::handle-set-request signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Connectivity-InternetGateway.top_of_page">com.lge.temil.Connectivity.InternetGateway</link>.
 */

static void
com_lge_temil_connectivity_internet_gateway_default_init (ComLgeTemilConnectivityInternetGatewayIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * ComLgeTemilConnectivityInternetGateway::handle-set-request:
   * @object: A #ComLgeTemilConnectivityInternetGateway.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_internetGateway: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-lge-temil-Connectivity-InternetGateway.SetRequest">SetRequest()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call com_lge_temil_connectivity_internet_gateway_complete_set_request() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-set-request",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (ComLgeTemilConnectivityInternetGatewayIface, handle_set_request),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_UINT);

  /**
   * ComLgeTemilConnectivityInternetGateway::handle-request:
   * @object: A #ComLgeTemilConnectivityInternetGateway.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-lge-temil-Connectivity-InternetGateway.Request">Request()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call com_lge_temil_connectivity_internet_gateway_complete_request() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-request",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (ComLgeTemilConnectivityInternetGatewayIface, handle_request),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

}

typedef ComLgeTemilConnectivityInternetGatewayIface ComLgeTemilConnectivityInternetGatewayInterface;
G_DEFINE_INTERFACE (ComLgeTemilConnectivityInternetGateway, com_lge_temil_connectivity_internet_gateway, G_TYPE_OBJECT);

/**
 * com_lge_temil_connectivity_internet_gateway_call_set_request:
 * @proxy: A #ComLgeTemilConnectivityInternetGatewayProxy.
 * @arg_internetGateway: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-lge-temil-Connectivity-InternetGateway.SetRequest">SetRequest()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_connectivity_internet_gateway_call_set_request_finish() to get the result of the operation.
 *
 * See com_lge_temil_connectivity_internet_gateway_call_set_request_sync() for the synchronous, blocking version of this method.
 */
void
com_lge_temil_connectivity_internet_gateway_call_set_request (
    ComLgeTemilConnectivityInternetGateway *proxy,
    guint arg_internetGateway,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "SetRequest",
    g_variant_new ("(u)",
                   arg_internetGateway),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * com_lge_temil_connectivity_internet_gateway_call_set_request_finish:
 * @proxy: A #ComLgeTemilConnectivityInternetGatewayProxy.
 * @out_internetGatewayResult: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_connectivity_internet_gateway_call_set_request().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with com_lge_temil_connectivity_internet_gateway_call_set_request().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_connectivity_internet_gateway_call_set_request_finish (
    ComLgeTemilConnectivityInternetGateway *proxy,
    guint *out_internetGatewayResult,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(u)",
                 out_internetGatewayResult);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_connectivity_internet_gateway_call_set_request_sync:
 * @proxy: A #ComLgeTemilConnectivityInternetGatewayProxy.
 * @arg_internetGateway: Argument to pass with the method invocation.
 * @out_internetGatewayResult: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-lge-temil-Connectivity-InternetGateway.SetRequest">SetRequest()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_connectivity_internet_gateway_call_set_request() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_connectivity_internet_gateway_call_set_request_sync (
    ComLgeTemilConnectivityInternetGateway *proxy,
    guint arg_internetGateway,
    guint *out_internetGatewayResult,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "SetRequest",
    g_variant_new ("(u)",
                   arg_internetGateway),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(u)",
                 out_internetGatewayResult);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_connectivity_internet_gateway_call_request:
 * @proxy: A #ComLgeTemilConnectivityInternetGatewayProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-lge-temil-Connectivity-InternetGateway.Request">Request()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_connectivity_internet_gateway_call_request_finish() to get the result of the operation.
 *
 * See com_lge_temil_connectivity_internet_gateway_call_request_sync() for the synchronous, blocking version of this method.
 */
void
com_lge_temil_connectivity_internet_gateway_call_request (
    ComLgeTemilConnectivityInternetGateway *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Request",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * com_lge_temil_connectivity_internet_gateway_call_request_finish:
 * @proxy: A #ComLgeTemilConnectivityInternetGatewayProxy.
 * @out_internetGateway: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_connectivity_internet_gateway_call_request().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with com_lge_temil_connectivity_internet_gateway_call_request().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_connectivity_internet_gateway_call_request_finish (
    ComLgeTemilConnectivityInternetGateway *proxy,
    guint *out_internetGateway,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(u)",
                 out_internetGateway);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_connectivity_internet_gateway_call_request_sync:
 * @proxy: A #ComLgeTemilConnectivityInternetGatewayProxy.
 * @out_internetGateway: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-lge-temil-Connectivity-InternetGateway.Request">Request()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_connectivity_internet_gateway_call_request() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_connectivity_internet_gateway_call_request_sync (
    ComLgeTemilConnectivityInternetGateway *proxy,
    guint *out_internetGateway,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Request",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(u)",
                 out_internetGateway);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_connectivity_internet_gateway_complete_set_request:
 * @object: A #ComLgeTemilConnectivityInternetGateway.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @internetGatewayResult: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-lge-temil-Connectivity-InternetGateway.SetRequest">SetRequest()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
com_lge_temil_connectivity_internet_gateway_complete_set_request (
    ComLgeTemilConnectivityInternetGateway *object,
    GDBusMethodInvocation *invocation,
    guint internetGatewayResult)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(u)",
                   internetGatewayResult));
}

/**
 * com_lge_temil_connectivity_internet_gateway_complete_request:
 * @object: A #ComLgeTemilConnectivityInternetGateway.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @internetGateway: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-lge-temil-Connectivity-InternetGateway.Request">Request()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
com_lge_temil_connectivity_internet_gateway_complete_request (
    ComLgeTemilConnectivityInternetGateway *object,
    GDBusMethodInvocation *invocation,
    guint internetGateway)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(u)",
                   internetGateway));
}

/* ------------------------------------------------------------------------ */

/**
 * ComLgeTemilConnectivityInternetGatewayProxy:
 *
 * The #ComLgeTemilConnectivityInternetGatewayProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * ComLgeTemilConnectivityInternetGatewayProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #ComLgeTemilConnectivityInternetGatewayProxy.
 */

struct _ComLgeTemilConnectivityInternetGatewayProxyPrivate
{
  GData *qdata;
};

static void com_lge_temil_connectivity_internet_gateway_proxy_iface_init (ComLgeTemilConnectivityInternetGatewayIface *iface);

G_DEFINE_TYPE_WITH_CODE (ComLgeTemilConnectivityInternetGatewayProxy, com_lge_temil_connectivity_internet_gateway_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (TYPE_COM_LGE_TEMIL_CONNECTIVITY_INTERNET_GATEWAY, com_lge_temil_connectivity_internet_gateway_proxy_iface_init));

static void
com_lge_temil_connectivity_internet_gateway_proxy_finalize (GObject *object)
{
  ComLgeTemilConnectivityInternetGatewayProxy *proxy = COM_LGE_TEMIL_CONNECTIVITY_INTERNET_GATEWAY_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (com_lge_temil_connectivity_internet_gateway_proxy_parent_class)->finalize (object);
}

static void
com_lge_temil_connectivity_internet_gateway_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec)
{
}

static void
com_lge_temil_connectivity_internet_gateway_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
}

static void
com_lge_temil_connectivity_internet_gateway_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_com_lge_temil_connectivity_internet_gateway_interface_info, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], TYPE_COM_LGE_TEMIL_CONNECTIVITY_INTERNET_GATEWAY);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_COM_LGE_TEMIL_CONNECTIVITY_INTERNET_GATEWAY);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
com_lge_temil_connectivity_internet_gateway_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  ComLgeTemilConnectivityInternetGatewayProxy *proxy = COM_LGE_TEMIL_CONNECTIVITY_INTERNET_GATEWAY_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_connectivity_internet_gateway_interface_info, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_connectivity_internet_gateway_interface_info, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static void
com_lge_temil_connectivity_internet_gateway_proxy_init (ComLgeTemilConnectivityInternetGatewayProxy *proxy)
{
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, TYPE_COM_LGE_TEMIL_CONNECTIVITY_INTERNET_GATEWAY_PROXY, ComLgeTemilConnectivityInternetGatewayProxyPrivate);
  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), com_lge_temil_connectivity_internet_gateway_interface_info ());
}

static void
com_lge_temil_connectivity_internet_gateway_proxy_class_init (ComLgeTemilConnectivityInternetGatewayProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  g_type_class_add_private (klass, sizeof (ComLgeTemilConnectivityInternetGatewayProxyPrivate));

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = com_lge_temil_connectivity_internet_gateway_proxy_finalize;
  gobject_class->get_property = com_lge_temil_connectivity_internet_gateway_proxy_get_property;
  gobject_class->set_property = com_lge_temil_connectivity_internet_gateway_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = com_lge_temil_connectivity_internet_gateway_proxy_g_signal;
  proxy_class->g_properties_changed = com_lge_temil_connectivity_internet_gateway_proxy_g_properties_changed;

}

static void
com_lge_temil_connectivity_internet_gateway_proxy_iface_init (ComLgeTemilConnectivityInternetGatewayIface *iface)
{
}

/**
 * com_lge_temil_connectivity_internet_gateway_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Connectivity-InternetGateway.top_of_page">com.lge.temil.Connectivity.InternetGateway</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_connectivity_internet_gateway_proxy_new_finish() to get the result of the operation.
 *
 * See com_lge_temil_connectivity_internet_gateway_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
com_lge_temil_connectivity_internet_gateway_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_COM_LGE_TEMIL_CONNECTIVITY_INTERNET_GATEWAY_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Connectivity.InternetGateway", NULL);
}

/**
 * com_lge_temil_connectivity_internet_gateway_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_connectivity_internet_gateway_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with com_lge_temil_connectivity_internet_gateway_proxy_new().
 *
 * Returns: (transfer full) (type ComLgeTemilConnectivityInternetGatewayProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilConnectivityInternetGateway *
com_lge_temil_connectivity_internet_gateway_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return COM_LGE_TEMIL_CONNECTIVITY_INTERNET_GATEWAY (ret);
  else
    return NULL;
}

/**
 * com_lge_temil_connectivity_internet_gateway_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Connectivity-InternetGateway.top_of_page">com.lge.temil.Connectivity.InternetGateway</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_connectivity_internet_gateway_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type ComLgeTemilConnectivityInternetGatewayProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilConnectivityInternetGateway *
com_lge_temil_connectivity_internet_gateway_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_COM_LGE_TEMIL_CONNECTIVITY_INTERNET_GATEWAY_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Connectivity.InternetGateway", NULL);
  if (ret != NULL)
    return COM_LGE_TEMIL_CONNECTIVITY_INTERNET_GATEWAY (ret);
  else
    return NULL;
}


/**
 * com_lge_temil_connectivity_internet_gateway_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like com_lge_temil_connectivity_internet_gateway_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_connectivity_internet_gateway_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See com_lge_temil_connectivity_internet_gateway_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
com_lge_temil_connectivity_internet_gateway_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_COM_LGE_TEMIL_CONNECTIVITY_INTERNET_GATEWAY_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Connectivity.InternetGateway", NULL);
}

/**
 * com_lge_temil_connectivity_internet_gateway_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_connectivity_internet_gateway_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with com_lge_temil_connectivity_internet_gateway_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type ComLgeTemilConnectivityInternetGatewayProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilConnectivityInternetGateway *
com_lge_temil_connectivity_internet_gateway_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return COM_LGE_TEMIL_CONNECTIVITY_INTERNET_GATEWAY (ret);
  else
    return NULL;
}

/**
 * com_lge_temil_connectivity_internet_gateway_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like com_lge_temil_connectivity_internet_gateway_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_connectivity_internet_gateway_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type ComLgeTemilConnectivityInternetGatewayProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilConnectivityInternetGateway *
com_lge_temil_connectivity_internet_gateway_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_COM_LGE_TEMIL_CONNECTIVITY_INTERNET_GATEWAY_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Connectivity.InternetGateway", NULL);
  if (ret != NULL)
    return COM_LGE_TEMIL_CONNECTIVITY_INTERNET_GATEWAY (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * ComLgeTemilConnectivityInternetGatewaySkeleton:
 *
 * The #ComLgeTemilConnectivityInternetGatewaySkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * ComLgeTemilConnectivityInternetGatewaySkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #ComLgeTemilConnectivityInternetGatewaySkeleton.
 */

struct _ComLgeTemilConnectivityInternetGatewaySkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_com_lge_temil_connectivity_internet_gateway_skeleton_handle_method_call (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  ComLgeTemilConnectivityInternetGatewaySkeleton *skeleton = COM_LGE_TEMIL_CONNECTIVITY_INTERNET_GATEWAY_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], TYPE_COM_LGE_TEMIL_CONNECTIVITY_INTERNET_GATEWAY);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_COM_LGE_TEMIL_CONNECTIVITY_INTERNET_GATEWAY);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_com_lge_temil_connectivity_internet_gateway_skeleton_handle_get_property (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  ComLgeTemilConnectivityInternetGatewaySkeleton *skeleton = COM_LGE_TEMIL_CONNECTIVITY_INTERNET_GATEWAY_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_connectivity_internet_gateway_interface_info, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_com_lge_temil_connectivity_internet_gateway_skeleton_handle_set_property (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  ComLgeTemilConnectivityInternetGatewaySkeleton *skeleton = COM_LGE_TEMIL_CONNECTIVITY_INTERNET_GATEWAY_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_connectivity_internet_gateway_interface_info, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _com_lge_temil_connectivity_internet_gateway_skeleton_vtable =
{
  _com_lge_temil_connectivity_internet_gateway_skeleton_handle_method_call,
  _com_lge_temil_connectivity_internet_gateway_skeleton_handle_get_property,
  _com_lge_temil_connectivity_internet_gateway_skeleton_handle_set_property
};

static GDBusInterfaceInfo *
com_lge_temil_connectivity_internet_gateway_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton)
{
  return com_lge_temil_connectivity_internet_gateway_interface_info ();
}

static GDBusInterfaceVTable *
com_lge_temil_connectivity_internet_gateway_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton)
{
  return (GDBusInterfaceVTable *) &_com_lge_temil_connectivity_internet_gateway_skeleton_vtable;
}

static GVariant *
com_lge_temil_connectivity_internet_gateway_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  ComLgeTemilConnectivityInternetGatewaySkeleton *skeleton = COM_LGE_TEMIL_CONNECTIVITY_INTERNET_GATEWAY_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_com_lge_temil_connectivity_internet_gateway_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _com_lge_temil_connectivity_internet_gateway_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _com_lge_temil_connectivity_internet_gateway_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _com_lge_temil_connectivity_internet_gateway_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.lge.temil.Connectivity.InternetGateway", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static void
com_lge_temil_connectivity_internet_gateway_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
}

static void com_lge_temil_connectivity_internet_gateway_skeleton_iface_init (ComLgeTemilConnectivityInternetGatewayIface *iface);
G_DEFINE_TYPE_WITH_CODE (ComLgeTemilConnectivityInternetGatewaySkeleton, com_lge_temil_connectivity_internet_gateway_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (TYPE_COM_LGE_TEMIL_CONNECTIVITY_INTERNET_GATEWAY, com_lge_temil_connectivity_internet_gateway_skeleton_iface_init));

static void
com_lge_temil_connectivity_internet_gateway_skeleton_finalize (GObject *object)
{
  ComLgeTemilConnectivityInternetGatewaySkeleton *skeleton = COM_LGE_TEMIL_CONNECTIVITY_INTERNET_GATEWAY_SKELETON (object);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (com_lge_temil_connectivity_internet_gateway_skeleton_parent_class)->finalize (object);
}

static void
com_lge_temil_connectivity_internet_gateway_skeleton_init (ComLgeTemilConnectivityInternetGatewaySkeleton *skeleton)
{
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, TYPE_COM_LGE_TEMIL_CONNECTIVITY_INTERNET_GATEWAY_SKELETON, ComLgeTemilConnectivityInternetGatewaySkeletonPrivate);
  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
}

static void
com_lge_temil_connectivity_internet_gateway_skeleton_class_init (ComLgeTemilConnectivityInternetGatewaySkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  g_type_class_add_private (klass, sizeof (ComLgeTemilConnectivityInternetGatewaySkeletonPrivate));

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = com_lge_temil_connectivity_internet_gateway_skeleton_finalize;

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = com_lge_temil_connectivity_internet_gateway_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = com_lge_temil_connectivity_internet_gateway_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = com_lge_temil_connectivity_internet_gateway_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = com_lge_temil_connectivity_internet_gateway_skeleton_dbus_interface_get_vtable;
}

static void
com_lge_temil_connectivity_internet_gateway_skeleton_iface_init (ComLgeTemilConnectivityInternetGatewayIface *iface)
{
}

/**
 * com_lge_temil_connectivity_internet_gateway_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Connectivity-InternetGateway.top_of_page">com.lge.temil.Connectivity.InternetGateway</link>.
 *
 * Returns: (transfer full) (type ComLgeTemilConnectivityInternetGatewaySkeleton): The skeleton object.
 */
ComLgeTemilConnectivityInternetGateway *
com_lge_temil_connectivity_internet_gateway_skeleton_new (void)
{
  return COM_LGE_TEMIL_CONNECTIVITY_INTERNET_GATEWAY (g_object_new (TYPE_COM_LGE_TEMIL_CONNECTIVITY_INTERNET_GATEWAY_SKELETON, NULL));
}

