/*
 * Generated by gdbus-codegen 2.32.4. DO NOT EDIT.
 *
 * The license of this code is the same as for the source it was derived from.
 */

#ifdef HAVE_CONFIG_H
#  include "config.h"
#endif

#include "Telematics.h"

#include <string.h>
#ifdef G_OS_UNIX
#  include <gio/gunixfdlist.h>
#endif

typedef struct
{
  GDBusArgInfo parent_struct;
  gboolean use_gvariant;
} _ExtendedGDBusArgInfo;

typedef struct
{
  GDBusMethodInfo parent_struct;
  const gchar *signal_name;
  gboolean pass_fdlist;
} _ExtendedGDBusMethodInfo;

typedef struct
{
  GDBusSignalInfo parent_struct;
  const gchar *signal_name;
} _ExtendedGDBusSignalInfo;

typedef struct
{
  GDBusPropertyInfo parent_struct;
  const gchar *hyphen_name;
  gboolean use_gvariant;
} _ExtendedGDBusPropertyInfo;

typedef struct
{
  GDBusInterfaceInfo parent_struct;
  const gchar *hyphen_name;
} _ExtendedGDBusInterfaceInfo;

typedef struct
{
  const _ExtendedGDBusPropertyInfo *info;
  guint prop_id;
  GValue orig_value; /* the value before the change */
} ChangedProperty;

static void
_changed_property_free (ChangedProperty *data)
{
  g_value_unset (&data->orig_value);
  g_free (data);
}

static gboolean
_g_strv_equal0 (gchar **a, gchar **b)
{
  gboolean ret = FALSE;
  guint n;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  if (g_strv_length (a) != g_strv_length (b))
    goto out;
  for (n = 0; a[n] != NULL; n++)
    if (g_strcmp0 (a[n], b[n]) != 0)
      goto out;
  ret = TRUE;
out:
  return ret;
}

static gboolean
_g_variant_equal0 (GVariant *a, GVariant *b)
{
  gboolean ret = FALSE;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  ret = g_variant_equal (a, b);
out:
  return ret;
}

G_GNUC_UNUSED static gboolean
_g_value_equal (const GValue *a, const GValue *b)
{
  gboolean ret = FALSE;
  g_assert (G_VALUE_TYPE (a) == G_VALUE_TYPE (b));
  switch (G_VALUE_TYPE (a))
    {
      case G_TYPE_BOOLEAN:
        ret = (g_value_get_boolean (a) == g_value_get_boolean (b));
        break;
      case G_TYPE_UCHAR:
        ret = (g_value_get_uchar (a) == g_value_get_uchar (b));
        break;
      case G_TYPE_INT:
        ret = (g_value_get_int (a) == g_value_get_int (b));
        break;
      case G_TYPE_UINT:
        ret = (g_value_get_uint (a) == g_value_get_uint (b));
        break;
      case G_TYPE_INT64:
        ret = (g_value_get_int64 (a) == g_value_get_int64 (b));
        break;
      case G_TYPE_UINT64:
        ret = (g_value_get_uint64 (a) == g_value_get_uint64 (b));
        break;
      case G_TYPE_DOUBLE:
        {
          /* Avoid -Wfloat-equal warnings by doing a direct bit compare */
          gdouble da = g_value_get_double (a);
          gdouble db = g_value_get_double (b);
          ret = memcmp (&da, &db, sizeof (gdouble)) == 0;
        }
        break;
      case G_TYPE_STRING:
        ret = (g_strcmp0 (g_value_get_string (a), g_value_get_string (b)) == 0);
        break;
      case G_TYPE_VARIANT:
        ret = _g_variant_equal0 (g_value_get_variant (a), g_value_get_variant (b));
        break;
      default:
        if (G_VALUE_TYPE (a) == G_TYPE_STRV)
          ret = _g_strv_equal0 (g_value_get_boxed (a), g_value_get_boxed (b));
        else
          g_critical ("_g_value_equal() does not handle type %s", g_type_name (G_VALUE_TYPE (a)));
        break;
    }
  return ret;
}

/* ------------------------------------------------------------------------
 * Code for interface com.lge.temil.Telematics.TelematicSettings
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:ComLgeTemilTelematicsTelematicSettings
 * @title: ComLgeTemilTelematicsTelematicSettings
 * @short_description: Generated C code for the com.lge.temil.Telematics.TelematicSettings D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-com-lge-temil-Telematics-TelematicSettings.top_of_page">com.lge.temil.Telematics.TelematicSettings</link> D-Bus interface in C.
 */

/* ---- Introspection data for com.lge.temil.Telematics.TelematicSettings ---- */

static const _ExtendedGDBusArgInfo _com_lge_temil_telematics_telematic_settings_method_info_set_request_IN_ARG_telemSetting =
{
  {
    -1,
    "telemSetting",
    "(b)",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _com_lge_temil_telematics_telematic_settings_method_info_set_request_IN_ARG_pointers[] =
{
  &_com_lge_temil_telematics_telematic_settings_method_info_set_request_IN_ARG_telemSetting,
  NULL
};

static const _ExtendedGDBusArgInfo _com_lge_temil_telematics_telematic_settings_method_info_set_request_OUT_ARG_telemSettingResult =
{
  {
    -1,
    "telemSettingResult",
    "(b)",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _com_lge_temil_telematics_telematic_settings_method_info_set_request_OUT_ARG_pointers[] =
{
  &_com_lge_temil_telematics_telematic_settings_method_info_set_request_OUT_ARG_telemSettingResult,
  NULL
};

static const _ExtendedGDBusMethodInfo _com_lge_temil_telematics_telematic_settings_method_info_set_request =
{
  {
    -1,
    "SetRequest",
    (GDBusArgInfo **) &_com_lge_temil_telematics_telematic_settings_method_info_set_request_IN_ARG_pointers,
    (GDBusArgInfo **) &_com_lge_temil_telematics_telematic_settings_method_info_set_request_OUT_ARG_pointers,
    NULL
  },
  "handle-set-request",
  FALSE
};

static const _ExtendedGDBusArgInfo _com_lge_temil_telematics_telematic_settings_method_info_request_OUT_ARG_telemSetting =
{
  {
    -1,
    "telemSetting",
    "(b)",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _com_lge_temil_telematics_telematic_settings_method_info_request_OUT_ARG_pointers[] =
{
  &_com_lge_temil_telematics_telematic_settings_method_info_request_OUT_ARG_telemSetting,
  NULL
};

static const _ExtendedGDBusMethodInfo _com_lge_temil_telematics_telematic_settings_method_info_request =
{
  {
    -1,
    "Request",
    NULL,
    (GDBusArgInfo **) &_com_lge_temil_telematics_telematic_settings_method_info_request_OUT_ARG_pointers,
    NULL
  },
  "handle-request",
  FALSE
};

static const _ExtendedGDBusArgInfo _com_lge_temil_telematics_telematic_settings_method_info_notification_request_IN_ARG_subscribed =
{
  {
    -1,
    "subscribed",
    "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _com_lge_temil_telematics_telematic_settings_method_info_notification_request_IN_ARG_pointers[] =
{
  &_com_lge_temil_telematics_telematic_settings_method_info_notification_request_IN_ARG_subscribed,
  NULL
};

static const _ExtendedGDBusMethodInfo _com_lge_temil_telematics_telematic_settings_method_info_notification_request =
{
  {
    -1,
    "NotificationRequest",
    (GDBusArgInfo **) &_com_lge_temil_telematics_telematic_settings_method_info_notification_request_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-notification-request",
  FALSE
};

static const _ExtendedGDBusMethodInfo * const _com_lge_temil_telematics_telematic_settings_method_info_pointers[] =
{
  &_com_lge_temil_telematics_telematic_settings_method_info_set_request,
  &_com_lge_temil_telematics_telematic_settings_method_info_request,
  &_com_lge_temil_telematics_telematic_settings_method_info_notification_request,
  NULL
};

static const _ExtendedGDBusArgInfo _com_lge_temil_telematics_telematic_settings_signal_info_notification_ARG_telemSetting =
{
  {
    -1,
    "telemSetting",
    "(b)",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _com_lge_temil_telematics_telematic_settings_signal_info_notification_ARG_pointers[] =
{
  &_com_lge_temil_telematics_telematic_settings_signal_info_notification_ARG_telemSetting,
  NULL
};

static const _ExtendedGDBusSignalInfo _com_lge_temil_telematics_telematic_settings_signal_info_notification =
{
  {
    -1,
    "Notification",
    (GDBusArgInfo **) &_com_lge_temil_telematics_telematic_settings_signal_info_notification_ARG_pointers,
    NULL
  },
  "notification"
};

static const _ExtendedGDBusSignalInfo * const _com_lge_temil_telematics_telematic_settings_signal_info_pointers[] =
{
  &_com_lge_temil_telematics_telematic_settings_signal_info_notification,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _com_lge_temil_telematics_telematic_settings_interface_info =
{
  {
    -1,
    "com.lge.temil.Telematics.TelematicSettings",
    (GDBusMethodInfo **) &_com_lge_temil_telematics_telematic_settings_method_info_pointers,
    (GDBusSignalInfo **) &_com_lge_temil_telematics_telematic_settings_signal_info_pointers,
    NULL,
    NULL
  },
  "com-lge-temil-telematics-telematic-settings",
};


/**
 * com_lge_temil_telematics_telematic_settings_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-com-lge-temil-Telematics-TelematicSettings.top_of_page">com.lge.temil.Telematics.TelematicSettings</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
com_lge_temil_telematics_telematic_settings_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_com_lge_temil_telematics_telematic_settings_interface_info;
}

/**
 * com_lge_temil_telematics_telematic_settings_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #ComLgeTemilTelematicsTelematicSettings interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
com_lge_temil_telematics_telematic_settings_override_properties (GObjectClass *klass, guint property_id_begin)
{
  return property_id_begin - 1;
}



/**
 * ComLgeTemilTelematicsTelematicSettings:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Telematics-TelematicSettings.top_of_page">com.lge.temil.Telematics.TelematicSettings</link>.
 */

/**
 * ComLgeTemilTelematicsTelematicSettingsIface:
 * @parent_iface: The parent interface.
 * @handle_notification_request: Handler for the #ComLgeTemilTelematicsTelematicSettings::handle-notification-request signal.
 * @handle_request: Handler for the #ComLgeTemilTelematicsTelematicSettings::handle-request signal.
 * @handle_set_request: Handler for the #ComLgeTemilTelematicsTelematicSettings::handle-set-request signal.
 * @notification: Handler for the #ComLgeTemilTelematicsTelematicSettings::notification signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Telematics-TelematicSettings.top_of_page">com.lge.temil.Telematics.TelematicSettings</link>.
 */

static void
com_lge_temil_telematics_telematic_settings_default_init (ComLgeTemilTelematicsTelematicSettingsIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * ComLgeTemilTelematicsTelematicSettings::handle-set-request:
   * @object: A #ComLgeTemilTelematicsTelematicSettings.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_telemSetting: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-lge-temil-Telematics-TelematicSettings.SetRequest">SetRequest()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call com_lge_temil_telematics_telematic_settings_complete_set_request() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-set-request",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (ComLgeTemilTelematicsTelematicSettingsIface, handle_set_request),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_VARIANT);

  /**
   * ComLgeTemilTelematicsTelematicSettings::handle-request:
   * @object: A #ComLgeTemilTelematicsTelematicSettings.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-lge-temil-Telematics-TelematicSettings.Request">Request()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call com_lge_temil_telematics_telematic_settings_complete_request() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-request",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (ComLgeTemilTelematicsTelematicSettingsIface, handle_request),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * ComLgeTemilTelematicsTelematicSettings::handle-notification-request:
   * @object: A #ComLgeTemilTelematicsTelematicSettings.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_subscribed: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-lge-temil-Telematics-TelematicSettings.NotificationRequest">NotificationRequest()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call com_lge_temil_telematics_telematic_settings_complete_notification_request() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-notification-request",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (ComLgeTemilTelematicsTelematicSettingsIface, handle_notification_request),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_BOOLEAN);

  /* GObject signals for received D-Bus signals: */
  /**
   * ComLgeTemilTelematicsTelematicSettings::notification:
   * @object: A #ComLgeTemilTelematicsTelematicSettings.
   * @arg_telemSetting: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-lge-temil-Telematics-TelematicSettings.Notification">"Notification"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("notification",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (ComLgeTemilTelematicsTelematicSettingsIface, notification),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    1, G_TYPE_VARIANT);

}

typedef ComLgeTemilTelematicsTelematicSettingsIface ComLgeTemilTelematicsTelematicSettingsInterface;
G_DEFINE_INTERFACE (ComLgeTemilTelematicsTelematicSettings, com_lge_temil_telematics_telematic_settings, G_TYPE_OBJECT);

/**
 * com_lge_temil_telematics_telematic_settings_emit_notification:
 * @object: A #ComLgeTemilTelematicsTelematicSettings.
 * @arg_telemSetting: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-com-lge-temil-Telematics-TelematicSettings.Notification">"Notification"</link> D-Bus signal.
 */
void
com_lge_temil_telematics_telematic_settings_emit_notification (
    ComLgeTemilTelematicsTelematicSettings *object,
    GVariant *arg_telemSetting)
{
  g_signal_emit_by_name (object, "notification", arg_telemSetting);
}

/**
 * com_lge_temil_telematics_telematic_settings_call_set_request:
 * @proxy: A #ComLgeTemilTelematicsTelematicSettingsProxy.
 * @arg_telemSetting: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-lge-temil-Telematics-TelematicSettings.SetRequest">SetRequest()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_telematics_telematic_settings_call_set_request_finish() to get the result of the operation.
 *
 * See com_lge_temil_telematics_telematic_settings_call_set_request_sync() for the synchronous, blocking version of this method.
 */
void
com_lge_temil_telematics_telematic_settings_call_set_request (
    ComLgeTemilTelematicsTelematicSettings *proxy,
    GVariant *arg_telemSetting,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "SetRequest",
    g_variant_new ("(@(b))",
                   arg_telemSetting),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * com_lge_temil_telematics_telematic_settings_call_set_request_finish:
 * @proxy: A #ComLgeTemilTelematicsTelematicSettingsProxy.
 * @out_telemSettingResult: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_telematics_telematic_settings_call_set_request().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with com_lge_temil_telematics_telematic_settings_call_set_request().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_telematics_telematic_settings_call_set_request_finish (
    ComLgeTemilTelematicsTelematicSettings *proxy,
    GVariant **out_telemSettingResult,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@(b))",
                 out_telemSettingResult);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_telematics_telematic_settings_call_set_request_sync:
 * @proxy: A #ComLgeTemilTelematicsTelematicSettingsProxy.
 * @arg_telemSetting: Argument to pass with the method invocation.
 * @out_telemSettingResult: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-lge-temil-Telematics-TelematicSettings.SetRequest">SetRequest()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_telematics_telematic_settings_call_set_request() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_telematics_telematic_settings_call_set_request_sync (
    ComLgeTemilTelematicsTelematicSettings *proxy,
    GVariant *arg_telemSetting,
    GVariant **out_telemSettingResult,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "SetRequest",
    g_variant_new ("(@(b))",
                   arg_telemSetting),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@(b))",
                 out_telemSettingResult);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_telematics_telematic_settings_call_request:
 * @proxy: A #ComLgeTemilTelematicsTelematicSettingsProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-lge-temil-Telematics-TelematicSettings.Request">Request()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_telematics_telematic_settings_call_request_finish() to get the result of the operation.
 *
 * See com_lge_temil_telematics_telematic_settings_call_request_sync() for the synchronous, blocking version of this method.
 */
void
com_lge_temil_telematics_telematic_settings_call_request (
    ComLgeTemilTelematicsTelematicSettings *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Request",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * com_lge_temil_telematics_telematic_settings_call_request_finish:
 * @proxy: A #ComLgeTemilTelematicsTelematicSettingsProxy.
 * @out_telemSetting: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_telematics_telematic_settings_call_request().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with com_lge_temil_telematics_telematic_settings_call_request().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_telematics_telematic_settings_call_request_finish (
    ComLgeTemilTelematicsTelematicSettings *proxy,
    GVariant **out_telemSetting,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@(b))",
                 out_telemSetting);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_telematics_telematic_settings_call_request_sync:
 * @proxy: A #ComLgeTemilTelematicsTelematicSettingsProxy.
 * @out_telemSetting: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-lge-temil-Telematics-TelematicSettings.Request">Request()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_telematics_telematic_settings_call_request() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_telematics_telematic_settings_call_request_sync (
    ComLgeTemilTelematicsTelematicSettings *proxy,
    GVariant **out_telemSetting,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Request",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@(b))",
                 out_telemSetting);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_telematics_telematic_settings_call_notification_request:
 * @proxy: A #ComLgeTemilTelematicsTelematicSettingsProxy.
 * @arg_subscribed: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-lge-temil-Telematics-TelematicSettings.NotificationRequest">NotificationRequest()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_telematics_telematic_settings_call_notification_request_finish() to get the result of the operation.
 *
 * See com_lge_temil_telematics_telematic_settings_call_notification_request_sync() for the synchronous, blocking version of this method.
 */
void
com_lge_temil_telematics_telematic_settings_call_notification_request (
    ComLgeTemilTelematicsTelematicSettings *proxy,
    gboolean arg_subscribed,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "NotificationRequest",
    g_variant_new ("(b)",
                   arg_subscribed),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * com_lge_temil_telematics_telematic_settings_call_notification_request_finish:
 * @proxy: A #ComLgeTemilTelematicsTelematicSettingsProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_telematics_telematic_settings_call_notification_request().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with com_lge_temil_telematics_telematic_settings_call_notification_request().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_telematics_telematic_settings_call_notification_request_finish (
    ComLgeTemilTelematicsTelematicSettings *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_telematics_telematic_settings_call_notification_request_sync:
 * @proxy: A #ComLgeTemilTelematicsTelematicSettingsProxy.
 * @arg_subscribed: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-lge-temil-Telematics-TelematicSettings.NotificationRequest">NotificationRequest()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_telematics_telematic_settings_call_notification_request() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_telematics_telematic_settings_call_notification_request_sync (
    ComLgeTemilTelematicsTelematicSettings *proxy,
    gboolean arg_subscribed,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "NotificationRequest",
    g_variant_new ("(b)",
                   arg_subscribed),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_telematics_telematic_settings_complete_set_request:
 * @object: A #ComLgeTemilTelematicsTelematicSettings.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @telemSettingResult: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-lge-temil-Telematics-TelematicSettings.SetRequest">SetRequest()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
com_lge_temil_telematics_telematic_settings_complete_set_request (
    ComLgeTemilTelematicsTelematicSettings *object,
    GDBusMethodInvocation *invocation,
    GVariant *telemSettingResult)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(@(b))",
                   telemSettingResult));
}

/**
 * com_lge_temil_telematics_telematic_settings_complete_request:
 * @object: A #ComLgeTemilTelematicsTelematicSettings.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @telemSetting: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-lge-temil-Telematics-TelematicSettings.Request">Request()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
com_lge_temil_telematics_telematic_settings_complete_request (
    ComLgeTemilTelematicsTelematicSettings *object,
    GDBusMethodInvocation *invocation,
    GVariant *telemSetting)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(@(b))",
                   telemSetting));
}

/**
 * com_lge_temil_telematics_telematic_settings_complete_notification_request:
 * @object: A #ComLgeTemilTelematicsTelematicSettings.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-lge-temil-Telematics-TelematicSettings.NotificationRequest">NotificationRequest()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
com_lge_temil_telematics_telematic_settings_complete_notification_request (
    ComLgeTemilTelematicsTelematicSettings *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * ComLgeTemilTelematicsTelematicSettingsProxy:
 *
 * The #ComLgeTemilTelematicsTelematicSettingsProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * ComLgeTemilTelematicsTelematicSettingsProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #ComLgeTemilTelematicsTelematicSettingsProxy.
 */

struct _ComLgeTemilTelematicsTelematicSettingsProxyPrivate
{
  GData *qdata;
};

static void com_lge_temil_telematics_telematic_settings_proxy_iface_init (ComLgeTemilTelematicsTelematicSettingsIface *iface);

G_DEFINE_TYPE_WITH_CODE (ComLgeTemilTelematicsTelematicSettingsProxy, com_lge_temil_telematics_telematic_settings_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (TYPE_COM_LGE_TEMIL_TELEMATICS_TELEMATIC_SETTINGS, com_lge_temil_telematics_telematic_settings_proxy_iface_init));

static void
com_lge_temil_telematics_telematic_settings_proxy_finalize (GObject *object)
{
  ComLgeTemilTelematicsTelematicSettingsProxy *proxy = COM_LGE_TEMIL_TELEMATICS_TELEMATIC_SETTINGS_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (com_lge_temil_telematics_telematic_settings_proxy_parent_class)->finalize (object);
}

static void
com_lge_temil_telematics_telematic_settings_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec)
{
}

static void
com_lge_temil_telematics_telematic_settings_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
}

static void
com_lge_temil_telematics_telematic_settings_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_com_lge_temil_telematics_telematic_settings_interface_info, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], TYPE_COM_LGE_TEMIL_TELEMATICS_TELEMATIC_SETTINGS);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_COM_LGE_TEMIL_TELEMATICS_TELEMATIC_SETTINGS);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
com_lge_temil_telematics_telematic_settings_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  ComLgeTemilTelematicsTelematicSettingsProxy *proxy = COM_LGE_TEMIL_TELEMATICS_TELEMATIC_SETTINGS_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_telematics_telematic_settings_interface_info, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_telematics_telematic_settings_interface_info, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static void
com_lge_temil_telematics_telematic_settings_proxy_init (ComLgeTemilTelematicsTelematicSettingsProxy *proxy)
{
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, TYPE_COM_LGE_TEMIL_TELEMATICS_TELEMATIC_SETTINGS_PROXY, ComLgeTemilTelematicsTelematicSettingsProxyPrivate);
  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), com_lge_temil_telematics_telematic_settings_interface_info ());
}

static void
com_lge_temil_telematics_telematic_settings_proxy_class_init (ComLgeTemilTelematicsTelematicSettingsProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  g_type_class_add_private (klass, sizeof (ComLgeTemilTelematicsTelematicSettingsProxyPrivate));

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = com_lge_temil_telematics_telematic_settings_proxy_finalize;
  gobject_class->get_property = com_lge_temil_telematics_telematic_settings_proxy_get_property;
  gobject_class->set_property = com_lge_temil_telematics_telematic_settings_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = com_lge_temil_telematics_telematic_settings_proxy_g_signal;
  proxy_class->g_properties_changed = com_lge_temil_telematics_telematic_settings_proxy_g_properties_changed;

}

static void
com_lge_temil_telematics_telematic_settings_proxy_iface_init (ComLgeTemilTelematicsTelematicSettingsIface *iface)
{
}

/**
 * com_lge_temil_telematics_telematic_settings_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Telematics-TelematicSettings.top_of_page">com.lge.temil.Telematics.TelematicSettings</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_telematics_telematic_settings_proxy_new_finish() to get the result of the operation.
 *
 * See com_lge_temil_telematics_telematic_settings_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
com_lge_temil_telematics_telematic_settings_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_COM_LGE_TEMIL_TELEMATICS_TELEMATIC_SETTINGS_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Telematics.TelematicSettings", NULL);
}

/**
 * com_lge_temil_telematics_telematic_settings_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_telematics_telematic_settings_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with com_lge_temil_telematics_telematic_settings_proxy_new().
 *
 * Returns: (transfer full) (type ComLgeTemilTelematicsTelematicSettingsProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilTelematicsTelematicSettings *
com_lge_temil_telematics_telematic_settings_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return COM_LGE_TEMIL_TELEMATICS_TELEMATIC_SETTINGS (ret);
  else
    return NULL;
}

/**
 * com_lge_temil_telematics_telematic_settings_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Telematics-TelematicSettings.top_of_page">com.lge.temil.Telematics.TelematicSettings</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_telematics_telematic_settings_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type ComLgeTemilTelematicsTelematicSettingsProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilTelematicsTelematicSettings *
com_lge_temil_telematics_telematic_settings_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_COM_LGE_TEMIL_TELEMATICS_TELEMATIC_SETTINGS_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Telematics.TelematicSettings", NULL);
  if (ret != NULL)
    return COM_LGE_TEMIL_TELEMATICS_TELEMATIC_SETTINGS (ret);
  else
    return NULL;
}


/**
 * com_lge_temil_telematics_telematic_settings_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like com_lge_temil_telematics_telematic_settings_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_telematics_telematic_settings_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See com_lge_temil_telematics_telematic_settings_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
com_lge_temil_telematics_telematic_settings_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_COM_LGE_TEMIL_TELEMATICS_TELEMATIC_SETTINGS_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Telematics.TelematicSettings", NULL);
}

/**
 * com_lge_temil_telematics_telematic_settings_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_telematics_telematic_settings_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with com_lge_temil_telematics_telematic_settings_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type ComLgeTemilTelematicsTelematicSettingsProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilTelematicsTelematicSettings *
com_lge_temil_telematics_telematic_settings_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return COM_LGE_TEMIL_TELEMATICS_TELEMATIC_SETTINGS (ret);
  else
    return NULL;
}

/**
 * com_lge_temil_telematics_telematic_settings_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like com_lge_temil_telematics_telematic_settings_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_telematics_telematic_settings_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type ComLgeTemilTelematicsTelematicSettingsProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilTelematicsTelematicSettings *
com_lge_temil_telematics_telematic_settings_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_COM_LGE_TEMIL_TELEMATICS_TELEMATIC_SETTINGS_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Telematics.TelematicSettings", NULL);
  if (ret != NULL)
    return COM_LGE_TEMIL_TELEMATICS_TELEMATIC_SETTINGS (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * ComLgeTemilTelematicsTelematicSettingsSkeleton:
 *
 * The #ComLgeTemilTelematicsTelematicSettingsSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * ComLgeTemilTelematicsTelematicSettingsSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #ComLgeTemilTelematicsTelematicSettingsSkeleton.
 */

struct _ComLgeTemilTelematicsTelematicSettingsSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_com_lge_temil_telematics_telematic_settings_skeleton_handle_method_call (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  ComLgeTemilTelematicsTelematicSettingsSkeleton *skeleton = COM_LGE_TEMIL_TELEMATICS_TELEMATIC_SETTINGS_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], TYPE_COM_LGE_TEMIL_TELEMATICS_TELEMATIC_SETTINGS);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_COM_LGE_TEMIL_TELEMATICS_TELEMATIC_SETTINGS);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_com_lge_temil_telematics_telematic_settings_skeleton_handle_get_property (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  ComLgeTemilTelematicsTelematicSettingsSkeleton *skeleton = COM_LGE_TEMIL_TELEMATICS_TELEMATIC_SETTINGS_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_telematics_telematic_settings_interface_info, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_com_lge_temil_telematics_telematic_settings_skeleton_handle_set_property (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  ComLgeTemilTelematicsTelematicSettingsSkeleton *skeleton = COM_LGE_TEMIL_TELEMATICS_TELEMATIC_SETTINGS_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_telematics_telematic_settings_interface_info, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _com_lge_temil_telematics_telematic_settings_skeleton_vtable =
{
  _com_lge_temil_telematics_telematic_settings_skeleton_handle_method_call,
  _com_lge_temil_telematics_telematic_settings_skeleton_handle_get_property,
  _com_lge_temil_telematics_telematic_settings_skeleton_handle_set_property
};

static GDBusInterfaceInfo *
com_lge_temil_telematics_telematic_settings_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton)
{
  return com_lge_temil_telematics_telematic_settings_interface_info ();
}

static GDBusInterfaceVTable *
com_lge_temil_telematics_telematic_settings_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton)
{
  return (GDBusInterfaceVTable *) &_com_lge_temil_telematics_telematic_settings_skeleton_vtable;
}

static GVariant *
com_lge_temil_telematics_telematic_settings_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  ComLgeTemilTelematicsTelematicSettingsSkeleton *skeleton = COM_LGE_TEMIL_TELEMATICS_TELEMATIC_SETTINGS_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_com_lge_temil_telematics_telematic_settings_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _com_lge_temil_telematics_telematic_settings_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _com_lge_temil_telematics_telematic_settings_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _com_lge_temil_telematics_telematic_settings_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.lge.temil.Telematics.TelematicSettings", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static void
com_lge_temil_telematics_telematic_settings_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
}

static void
_com_lge_temil_telematics_telematic_settings_on_signal_notification (
    ComLgeTemilTelematicsTelematicSettings *object,
    GVariant *arg_telemSetting)
{
  ComLgeTemilTelematicsTelematicSettingsSkeleton *skeleton = COM_LGE_TEMIL_TELEMATICS_TELEMATIC_SETTINGS_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(@(b))",
                   arg_telemSetting));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.lge.temil.Telematics.TelematicSettings", "Notification",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void com_lge_temil_telematics_telematic_settings_skeleton_iface_init (ComLgeTemilTelematicsTelematicSettingsIface *iface);
G_DEFINE_TYPE_WITH_CODE (ComLgeTemilTelematicsTelematicSettingsSkeleton, com_lge_temil_telematics_telematic_settings_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (TYPE_COM_LGE_TEMIL_TELEMATICS_TELEMATIC_SETTINGS, com_lge_temil_telematics_telematic_settings_skeleton_iface_init));

static void
com_lge_temil_telematics_telematic_settings_skeleton_finalize (GObject *object)
{
  ComLgeTemilTelematicsTelematicSettingsSkeleton *skeleton = COM_LGE_TEMIL_TELEMATICS_TELEMATIC_SETTINGS_SKELETON (object);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (com_lge_temil_telematics_telematic_settings_skeleton_parent_class)->finalize (object);
}

static void
com_lge_temil_telematics_telematic_settings_skeleton_init (ComLgeTemilTelematicsTelematicSettingsSkeleton *skeleton)
{
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, TYPE_COM_LGE_TEMIL_TELEMATICS_TELEMATIC_SETTINGS_SKELETON, ComLgeTemilTelematicsTelematicSettingsSkeletonPrivate);
  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
}

static void
com_lge_temil_telematics_telematic_settings_skeleton_class_init (ComLgeTemilTelematicsTelematicSettingsSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  g_type_class_add_private (klass, sizeof (ComLgeTemilTelematicsTelematicSettingsSkeletonPrivate));

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = com_lge_temil_telematics_telematic_settings_skeleton_finalize;

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = com_lge_temil_telematics_telematic_settings_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = com_lge_temil_telematics_telematic_settings_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = com_lge_temil_telematics_telematic_settings_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = com_lge_temil_telematics_telematic_settings_skeleton_dbus_interface_get_vtable;
}

static void
com_lge_temil_telematics_telematic_settings_skeleton_iface_init (ComLgeTemilTelematicsTelematicSettingsIface *iface)
{
  iface->notification = _com_lge_temil_telematics_telematic_settings_on_signal_notification;
}

/**
 * com_lge_temil_telematics_telematic_settings_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Telematics-TelematicSettings.top_of_page">com.lge.temil.Telematics.TelematicSettings</link>.
 *
 * Returns: (transfer full) (type ComLgeTemilTelematicsTelematicSettingsSkeleton): The skeleton object.
 */
ComLgeTemilTelematicsTelematicSettings *
com_lge_temil_telematics_telematic_settings_skeleton_new (void)
{
  return COM_LGE_TEMIL_TELEMATICS_TELEMATIC_SETTINGS (g_object_new (TYPE_COM_LGE_TEMIL_TELEMATICS_TELEMATIC_SETTINGS_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface com.lge.temil.Telematics.PositionData
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:ComLgeTemilTelematicsPositionData
 * @title: ComLgeTemilTelematicsPositionData
 * @short_description: Generated C code for the com.lge.temil.Telematics.PositionData D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-com-lge-temil-Telematics-PositionData.top_of_page">com.lge.temil.Telematics.PositionData</link> D-Bus interface in C.
 */

/* ---- Introspection data for com.lge.temil.Telematics.PositionData ---- */

static const _ExtendedGDBusArgInfo _com_lge_temil_telematics_position_data_method_info_request_OUT_ARG_position =
{
  {
    -1,
    "position",
    "(b((ii)(qyyyyy)uu))",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _com_lge_temil_telematics_position_data_method_info_request_OUT_ARG_pointers[] =
{
  &_com_lge_temil_telematics_position_data_method_info_request_OUT_ARG_position,
  NULL
};

static const _ExtendedGDBusMethodInfo _com_lge_temil_telematics_position_data_method_info_request =
{
  {
    -1,
    "Request",
    NULL,
    (GDBusArgInfo **) &_com_lge_temil_telematics_position_data_method_info_request_OUT_ARG_pointers,
    NULL
  },
  "handle-request",
  FALSE
};

static const _ExtendedGDBusMethodInfo * const _com_lge_temil_telematics_position_data_method_info_pointers[] =
{
  &_com_lge_temil_telematics_position_data_method_info_request,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _com_lge_temil_telematics_position_data_interface_info =
{
  {
    -1,
    "com.lge.temil.Telematics.PositionData",
    (GDBusMethodInfo **) &_com_lge_temil_telematics_position_data_method_info_pointers,
    NULL,
    NULL,
    NULL
  },
  "com-lge-temil-telematics-position-data",
};


/**
 * com_lge_temil_telematics_position_data_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-com-lge-temil-Telematics-PositionData.top_of_page">com.lge.temil.Telematics.PositionData</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
com_lge_temil_telematics_position_data_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_com_lge_temil_telematics_position_data_interface_info;
}

/**
 * com_lge_temil_telematics_position_data_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #ComLgeTemilTelematicsPositionData interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
com_lge_temil_telematics_position_data_override_properties (GObjectClass *klass, guint property_id_begin)
{
  return property_id_begin - 1;
}



/**
 * ComLgeTemilTelematicsPositionData:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Telematics-PositionData.top_of_page">com.lge.temil.Telematics.PositionData</link>.
 */

/**
 * ComLgeTemilTelematicsPositionDataIface:
 * @parent_iface: The parent interface.
 * @handle_request: Handler for the #ComLgeTemilTelematicsPositionData::handle-request signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Telematics-PositionData.top_of_page">com.lge.temil.Telematics.PositionData</link>.
 */

static void
com_lge_temil_telematics_position_data_default_init (ComLgeTemilTelematicsPositionDataIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * ComLgeTemilTelematicsPositionData::handle-request:
   * @object: A #ComLgeTemilTelematicsPositionData.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-lge-temil-Telematics-PositionData.Request">Request()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call com_lge_temil_telematics_position_data_complete_request() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-request",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (ComLgeTemilTelematicsPositionDataIface, handle_request),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

}

typedef ComLgeTemilTelematicsPositionDataIface ComLgeTemilTelematicsPositionDataInterface;
G_DEFINE_INTERFACE (ComLgeTemilTelematicsPositionData, com_lge_temil_telematics_position_data, G_TYPE_OBJECT);

/**
 * com_lge_temil_telematics_position_data_call_request:
 * @proxy: A #ComLgeTemilTelematicsPositionDataProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-lge-temil-Telematics-PositionData.Request">Request()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_telematics_position_data_call_request_finish() to get the result of the operation.
 *
 * See com_lge_temil_telematics_position_data_call_request_sync() for the synchronous, blocking version of this method.
 */
void
com_lge_temil_telematics_position_data_call_request (
    ComLgeTemilTelematicsPositionData *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Request",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * com_lge_temil_telematics_position_data_call_request_finish:
 * @proxy: A #ComLgeTemilTelematicsPositionDataProxy.
 * @out_position: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_telematics_position_data_call_request().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with com_lge_temil_telematics_position_data_call_request().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_telematics_position_data_call_request_finish (
    ComLgeTemilTelematicsPositionData *proxy,
    GVariant **out_position,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@(b((ii)(qyyyyy)uu)))",
                 out_position);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_telematics_position_data_call_request_sync:
 * @proxy: A #ComLgeTemilTelematicsPositionDataProxy.
 * @out_position: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-lge-temil-Telematics-PositionData.Request">Request()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_telematics_position_data_call_request() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_telematics_position_data_call_request_sync (
    ComLgeTemilTelematicsPositionData *proxy,
    GVariant **out_position,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Request",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@(b((ii)(qyyyyy)uu)))",
                 out_position);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_telematics_position_data_complete_request:
 * @object: A #ComLgeTemilTelematicsPositionData.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @position: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-lge-temil-Telematics-PositionData.Request">Request()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
com_lge_temil_telematics_position_data_complete_request (
    ComLgeTemilTelematicsPositionData *object,
    GDBusMethodInvocation *invocation,
    GVariant *position)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(@(b((ii)(qyyyyy)uu)))",
                   position));
}

/* ------------------------------------------------------------------------ */

/**
 * ComLgeTemilTelematicsPositionDataProxy:
 *
 * The #ComLgeTemilTelematicsPositionDataProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * ComLgeTemilTelematicsPositionDataProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #ComLgeTemilTelematicsPositionDataProxy.
 */

struct _ComLgeTemilTelematicsPositionDataProxyPrivate
{
  GData *qdata;
};

static void com_lge_temil_telematics_position_data_proxy_iface_init (ComLgeTemilTelematicsPositionDataIface *iface);

G_DEFINE_TYPE_WITH_CODE (ComLgeTemilTelematicsPositionDataProxy, com_lge_temil_telematics_position_data_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (TYPE_COM_LGE_TEMIL_TELEMATICS_POSITION_DATA, com_lge_temil_telematics_position_data_proxy_iface_init));

static void
com_lge_temil_telematics_position_data_proxy_finalize (GObject *object)
{
  ComLgeTemilTelematicsPositionDataProxy *proxy = COM_LGE_TEMIL_TELEMATICS_POSITION_DATA_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (com_lge_temil_telematics_position_data_proxy_parent_class)->finalize (object);
}

static void
com_lge_temil_telematics_position_data_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec)
{
}

static void
com_lge_temil_telematics_position_data_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
}

static void
com_lge_temil_telematics_position_data_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_com_lge_temil_telematics_position_data_interface_info, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], TYPE_COM_LGE_TEMIL_TELEMATICS_POSITION_DATA);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_COM_LGE_TEMIL_TELEMATICS_POSITION_DATA);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
com_lge_temil_telematics_position_data_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  ComLgeTemilTelematicsPositionDataProxy *proxy = COM_LGE_TEMIL_TELEMATICS_POSITION_DATA_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_telematics_position_data_interface_info, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_telematics_position_data_interface_info, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static void
com_lge_temil_telematics_position_data_proxy_init (ComLgeTemilTelematicsPositionDataProxy *proxy)
{
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, TYPE_COM_LGE_TEMIL_TELEMATICS_POSITION_DATA_PROXY, ComLgeTemilTelematicsPositionDataProxyPrivate);
  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), com_lge_temil_telematics_position_data_interface_info ());
}

static void
com_lge_temil_telematics_position_data_proxy_class_init (ComLgeTemilTelematicsPositionDataProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  g_type_class_add_private (klass, sizeof (ComLgeTemilTelematicsPositionDataProxyPrivate));

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = com_lge_temil_telematics_position_data_proxy_finalize;
  gobject_class->get_property = com_lge_temil_telematics_position_data_proxy_get_property;
  gobject_class->set_property = com_lge_temil_telematics_position_data_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = com_lge_temil_telematics_position_data_proxy_g_signal;
  proxy_class->g_properties_changed = com_lge_temil_telematics_position_data_proxy_g_properties_changed;

}

static void
com_lge_temil_telematics_position_data_proxy_iface_init (ComLgeTemilTelematicsPositionDataIface *iface)
{
}

/**
 * com_lge_temil_telematics_position_data_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Telematics-PositionData.top_of_page">com.lge.temil.Telematics.PositionData</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_telematics_position_data_proxy_new_finish() to get the result of the operation.
 *
 * See com_lge_temil_telematics_position_data_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
com_lge_temil_telematics_position_data_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_COM_LGE_TEMIL_TELEMATICS_POSITION_DATA_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Telematics.PositionData", NULL);
}

/**
 * com_lge_temil_telematics_position_data_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_telematics_position_data_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with com_lge_temil_telematics_position_data_proxy_new().
 *
 * Returns: (transfer full) (type ComLgeTemilTelematicsPositionDataProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilTelematicsPositionData *
com_lge_temil_telematics_position_data_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return COM_LGE_TEMIL_TELEMATICS_POSITION_DATA (ret);
  else
    return NULL;
}

/**
 * com_lge_temil_telematics_position_data_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Telematics-PositionData.top_of_page">com.lge.temil.Telematics.PositionData</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_telematics_position_data_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type ComLgeTemilTelematicsPositionDataProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilTelematicsPositionData *
com_lge_temil_telematics_position_data_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_COM_LGE_TEMIL_TELEMATICS_POSITION_DATA_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Telematics.PositionData", NULL);
  if (ret != NULL)
    return COM_LGE_TEMIL_TELEMATICS_POSITION_DATA (ret);
  else
    return NULL;
}


/**
 * com_lge_temil_telematics_position_data_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like com_lge_temil_telematics_position_data_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_telematics_position_data_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See com_lge_temil_telematics_position_data_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
com_lge_temil_telematics_position_data_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_COM_LGE_TEMIL_TELEMATICS_POSITION_DATA_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Telematics.PositionData", NULL);
}

/**
 * com_lge_temil_telematics_position_data_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_telematics_position_data_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with com_lge_temil_telematics_position_data_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type ComLgeTemilTelematicsPositionDataProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilTelematicsPositionData *
com_lge_temil_telematics_position_data_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return COM_LGE_TEMIL_TELEMATICS_POSITION_DATA (ret);
  else
    return NULL;
}

/**
 * com_lge_temil_telematics_position_data_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like com_lge_temil_telematics_position_data_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_telematics_position_data_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type ComLgeTemilTelematicsPositionDataProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilTelematicsPositionData *
com_lge_temil_telematics_position_data_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_COM_LGE_TEMIL_TELEMATICS_POSITION_DATA_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Telematics.PositionData", NULL);
  if (ret != NULL)
    return COM_LGE_TEMIL_TELEMATICS_POSITION_DATA (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * ComLgeTemilTelematicsPositionDataSkeleton:
 *
 * The #ComLgeTemilTelematicsPositionDataSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * ComLgeTemilTelematicsPositionDataSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #ComLgeTemilTelematicsPositionDataSkeleton.
 */

struct _ComLgeTemilTelematicsPositionDataSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_com_lge_temil_telematics_position_data_skeleton_handle_method_call (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  ComLgeTemilTelematicsPositionDataSkeleton *skeleton = COM_LGE_TEMIL_TELEMATICS_POSITION_DATA_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], TYPE_COM_LGE_TEMIL_TELEMATICS_POSITION_DATA);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_COM_LGE_TEMIL_TELEMATICS_POSITION_DATA);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_com_lge_temil_telematics_position_data_skeleton_handle_get_property (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  ComLgeTemilTelematicsPositionDataSkeleton *skeleton = COM_LGE_TEMIL_TELEMATICS_POSITION_DATA_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_telematics_position_data_interface_info, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_com_lge_temil_telematics_position_data_skeleton_handle_set_property (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  ComLgeTemilTelematicsPositionDataSkeleton *skeleton = COM_LGE_TEMIL_TELEMATICS_POSITION_DATA_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_telematics_position_data_interface_info, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _com_lge_temil_telematics_position_data_skeleton_vtable =
{
  _com_lge_temil_telematics_position_data_skeleton_handle_method_call,
  _com_lge_temil_telematics_position_data_skeleton_handle_get_property,
  _com_lge_temil_telematics_position_data_skeleton_handle_set_property
};

static GDBusInterfaceInfo *
com_lge_temil_telematics_position_data_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton)
{
  return com_lge_temil_telematics_position_data_interface_info ();
}

static GDBusInterfaceVTable *
com_lge_temil_telematics_position_data_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton)
{
  return (GDBusInterfaceVTable *) &_com_lge_temil_telematics_position_data_skeleton_vtable;
}

static GVariant *
com_lge_temil_telematics_position_data_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  ComLgeTemilTelematicsPositionDataSkeleton *skeleton = COM_LGE_TEMIL_TELEMATICS_POSITION_DATA_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_com_lge_temil_telematics_position_data_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _com_lge_temil_telematics_position_data_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _com_lge_temil_telematics_position_data_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _com_lge_temil_telematics_position_data_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.lge.temil.Telematics.PositionData", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static void
com_lge_temil_telematics_position_data_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
}

static void com_lge_temil_telematics_position_data_skeleton_iface_init (ComLgeTemilTelematicsPositionDataIface *iface);
G_DEFINE_TYPE_WITH_CODE (ComLgeTemilTelematicsPositionDataSkeleton, com_lge_temil_telematics_position_data_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (TYPE_COM_LGE_TEMIL_TELEMATICS_POSITION_DATA, com_lge_temil_telematics_position_data_skeleton_iface_init));

static void
com_lge_temil_telematics_position_data_skeleton_finalize (GObject *object)
{
  ComLgeTemilTelematicsPositionDataSkeleton *skeleton = COM_LGE_TEMIL_TELEMATICS_POSITION_DATA_SKELETON (object);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (com_lge_temil_telematics_position_data_skeleton_parent_class)->finalize (object);
}

static void
com_lge_temil_telematics_position_data_skeleton_init (ComLgeTemilTelematicsPositionDataSkeleton *skeleton)
{
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, TYPE_COM_LGE_TEMIL_TELEMATICS_POSITION_DATA_SKELETON, ComLgeTemilTelematicsPositionDataSkeletonPrivate);
  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
}

static void
com_lge_temil_telematics_position_data_skeleton_class_init (ComLgeTemilTelematicsPositionDataSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  g_type_class_add_private (klass, sizeof (ComLgeTemilTelematicsPositionDataSkeletonPrivate));

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = com_lge_temil_telematics_position_data_skeleton_finalize;

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = com_lge_temil_telematics_position_data_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = com_lge_temil_telematics_position_data_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = com_lge_temil_telematics_position_data_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = com_lge_temil_telematics_position_data_skeleton_dbus_interface_get_vtable;
}

static void
com_lge_temil_telematics_position_data_skeleton_iface_init (ComLgeTemilTelematicsPositionDataIface *iface)
{
}

/**
 * com_lge_temil_telematics_position_data_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Telematics-PositionData.top_of_page">com.lge.temil.Telematics.PositionData</link>.
 *
 * Returns: (transfer full) (type ComLgeTemilTelematicsPositionDataSkeleton): The skeleton object.
 */
ComLgeTemilTelematicsPositionData *
com_lge_temil_telematics_position_data_skeleton_new (void)
{
  return COM_LGE_TEMIL_TELEMATICS_POSITION_DATA (g_object_new (TYPE_COM_LGE_TEMIL_TELEMATICS_POSITION_DATA_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface com.lge.temil.Telematics.SubscriptionActivation
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:ComLgeTemilTelematicsSubscriptionActivation
 * @title: ComLgeTemilTelematicsSubscriptionActivation
 * @short_description: Generated C code for the com.lge.temil.Telematics.SubscriptionActivation D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-com-lge-temil-Telematics-SubscriptionActivation.top_of_page">com.lge.temil.Telematics.SubscriptionActivation</link> D-Bus interface in C.
 */

/* ---- Introspection data for com.lge.temil.Telematics.SubscriptionActivation ---- */

static const _ExtendedGDBusArgInfo _com_lge_temil_telematics_subscription_activation_method_info_request_OUT_ARG_status =
{
  {
    -1,
    "status",
    "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _com_lge_temil_telematics_subscription_activation_method_info_request_OUT_ARG_pointers[] =
{
  &_com_lge_temil_telematics_subscription_activation_method_info_request_OUT_ARG_status,
  NULL
};

static const _ExtendedGDBusMethodInfo _com_lge_temil_telematics_subscription_activation_method_info_request =
{
  {
    -1,
    "Request",
    NULL,
    (GDBusArgInfo **) &_com_lge_temil_telematics_subscription_activation_method_info_request_OUT_ARG_pointers,
    NULL
  },
  "handle-request",
  FALSE
};

static const _ExtendedGDBusArgInfo _com_lge_temil_telematics_subscription_activation_method_info_notification_request_IN_ARG_subscribed =
{
  {
    -1,
    "subscribed",
    "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _com_lge_temil_telematics_subscription_activation_method_info_notification_request_IN_ARG_pointers[] =
{
  &_com_lge_temil_telematics_subscription_activation_method_info_notification_request_IN_ARG_subscribed,
  NULL
};

static const _ExtendedGDBusMethodInfo _com_lge_temil_telematics_subscription_activation_method_info_notification_request =
{
  {
    -1,
    "NotificationRequest",
    (GDBusArgInfo **) &_com_lge_temil_telematics_subscription_activation_method_info_notification_request_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-notification-request",
  FALSE
};

static const _ExtendedGDBusMethodInfo * const _com_lge_temil_telematics_subscription_activation_method_info_pointers[] =
{
  &_com_lge_temil_telematics_subscription_activation_method_info_request,
  &_com_lge_temil_telematics_subscription_activation_method_info_notification_request,
  NULL
};

static const _ExtendedGDBusArgInfo _com_lge_temil_telematics_subscription_activation_signal_info_notification_ARG_status =
{
  {
    -1,
    "status",
    "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _com_lge_temil_telematics_subscription_activation_signal_info_notification_ARG_pointers[] =
{
  &_com_lge_temil_telematics_subscription_activation_signal_info_notification_ARG_status,
  NULL
};

static const _ExtendedGDBusSignalInfo _com_lge_temil_telematics_subscription_activation_signal_info_notification =
{
  {
    -1,
    "Notification",
    (GDBusArgInfo **) &_com_lge_temil_telematics_subscription_activation_signal_info_notification_ARG_pointers,
    NULL
  },
  "notification"
};

static const _ExtendedGDBusSignalInfo * const _com_lge_temil_telematics_subscription_activation_signal_info_pointers[] =
{
  &_com_lge_temil_telematics_subscription_activation_signal_info_notification,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _com_lge_temil_telematics_subscription_activation_interface_info =
{
  {
    -1,
    "com.lge.temil.Telematics.SubscriptionActivation",
    (GDBusMethodInfo **) &_com_lge_temil_telematics_subscription_activation_method_info_pointers,
    (GDBusSignalInfo **) &_com_lge_temil_telematics_subscription_activation_signal_info_pointers,
    NULL,
    NULL
  },
  "com-lge-temil-telematics-subscription-activation",
};


/**
 * com_lge_temil_telematics_subscription_activation_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-com-lge-temil-Telematics-SubscriptionActivation.top_of_page">com.lge.temil.Telematics.SubscriptionActivation</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
com_lge_temil_telematics_subscription_activation_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_com_lge_temil_telematics_subscription_activation_interface_info;
}

/**
 * com_lge_temil_telematics_subscription_activation_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #ComLgeTemilTelematicsSubscriptionActivation interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
com_lge_temil_telematics_subscription_activation_override_properties (GObjectClass *klass, guint property_id_begin)
{
  return property_id_begin - 1;
}



/**
 * ComLgeTemilTelematicsSubscriptionActivation:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Telematics-SubscriptionActivation.top_of_page">com.lge.temil.Telematics.SubscriptionActivation</link>.
 */

/**
 * ComLgeTemilTelematicsSubscriptionActivationIface:
 * @parent_iface: The parent interface.
 * @handle_notification_request: Handler for the #ComLgeTemilTelematicsSubscriptionActivation::handle-notification-request signal.
 * @handle_request: Handler for the #ComLgeTemilTelematicsSubscriptionActivation::handle-request signal.
 * @notification: Handler for the #ComLgeTemilTelematicsSubscriptionActivation::notification signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Telematics-SubscriptionActivation.top_of_page">com.lge.temil.Telematics.SubscriptionActivation</link>.
 */

static void
com_lge_temil_telematics_subscription_activation_default_init (ComLgeTemilTelematicsSubscriptionActivationIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * ComLgeTemilTelematicsSubscriptionActivation::handle-request:
   * @object: A #ComLgeTemilTelematicsSubscriptionActivation.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-lge-temil-Telematics-SubscriptionActivation.Request">Request()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call com_lge_temil_telematics_subscription_activation_complete_request() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-request",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (ComLgeTemilTelematicsSubscriptionActivationIface, handle_request),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * ComLgeTemilTelematicsSubscriptionActivation::handle-notification-request:
   * @object: A #ComLgeTemilTelematicsSubscriptionActivation.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_subscribed: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-lge-temil-Telematics-SubscriptionActivation.NotificationRequest">NotificationRequest()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call com_lge_temil_telematics_subscription_activation_complete_notification_request() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-notification-request",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (ComLgeTemilTelematicsSubscriptionActivationIface, handle_notification_request),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_BOOLEAN);

  /* GObject signals for received D-Bus signals: */
  /**
   * ComLgeTemilTelematicsSubscriptionActivation::notification:
   * @object: A #ComLgeTemilTelematicsSubscriptionActivation.
   * @arg_status: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-lge-temil-Telematics-SubscriptionActivation.Notification">"Notification"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("notification",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (ComLgeTemilTelematicsSubscriptionActivationIface, notification),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    1, G_TYPE_UINT);

}

typedef ComLgeTemilTelematicsSubscriptionActivationIface ComLgeTemilTelematicsSubscriptionActivationInterface;
G_DEFINE_INTERFACE (ComLgeTemilTelematicsSubscriptionActivation, com_lge_temil_telematics_subscription_activation, G_TYPE_OBJECT);

/**
 * com_lge_temil_telematics_subscription_activation_emit_notification:
 * @object: A #ComLgeTemilTelematicsSubscriptionActivation.
 * @arg_status: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-com-lge-temil-Telematics-SubscriptionActivation.Notification">"Notification"</link> D-Bus signal.
 */
void
com_lge_temil_telematics_subscription_activation_emit_notification (
    ComLgeTemilTelematicsSubscriptionActivation *object,
    guint arg_status)
{
  g_signal_emit_by_name (object, "notification", arg_status);
}

/**
 * com_lge_temil_telematics_subscription_activation_call_request:
 * @proxy: A #ComLgeTemilTelematicsSubscriptionActivationProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-lge-temil-Telematics-SubscriptionActivation.Request">Request()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_telematics_subscription_activation_call_request_finish() to get the result of the operation.
 *
 * See com_lge_temil_telematics_subscription_activation_call_request_sync() for the synchronous, blocking version of this method.
 */
void
com_lge_temil_telematics_subscription_activation_call_request (
    ComLgeTemilTelematicsSubscriptionActivation *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Request",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * com_lge_temil_telematics_subscription_activation_call_request_finish:
 * @proxy: A #ComLgeTemilTelematicsSubscriptionActivationProxy.
 * @out_status: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_telematics_subscription_activation_call_request().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with com_lge_temil_telematics_subscription_activation_call_request().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_telematics_subscription_activation_call_request_finish (
    ComLgeTemilTelematicsSubscriptionActivation *proxy,
    guint *out_status,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(u)",
                 out_status);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_telematics_subscription_activation_call_request_sync:
 * @proxy: A #ComLgeTemilTelematicsSubscriptionActivationProxy.
 * @out_status: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-lge-temil-Telematics-SubscriptionActivation.Request">Request()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_telematics_subscription_activation_call_request() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_telematics_subscription_activation_call_request_sync (
    ComLgeTemilTelematicsSubscriptionActivation *proxy,
    guint *out_status,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Request",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(u)",
                 out_status);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_telematics_subscription_activation_call_notification_request:
 * @proxy: A #ComLgeTemilTelematicsSubscriptionActivationProxy.
 * @arg_subscribed: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-lge-temil-Telematics-SubscriptionActivation.NotificationRequest">NotificationRequest()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_telematics_subscription_activation_call_notification_request_finish() to get the result of the operation.
 *
 * See com_lge_temil_telematics_subscription_activation_call_notification_request_sync() for the synchronous, blocking version of this method.
 */
void
com_lge_temil_telematics_subscription_activation_call_notification_request (
    ComLgeTemilTelematicsSubscriptionActivation *proxy,
    gboolean arg_subscribed,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "NotificationRequest",
    g_variant_new ("(b)",
                   arg_subscribed),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * com_lge_temil_telematics_subscription_activation_call_notification_request_finish:
 * @proxy: A #ComLgeTemilTelematicsSubscriptionActivationProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_telematics_subscription_activation_call_notification_request().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with com_lge_temil_telematics_subscription_activation_call_notification_request().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_telematics_subscription_activation_call_notification_request_finish (
    ComLgeTemilTelematicsSubscriptionActivation *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_telematics_subscription_activation_call_notification_request_sync:
 * @proxy: A #ComLgeTemilTelematicsSubscriptionActivationProxy.
 * @arg_subscribed: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-lge-temil-Telematics-SubscriptionActivation.NotificationRequest">NotificationRequest()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_telematics_subscription_activation_call_notification_request() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_telematics_subscription_activation_call_notification_request_sync (
    ComLgeTemilTelematicsSubscriptionActivation *proxy,
    gboolean arg_subscribed,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "NotificationRequest",
    g_variant_new ("(b)",
                   arg_subscribed),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_telematics_subscription_activation_complete_request:
 * @object: A #ComLgeTemilTelematicsSubscriptionActivation.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @status: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-lge-temil-Telematics-SubscriptionActivation.Request">Request()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
com_lge_temil_telematics_subscription_activation_complete_request (
    ComLgeTemilTelematicsSubscriptionActivation *object,
    GDBusMethodInvocation *invocation,
    guint status)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(u)",
                   status));
}

/**
 * com_lge_temil_telematics_subscription_activation_complete_notification_request:
 * @object: A #ComLgeTemilTelematicsSubscriptionActivation.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-lge-temil-Telematics-SubscriptionActivation.NotificationRequest">NotificationRequest()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
com_lge_temil_telematics_subscription_activation_complete_notification_request (
    ComLgeTemilTelematicsSubscriptionActivation *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * ComLgeTemilTelematicsSubscriptionActivationProxy:
 *
 * The #ComLgeTemilTelematicsSubscriptionActivationProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * ComLgeTemilTelematicsSubscriptionActivationProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #ComLgeTemilTelematicsSubscriptionActivationProxy.
 */

struct _ComLgeTemilTelematicsSubscriptionActivationProxyPrivate
{
  GData *qdata;
};

static void com_lge_temil_telematics_subscription_activation_proxy_iface_init (ComLgeTemilTelematicsSubscriptionActivationIface *iface);

G_DEFINE_TYPE_WITH_CODE (ComLgeTemilTelematicsSubscriptionActivationProxy, com_lge_temil_telematics_subscription_activation_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (TYPE_COM_LGE_TEMIL_TELEMATICS_SUBSCRIPTION_ACTIVATION, com_lge_temil_telematics_subscription_activation_proxy_iface_init));

static void
com_lge_temil_telematics_subscription_activation_proxy_finalize (GObject *object)
{
  ComLgeTemilTelematicsSubscriptionActivationProxy *proxy = COM_LGE_TEMIL_TELEMATICS_SUBSCRIPTION_ACTIVATION_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (com_lge_temil_telematics_subscription_activation_proxy_parent_class)->finalize (object);
}

static void
com_lge_temil_telematics_subscription_activation_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec)
{
}

static void
com_lge_temil_telematics_subscription_activation_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
}

static void
com_lge_temil_telematics_subscription_activation_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_com_lge_temil_telematics_subscription_activation_interface_info, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], TYPE_COM_LGE_TEMIL_TELEMATICS_SUBSCRIPTION_ACTIVATION);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_COM_LGE_TEMIL_TELEMATICS_SUBSCRIPTION_ACTIVATION);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
com_lge_temil_telematics_subscription_activation_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  ComLgeTemilTelematicsSubscriptionActivationProxy *proxy = COM_LGE_TEMIL_TELEMATICS_SUBSCRIPTION_ACTIVATION_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_telematics_subscription_activation_interface_info, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_telematics_subscription_activation_interface_info, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static void
com_lge_temil_telematics_subscription_activation_proxy_init (ComLgeTemilTelematicsSubscriptionActivationProxy *proxy)
{
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, TYPE_COM_LGE_TEMIL_TELEMATICS_SUBSCRIPTION_ACTIVATION_PROXY, ComLgeTemilTelematicsSubscriptionActivationProxyPrivate);
  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), com_lge_temil_telematics_subscription_activation_interface_info ());
}

static void
com_lge_temil_telematics_subscription_activation_proxy_class_init (ComLgeTemilTelematicsSubscriptionActivationProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  g_type_class_add_private (klass, sizeof (ComLgeTemilTelematicsSubscriptionActivationProxyPrivate));

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = com_lge_temil_telematics_subscription_activation_proxy_finalize;
  gobject_class->get_property = com_lge_temil_telematics_subscription_activation_proxy_get_property;
  gobject_class->set_property = com_lge_temil_telematics_subscription_activation_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = com_lge_temil_telematics_subscription_activation_proxy_g_signal;
  proxy_class->g_properties_changed = com_lge_temil_telematics_subscription_activation_proxy_g_properties_changed;

}

static void
com_lge_temil_telematics_subscription_activation_proxy_iface_init (ComLgeTemilTelematicsSubscriptionActivationIface *iface)
{
}

/**
 * com_lge_temil_telematics_subscription_activation_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Telematics-SubscriptionActivation.top_of_page">com.lge.temil.Telematics.SubscriptionActivation</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_telematics_subscription_activation_proxy_new_finish() to get the result of the operation.
 *
 * See com_lge_temil_telematics_subscription_activation_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
com_lge_temil_telematics_subscription_activation_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_COM_LGE_TEMIL_TELEMATICS_SUBSCRIPTION_ACTIVATION_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Telematics.SubscriptionActivation", NULL);
}

/**
 * com_lge_temil_telematics_subscription_activation_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_telematics_subscription_activation_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with com_lge_temil_telematics_subscription_activation_proxy_new().
 *
 * Returns: (transfer full) (type ComLgeTemilTelematicsSubscriptionActivationProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilTelematicsSubscriptionActivation *
com_lge_temil_telematics_subscription_activation_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return COM_LGE_TEMIL_TELEMATICS_SUBSCRIPTION_ACTIVATION (ret);
  else
    return NULL;
}

/**
 * com_lge_temil_telematics_subscription_activation_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Telematics-SubscriptionActivation.top_of_page">com.lge.temil.Telematics.SubscriptionActivation</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_telematics_subscription_activation_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type ComLgeTemilTelematicsSubscriptionActivationProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilTelematicsSubscriptionActivation *
com_lge_temil_telematics_subscription_activation_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_COM_LGE_TEMIL_TELEMATICS_SUBSCRIPTION_ACTIVATION_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Telematics.SubscriptionActivation", NULL);
  if (ret != NULL)
    return COM_LGE_TEMIL_TELEMATICS_SUBSCRIPTION_ACTIVATION (ret);
  else
    return NULL;
}


/**
 * com_lge_temil_telematics_subscription_activation_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like com_lge_temil_telematics_subscription_activation_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_telematics_subscription_activation_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See com_lge_temil_telematics_subscription_activation_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
com_lge_temil_telematics_subscription_activation_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_COM_LGE_TEMIL_TELEMATICS_SUBSCRIPTION_ACTIVATION_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Telematics.SubscriptionActivation", NULL);
}

/**
 * com_lge_temil_telematics_subscription_activation_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_telematics_subscription_activation_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with com_lge_temil_telematics_subscription_activation_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type ComLgeTemilTelematicsSubscriptionActivationProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilTelematicsSubscriptionActivation *
com_lge_temil_telematics_subscription_activation_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return COM_LGE_TEMIL_TELEMATICS_SUBSCRIPTION_ACTIVATION (ret);
  else
    return NULL;
}

/**
 * com_lge_temil_telematics_subscription_activation_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like com_lge_temil_telematics_subscription_activation_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_telematics_subscription_activation_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type ComLgeTemilTelematicsSubscriptionActivationProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilTelematicsSubscriptionActivation *
com_lge_temil_telematics_subscription_activation_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_COM_LGE_TEMIL_TELEMATICS_SUBSCRIPTION_ACTIVATION_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Telematics.SubscriptionActivation", NULL);
  if (ret != NULL)
    return COM_LGE_TEMIL_TELEMATICS_SUBSCRIPTION_ACTIVATION (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * ComLgeTemilTelematicsSubscriptionActivationSkeleton:
 *
 * The #ComLgeTemilTelematicsSubscriptionActivationSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * ComLgeTemilTelematicsSubscriptionActivationSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #ComLgeTemilTelematicsSubscriptionActivationSkeleton.
 */

struct _ComLgeTemilTelematicsSubscriptionActivationSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_com_lge_temil_telematics_subscription_activation_skeleton_handle_method_call (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  ComLgeTemilTelematicsSubscriptionActivationSkeleton *skeleton = COM_LGE_TEMIL_TELEMATICS_SUBSCRIPTION_ACTIVATION_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], TYPE_COM_LGE_TEMIL_TELEMATICS_SUBSCRIPTION_ACTIVATION);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_COM_LGE_TEMIL_TELEMATICS_SUBSCRIPTION_ACTIVATION);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_com_lge_temil_telematics_subscription_activation_skeleton_handle_get_property (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  ComLgeTemilTelematicsSubscriptionActivationSkeleton *skeleton = COM_LGE_TEMIL_TELEMATICS_SUBSCRIPTION_ACTIVATION_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_telematics_subscription_activation_interface_info, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_com_lge_temil_telematics_subscription_activation_skeleton_handle_set_property (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  ComLgeTemilTelematicsSubscriptionActivationSkeleton *skeleton = COM_LGE_TEMIL_TELEMATICS_SUBSCRIPTION_ACTIVATION_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_telematics_subscription_activation_interface_info, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _com_lge_temil_telematics_subscription_activation_skeleton_vtable =
{
  _com_lge_temil_telematics_subscription_activation_skeleton_handle_method_call,
  _com_lge_temil_telematics_subscription_activation_skeleton_handle_get_property,
  _com_lge_temil_telematics_subscription_activation_skeleton_handle_set_property
};

static GDBusInterfaceInfo *
com_lge_temil_telematics_subscription_activation_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton)
{
  return com_lge_temil_telematics_subscription_activation_interface_info ();
}

static GDBusInterfaceVTable *
com_lge_temil_telematics_subscription_activation_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton)
{
  return (GDBusInterfaceVTable *) &_com_lge_temil_telematics_subscription_activation_skeleton_vtable;
}

static GVariant *
com_lge_temil_telematics_subscription_activation_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  ComLgeTemilTelematicsSubscriptionActivationSkeleton *skeleton = COM_LGE_TEMIL_TELEMATICS_SUBSCRIPTION_ACTIVATION_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_com_lge_temil_telematics_subscription_activation_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _com_lge_temil_telematics_subscription_activation_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _com_lge_temil_telematics_subscription_activation_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _com_lge_temil_telematics_subscription_activation_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.lge.temil.Telematics.SubscriptionActivation", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static void
com_lge_temil_telematics_subscription_activation_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
}

static void
_com_lge_temil_telematics_subscription_activation_on_signal_notification (
    ComLgeTemilTelematicsSubscriptionActivation *object,
    guint arg_status)
{
  ComLgeTemilTelematicsSubscriptionActivationSkeleton *skeleton = COM_LGE_TEMIL_TELEMATICS_SUBSCRIPTION_ACTIVATION_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(u)",
                   arg_status));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.lge.temil.Telematics.SubscriptionActivation", "Notification",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void com_lge_temil_telematics_subscription_activation_skeleton_iface_init (ComLgeTemilTelematicsSubscriptionActivationIface *iface);
G_DEFINE_TYPE_WITH_CODE (ComLgeTemilTelematicsSubscriptionActivationSkeleton, com_lge_temil_telematics_subscription_activation_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (TYPE_COM_LGE_TEMIL_TELEMATICS_SUBSCRIPTION_ACTIVATION, com_lge_temil_telematics_subscription_activation_skeleton_iface_init));

static void
com_lge_temil_telematics_subscription_activation_skeleton_finalize (GObject *object)
{
  ComLgeTemilTelematicsSubscriptionActivationSkeleton *skeleton = COM_LGE_TEMIL_TELEMATICS_SUBSCRIPTION_ACTIVATION_SKELETON (object);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (com_lge_temil_telematics_subscription_activation_skeleton_parent_class)->finalize (object);
}

static void
com_lge_temil_telematics_subscription_activation_skeleton_init (ComLgeTemilTelematicsSubscriptionActivationSkeleton *skeleton)
{
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, TYPE_COM_LGE_TEMIL_TELEMATICS_SUBSCRIPTION_ACTIVATION_SKELETON, ComLgeTemilTelematicsSubscriptionActivationSkeletonPrivate);
  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
}

static void
com_lge_temil_telematics_subscription_activation_skeleton_class_init (ComLgeTemilTelematicsSubscriptionActivationSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  g_type_class_add_private (klass, sizeof (ComLgeTemilTelematicsSubscriptionActivationSkeletonPrivate));

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = com_lge_temil_telematics_subscription_activation_skeleton_finalize;

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = com_lge_temil_telematics_subscription_activation_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = com_lge_temil_telematics_subscription_activation_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = com_lge_temil_telematics_subscription_activation_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = com_lge_temil_telematics_subscription_activation_skeleton_dbus_interface_get_vtable;
}

static void
com_lge_temil_telematics_subscription_activation_skeleton_iface_init (ComLgeTemilTelematicsSubscriptionActivationIface *iface)
{
  iface->notification = _com_lge_temil_telematics_subscription_activation_on_signal_notification;
}

/**
 * com_lge_temil_telematics_subscription_activation_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Telematics-SubscriptionActivation.top_of_page">com.lge.temil.Telematics.SubscriptionActivation</link>.
 *
 * Returns: (transfer full) (type ComLgeTemilTelematicsSubscriptionActivationSkeleton): The skeleton object.
 */
ComLgeTemilTelematicsSubscriptionActivation *
com_lge_temil_telematics_subscription_activation_skeleton_new (void)
{
  return COM_LGE_TEMIL_TELEMATICS_SUBSCRIPTION_ACTIVATION (g_object_new (TYPE_COM_LGE_TEMIL_TELEMATICS_SUBSCRIPTION_ACTIVATION_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface com.lge.temil.Telematics.ServiceActivation
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:ComLgeTemilTelematicsServiceActivation
 * @title: ComLgeTemilTelematicsServiceActivation
 * @short_description: Generated C code for the com.lge.temil.Telematics.ServiceActivation D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-com-lge-temil-Telematics-ServiceActivation.top_of_page">com.lge.temil.Telematics.ServiceActivation</link> D-Bus interface in C.
 */

/* ---- Introspection data for com.lge.temil.Telematics.ServiceActivation ---- */

static const _ExtendedGDBusArgInfo _com_lge_temil_telematics_service_activation_method_info_set_request_IN_ARG_service =
{
  {
    -1,
    "service",
    "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _com_lge_temil_telematics_service_activation_method_info_set_request_IN_ARG_action =
{
  {
    -1,
    "action",
    "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _com_lge_temil_telematics_service_activation_method_info_set_request_IN_ARG_pointers[] =
{
  &_com_lge_temil_telematics_service_activation_method_info_set_request_IN_ARG_service,
  &_com_lge_temil_telematics_service_activation_method_info_set_request_IN_ARG_action,
  NULL
};

static const _ExtendedGDBusMethodInfo _com_lge_temil_telematics_service_activation_method_info_set_request =
{
  {
    -1,
    "SetRequest",
    (GDBusArgInfo **) &_com_lge_temil_telematics_service_activation_method_info_set_request_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-set-request",
  FALSE
};

static const _ExtendedGDBusMethodInfo * const _com_lge_temil_telematics_service_activation_method_info_pointers[] =
{
  &_com_lge_temil_telematics_service_activation_method_info_set_request,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _com_lge_temil_telematics_service_activation_interface_info =
{
  {
    -1,
    "com.lge.temil.Telematics.ServiceActivation",
    (GDBusMethodInfo **) &_com_lge_temil_telematics_service_activation_method_info_pointers,
    NULL,
    NULL,
    NULL
  },
  "com-lge-temil-telematics-service-activation",
};


/**
 * com_lge_temil_telematics_service_activation_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-com-lge-temil-Telematics-ServiceActivation.top_of_page">com.lge.temil.Telematics.ServiceActivation</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
com_lge_temil_telematics_service_activation_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_com_lge_temil_telematics_service_activation_interface_info;
}

/**
 * com_lge_temil_telematics_service_activation_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #ComLgeTemilTelematicsServiceActivation interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
com_lge_temil_telematics_service_activation_override_properties (GObjectClass *klass, guint property_id_begin)
{
  return property_id_begin - 1;
}



/**
 * ComLgeTemilTelematicsServiceActivation:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Telematics-ServiceActivation.top_of_page">com.lge.temil.Telematics.ServiceActivation</link>.
 */

/**
 * ComLgeTemilTelematicsServiceActivationIface:
 * @parent_iface: The parent interface.
 * @handle_set_request: Handler for the #ComLgeTemilTelematicsServiceActivation::handle-set-request signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Telematics-ServiceActivation.top_of_page">com.lge.temil.Telematics.ServiceActivation</link>.
 */

static void
com_lge_temil_telematics_service_activation_default_init (ComLgeTemilTelematicsServiceActivationIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * ComLgeTemilTelematicsServiceActivation::handle-set-request:
   * @object: A #ComLgeTemilTelematicsServiceActivation.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_service: Argument passed by remote caller.
   * @arg_action: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-lge-temil-Telematics-ServiceActivation.SetRequest">SetRequest()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call com_lge_temil_telematics_service_activation_complete_set_request() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-set-request",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (ComLgeTemilTelematicsServiceActivationIface, handle_set_request),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_UINT, G_TYPE_UINT);

}

typedef ComLgeTemilTelematicsServiceActivationIface ComLgeTemilTelematicsServiceActivationInterface;
G_DEFINE_INTERFACE (ComLgeTemilTelematicsServiceActivation, com_lge_temil_telematics_service_activation, G_TYPE_OBJECT);

/**
 * com_lge_temil_telematics_service_activation_call_set_request:
 * @proxy: A #ComLgeTemilTelematicsServiceActivationProxy.
 * @arg_service: Argument to pass with the method invocation.
 * @arg_action: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-lge-temil-Telematics-ServiceActivation.SetRequest">SetRequest()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_telematics_service_activation_call_set_request_finish() to get the result of the operation.
 *
 * See com_lge_temil_telematics_service_activation_call_set_request_sync() for the synchronous, blocking version of this method.
 */
void
com_lge_temil_telematics_service_activation_call_set_request (
    ComLgeTemilTelematicsServiceActivation *proxy,
    guint arg_service,
    guint arg_action,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "SetRequest",
    g_variant_new ("(uu)",
                   arg_service,
                   arg_action),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * com_lge_temil_telematics_service_activation_call_set_request_finish:
 * @proxy: A #ComLgeTemilTelematicsServiceActivationProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_telematics_service_activation_call_set_request().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with com_lge_temil_telematics_service_activation_call_set_request().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_telematics_service_activation_call_set_request_finish (
    ComLgeTemilTelematicsServiceActivation *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_telematics_service_activation_call_set_request_sync:
 * @proxy: A #ComLgeTemilTelematicsServiceActivationProxy.
 * @arg_service: Argument to pass with the method invocation.
 * @arg_action: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-lge-temil-Telematics-ServiceActivation.SetRequest">SetRequest()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_telematics_service_activation_call_set_request() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_telematics_service_activation_call_set_request_sync (
    ComLgeTemilTelematicsServiceActivation *proxy,
    guint arg_service,
    guint arg_action,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "SetRequest",
    g_variant_new ("(uu)",
                   arg_service,
                   arg_action),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_telematics_service_activation_complete_set_request:
 * @object: A #ComLgeTemilTelematicsServiceActivation.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-lge-temil-Telematics-ServiceActivation.SetRequest">SetRequest()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
com_lge_temil_telematics_service_activation_complete_set_request (
    ComLgeTemilTelematicsServiceActivation *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * ComLgeTemilTelematicsServiceActivationProxy:
 *
 * The #ComLgeTemilTelematicsServiceActivationProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * ComLgeTemilTelematicsServiceActivationProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #ComLgeTemilTelematicsServiceActivationProxy.
 */

struct _ComLgeTemilTelematicsServiceActivationProxyPrivate
{
  GData *qdata;
};

static void com_lge_temil_telematics_service_activation_proxy_iface_init (ComLgeTemilTelematicsServiceActivationIface *iface);

G_DEFINE_TYPE_WITH_CODE (ComLgeTemilTelematicsServiceActivationProxy, com_lge_temil_telematics_service_activation_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (TYPE_COM_LGE_TEMIL_TELEMATICS_SERVICE_ACTIVATION, com_lge_temil_telematics_service_activation_proxy_iface_init));

static void
com_lge_temil_telematics_service_activation_proxy_finalize (GObject *object)
{
  ComLgeTemilTelematicsServiceActivationProxy *proxy = COM_LGE_TEMIL_TELEMATICS_SERVICE_ACTIVATION_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (com_lge_temil_telematics_service_activation_proxy_parent_class)->finalize (object);
}

static void
com_lge_temil_telematics_service_activation_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec)
{
}

static void
com_lge_temil_telematics_service_activation_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
}

static void
com_lge_temil_telematics_service_activation_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_com_lge_temil_telematics_service_activation_interface_info, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], TYPE_COM_LGE_TEMIL_TELEMATICS_SERVICE_ACTIVATION);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_COM_LGE_TEMIL_TELEMATICS_SERVICE_ACTIVATION);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
com_lge_temil_telematics_service_activation_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  ComLgeTemilTelematicsServiceActivationProxy *proxy = COM_LGE_TEMIL_TELEMATICS_SERVICE_ACTIVATION_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_telematics_service_activation_interface_info, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_telematics_service_activation_interface_info, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static void
com_lge_temil_telematics_service_activation_proxy_init (ComLgeTemilTelematicsServiceActivationProxy *proxy)
{
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, TYPE_COM_LGE_TEMIL_TELEMATICS_SERVICE_ACTIVATION_PROXY, ComLgeTemilTelematicsServiceActivationProxyPrivate);
  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), com_lge_temil_telematics_service_activation_interface_info ());
}

static void
com_lge_temil_telematics_service_activation_proxy_class_init (ComLgeTemilTelematicsServiceActivationProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  g_type_class_add_private (klass, sizeof (ComLgeTemilTelematicsServiceActivationProxyPrivate));

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = com_lge_temil_telematics_service_activation_proxy_finalize;
  gobject_class->get_property = com_lge_temil_telematics_service_activation_proxy_get_property;
  gobject_class->set_property = com_lge_temil_telematics_service_activation_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = com_lge_temil_telematics_service_activation_proxy_g_signal;
  proxy_class->g_properties_changed = com_lge_temil_telematics_service_activation_proxy_g_properties_changed;

}

static void
com_lge_temil_telematics_service_activation_proxy_iface_init (ComLgeTemilTelematicsServiceActivationIface *iface)
{
}

/**
 * com_lge_temil_telematics_service_activation_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Telematics-ServiceActivation.top_of_page">com.lge.temil.Telematics.ServiceActivation</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_telematics_service_activation_proxy_new_finish() to get the result of the operation.
 *
 * See com_lge_temil_telematics_service_activation_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
com_lge_temil_telematics_service_activation_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_COM_LGE_TEMIL_TELEMATICS_SERVICE_ACTIVATION_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Telematics.ServiceActivation", NULL);
}

/**
 * com_lge_temil_telematics_service_activation_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_telematics_service_activation_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with com_lge_temil_telematics_service_activation_proxy_new().
 *
 * Returns: (transfer full) (type ComLgeTemilTelematicsServiceActivationProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilTelematicsServiceActivation *
com_lge_temil_telematics_service_activation_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return COM_LGE_TEMIL_TELEMATICS_SERVICE_ACTIVATION (ret);
  else
    return NULL;
}

/**
 * com_lge_temil_telematics_service_activation_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Telematics-ServiceActivation.top_of_page">com.lge.temil.Telematics.ServiceActivation</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_telematics_service_activation_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type ComLgeTemilTelematicsServiceActivationProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilTelematicsServiceActivation *
com_lge_temil_telematics_service_activation_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_COM_LGE_TEMIL_TELEMATICS_SERVICE_ACTIVATION_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Telematics.ServiceActivation", NULL);
  if (ret != NULL)
    return COM_LGE_TEMIL_TELEMATICS_SERVICE_ACTIVATION (ret);
  else
    return NULL;
}


/**
 * com_lge_temil_telematics_service_activation_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like com_lge_temil_telematics_service_activation_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_telematics_service_activation_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See com_lge_temil_telematics_service_activation_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
com_lge_temil_telematics_service_activation_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_COM_LGE_TEMIL_TELEMATICS_SERVICE_ACTIVATION_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Telematics.ServiceActivation", NULL);
}

/**
 * com_lge_temil_telematics_service_activation_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_telematics_service_activation_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with com_lge_temil_telematics_service_activation_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type ComLgeTemilTelematicsServiceActivationProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilTelematicsServiceActivation *
com_lge_temil_telematics_service_activation_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return COM_LGE_TEMIL_TELEMATICS_SERVICE_ACTIVATION (ret);
  else
    return NULL;
}

/**
 * com_lge_temil_telematics_service_activation_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like com_lge_temil_telematics_service_activation_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_telematics_service_activation_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type ComLgeTemilTelematicsServiceActivationProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilTelematicsServiceActivation *
com_lge_temil_telematics_service_activation_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_COM_LGE_TEMIL_TELEMATICS_SERVICE_ACTIVATION_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Telematics.ServiceActivation", NULL);
  if (ret != NULL)
    return COM_LGE_TEMIL_TELEMATICS_SERVICE_ACTIVATION (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * ComLgeTemilTelematicsServiceActivationSkeleton:
 *
 * The #ComLgeTemilTelematicsServiceActivationSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * ComLgeTemilTelematicsServiceActivationSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #ComLgeTemilTelematicsServiceActivationSkeleton.
 */

struct _ComLgeTemilTelematicsServiceActivationSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_com_lge_temil_telematics_service_activation_skeleton_handle_method_call (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  ComLgeTemilTelematicsServiceActivationSkeleton *skeleton = COM_LGE_TEMIL_TELEMATICS_SERVICE_ACTIVATION_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], TYPE_COM_LGE_TEMIL_TELEMATICS_SERVICE_ACTIVATION);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_COM_LGE_TEMIL_TELEMATICS_SERVICE_ACTIVATION);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_com_lge_temil_telematics_service_activation_skeleton_handle_get_property (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  ComLgeTemilTelematicsServiceActivationSkeleton *skeleton = COM_LGE_TEMIL_TELEMATICS_SERVICE_ACTIVATION_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_telematics_service_activation_interface_info, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_com_lge_temil_telematics_service_activation_skeleton_handle_set_property (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  ComLgeTemilTelematicsServiceActivationSkeleton *skeleton = COM_LGE_TEMIL_TELEMATICS_SERVICE_ACTIVATION_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_telematics_service_activation_interface_info, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _com_lge_temil_telematics_service_activation_skeleton_vtable =
{
  _com_lge_temil_telematics_service_activation_skeleton_handle_method_call,
  _com_lge_temil_telematics_service_activation_skeleton_handle_get_property,
  _com_lge_temil_telematics_service_activation_skeleton_handle_set_property
};

static GDBusInterfaceInfo *
com_lge_temil_telematics_service_activation_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton)
{
  return com_lge_temil_telematics_service_activation_interface_info ();
}

static GDBusInterfaceVTable *
com_lge_temil_telematics_service_activation_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton)
{
  return (GDBusInterfaceVTable *) &_com_lge_temil_telematics_service_activation_skeleton_vtable;
}

static GVariant *
com_lge_temil_telematics_service_activation_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  ComLgeTemilTelematicsServiceActivationSkeleton *skeleton = COM_LGE_TEMIL_TELEMATICS_SERVICE_ACTIVATION_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_com_lge_temil_telematics_service_activation_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _com_lge_temil_telematics_service_activation_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _com_lge_temil_telematics_service_activation_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _com_lge_temil_telematics_service_activation_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.lge.temil.Telematics.ServiceActivation", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static void
com_lge_temil_telematics_service_activation_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
}

static void com_lge_temil_telematics_service_activation_skeleton_iface_init (ComLgeTemilTelematicsServiceActivationIface *iface);
G_DEFINE_TYPE_WITH_CODE (ComLgeTemilTelematicsServiceActivationSkeleton, com_lge_temil_telematics_service_activation_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (TYPE_COM_LGE_TEMIL_TELEMATICS_SERVICE_ACTIVATION, com_lge_temil_telematics_service_activation_skeleton_iface_init));

static void
com_lge_temil_telematics_service_activation_skeleton_finalize (GObject *object)
{
  ComLgeTemilTelematicsServiceActivationSkeleton *skeleton = COM_LGE_TEMIL_TELEMATICS_SERVICE_ACTIVATION_SKELETON (object);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (com_lge_temil_telematics_service_activation_skeleton_parent_class)->finalize (object);
}

static void
com_lge_temil_telematics_service_activation_skeleton_init (ComLgeTemilTelematicsServiceActivationSkeleton *skeleton)
{
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, TYPE_COM_LGE_TEMIL_TELEMATICS_SERVICE_ACTIVATION_SKELETON, ComLgeTemilTelematicsServiceActivationSkeletonPrivate);
  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
}

static void
com_lge_temil_telematics_service_activation_skeleton_class_init (ComLgeTemilTelematicsServiceActivationSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  g_type_class_add_private (klass, sizeof (ComLgeTemilTelematicsServiceActivationSkeletonPrivate));

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = com_lge_temil_telematics_service_activation_skeleton_finalize;

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = com_lge_temil_telematics_service_activation_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = com_lge_temil_telematics_service_activation_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = com_lge_temil_telematics_service_activation_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = com_lge_temil_telematics_service_activation_skeleton_dbus_interface_get_vtable;
}

static void
com_lge_temil_telematics_service_activation_skeleton_iface_init (ComLgeTemilTelematicsServiceActivationIface *iface)
{
}

/**
 * com_lge_temil_telematics_service_activation_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Telematics-ServiceActivation.top_of_page">com.lge.temil.Telematics.ServiceActivation</link>.
 *
 * Returns: (transfer full) (type ComLgeTemilTelematicsServiceActivationSkeleton): The skeleton object.
 */
ComLgeTemilTelematicsServiceActivation *
com_lge_temil_telematics_service_activation_skeleton_new (void)
{
  return COM_LGE_TEMIL_TELEMATICS_SERVICE_ACTIVATION (g_object_new (TYPE_COM_LGE_TEMIL_TELEMATICS_SERVICE_ACTIVATION_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface com.lge.temil.Telematics.RescueStatus
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:ComLgeTemilTelematicsRescueStatus
 * @title: ComLgeTemilTelematicsRescueStatus
 * @short_description: Generated C code for the com.lge.temil.Telematics.RescueStatus D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-com-lge-temil-Telematics-RescueStatus.top_of_page">com.lge.temil.Telematics.RescueStatus</link> D-Bus interface in C.
 */

/* ---- Introspection data for com.lge.temil.Telematics.RescueStatus ---- */

static const _ExtendedGDBusArgInfo _com_lge_temil_telematics_rescue_status_method_info_request_OUT_ARG_eCallStatus =
{
  {
    -1,
    "eCallStatus",
    "(uuuuuuuu)",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _com_lge_temil_telematics_rescue_status_method_info_request_OUT_ARG_bCallStatus =
{
  {
    -1,
    "bCallStatus",
    "(uuuuuuuu)",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _com_lge_temil_telematics_rescue_status_method_info_request_OUT_ARG_iCallStatus =
{
  {
    -1,
    "iCallStatus",
    "(uuuuuuuu)",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _com_lge_temil_telematics_rescue_status_method_info_request_OUT_ARG_sdnStatus =
{
  {
    -1,
    "sdnStatus",
    "(uuuuuuuu)",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _com_lge_temil_telematics_rescue_status_method_info_request_OUT_ARG_backupAudioStatus =
{
  {
    -1,
    "backupAudioStatus",
    "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _com_lge_temil_telematics_rescue_status_method_info_request_OUT_ARG_pointers[] =
{
  &_com_lge_temil_telematics_rescue_status_method_info_request_OUT_ARG_eCallStatus,
  &_com_lge_temil_telematics_rescue_status_method_info_request_OUT_ARG_bCallStatus,
  &_com_lge_temil_telematics_rescue_status_method_info_request_OUT_ARG_iCallStatus,
  &_com_lge_temil_telematics_rescue_status_method_info_request_OUT_ARG_sdnStatus,
  &_com_lge_temil_telematics_rescue_status_method_info_request_OUT_ARG_backupAudioStatus,
  NULL
};

static const _ExtendedGDBusMethodInfo _com_lge_temil_telematics_rescue_status_method_info_request =
{
  {
    -1,
    "Request",
    NULL,
    (GDBusArgInfo **) &_com_lge_temil_telematics_rescue_status_method_info_request_OUT_ARG_pointers,
    NULL
  },
  "handle-request",
  FALSE
};

static const _ExtendedGDBusArgInfo _com_lge_temil_telematics_rescue_status_method_info_notification_request_IN_ARG_subscribed =
{
  {
    -1,
    "subscribed",
    "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _com_lge_temil_telematics_rescue_status_method_info_notification_request_IN_ARG_pointers[] =
{
  &_com_lge_temil_telematics_rescue_status_method_info_notification_request_IN_ARG_subscribed,
  NULL
};

static const _ExtendedGDBusMethodInfo _com_lge_temil_telematics_rescue_status_method_info_notification_request =
{
  {
    -1,
    "NotificationRequest",
    (GDBusArgInfo **) &_com_lge_temil_telematics_rescue_status_method_info_notification_request_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-notification-request",
  FALSE
};

static const _ExtendedGDBusMethodInfo * const _com_lge_temil_telematics_rescue_status_method_info_pointers[] =
{
  &_com_lge_temil_telematics_rescue_status_method_info_request,
  &_com_lge_temil_telematics_rescue_status_method_info_notification_request,
  NULL
};

static const _ExtendedGDBusArgInfo _com_lge_temil_telematics_rescue_status_signal_info_notification_ARG_eCallStatus =
{
  {
    -1,
    "eCallStatus",
    "(uuuuuuuu)",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _com_lge_temil_telematics_rescue_status_signal_info_notification_ARG_bCallStatus =
{
  {
    -1,
    "bCallStatus",
    "(uuuuuuuu)",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _com_lge_temil_telematics_rescue_status_signal_info_notification_ARG_iCallStatus =
{
  {
    -1,
    "iCallStatus",
    "(uuuuuuuu)",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _com_lge_temil_telematics_rescue_status_signal_info_notification_ARG_sdnStatus =
{
  {
    -1,
    "sdnStatus",
    "(uuuuuuuu)",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _com_lge_temil_telematics_rescue_status_signal_info_notification_ARG_backupAudioStatus =
{
  {
    -1,
    "backupAudioStatus",
    "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _com_lge_temil_telematics_rescue_status_signal_info_notification_ARG_pointers[] =
{
  &_com_lge_temil_telematics_rescue_status_signal_info_notification_ARG_eCallStatus,
  &_com_lge_temil_telematics_rescue_status_signal_info_notification_ARG_bCallStatus,
  &_com_lge_temil_telematics_rescue_status_signal_info_notification_ARG_iCallStatus,
  &_com_lge_temil_telematics_rescue_status_signal_info_notification_ARG_sdnStatus,
  &_com_lge_temil_telematics_rescue_status_signal_info_notification_ARG_backupAudioStatus,
  NULL
};

static const _ExtendedGDBusSignalInfo _com_lge_temil_telematics_rescue_status_signal_info_notification =
{
  {
    -1,
    "Notification",
    (GDBusArgInfo **) &_com_lge_temil_telematics_rescue_status_signal_info_notification_ARG_pointers,
    NULL
  },
  "notification"
};

static const _ExtendedGDBusSignalInfo * const _com_lge_temil_telematics_rescue_status_signal_info_pointers[] =
{
  &_com_lge_temil_telematics_rescue_status_signal_info_notification,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _com_lge_temil_telematics_rescue_status_interface_info =
{
  {
    -1,
    "com.lge.temil.Telematics.RescueStatus",
    (GDBusMethodInfo **) &_com_lge_temil_telematics_rescue_status_method_info_pointers,
    (GDBusSignalInfo **) &_com_lge_temil_telematics_rescue_status_signal_info_pointers,
    NULL,
    NULL
  },
  "com-lge-temil-telematics-rescue-status",
};


/**
 * com_lge_temil_telematics_rescue_status_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-com-lge-temil-Telematics-RescueStatus.top_of_page">com.lge.temil.Telematics.RescueStatus</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
com_lge_temil_telematics_rescue_status_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_com_lge_temil_telematics_rescue_status_interface_info;
}

/**
 * com_lge_temil_telematics_rescue_status_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #ComLgeTemilTelematicsRescueStatus interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
com_lge_temil_telematics_rescue_status_override_properties (GObjectClass *klass, guint property_id_begin)
{
  return property_id_begin - 1;
}



/**
 * ComLgeTemilTelematicsRescueStatus:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Telematics-RescueStatus.top_of_page">com.lge.temil.Telematics.RescueStatus</link>.
 */

/**
 * ComLgeTemilTelematicsRescueStatusIface:
 * @parent_iface: The parent interface.
 * @handle_notification_request: Handler for the #ComLgeTemilTelematicsRescueStatus::handle-notification-request signal.
 * @handle_request: Handler for the #ComLgeTemilTelematicsRescueStatus::handle-request signal.
 * @notification: Handler for the #ComLgeTemilTelematicsRescueStatus::notification signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Telematics-RescueStatus.top_of_page">com.lge.temil.Telematics.RescueStatus</link>.
 */

static void
com_lge_temil_telematics_rescue_status_default_init (ComLgeTemilTelematicsRescueStatusIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * ComLgeTemilTelematicsRescueStatus::handle-request:
   * @object: A #ComLgeTemilTelematicsRescueStatus.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-lge-temil-Telematics-RescueStatus.Request">Request()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call com_lge_temil_telematics_rescue_status_complete_request() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-request",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (ComLgeTemilTelematicsRescueStatusIface, handle_request),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * ComLgeTemilTelematicsRescueStatus::handle-notification-request:
   * @object: A #ComLgeTemilTelematicsRescueStatus.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_subscribed: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-lge-temil-Telematics-RescueStatus.NotificationRequest">NotificationRequest()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call com_lge_temil_telematics_rescue_status_complete_notification_request() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-notification-request",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (ComLgeTemilTelematicsRescueStatusIface, handle_notification_request),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_BOOLEAN);

  /* GObject signals for received D-Bus signals: */
  /**
   * ComLgeTemilTelematicsRescueStatus::notification:
   * @object: A #ComLgeTemilTelematicsRescueStatus.
   * @arg_eCallStatus: Argument.
   * @arg_bCallStatus: Argument.
   * @arg_iCallStatus: Argument.
   * @arg_sdnStatus: Argument.
   * @arg_backupAudioStatus: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-lge-temil-Telematics-RescueStatus.Notification">"Notification"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("notification",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (ComLgeTemilTelematicsRescueStatusIface, notification),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    5, G_TYPE_VARIANT, G_TYPE_VARIANT, G_TYPE_VARIANT, G_TYPE_VARIANT, G_TYPE_UINT);

}

typedef ComLgeTemilTelematicsRescueStatusIface ComLgeTemilTelematicsRescueStatusInterface;
G_DEFINE_INTERFACE (ComLgeTemilTelematicsRescueStatus, com_lge_temil_telematics_rescue_status, G_TYPE_OBJECT);

/**
 * com_lge_temil_telematics_rescue_status_emit_notification:
 * @object: A #ComLgeTemilTelematicsRescueStatus.
 * @arg_eCallStatus: Argument to pass with the signal.
 * @arg_bCallStatus: Argument to pass with the signal.
 * @arg_iCallStatus: Argument to pass with the signal.
 * @arg_sdnStatus: Argument to pass with the signal.
 * @arg_backupAudioStatus: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-com-lge-temil-Telematics-RescueStatus.Notification">"Notification"</link> D-Bus signal.
 */
void
com_lge_temil_telematics_rescue_status_emit_notification (
    ComLgeTemilTelematicsRescueStatus *object,
    GVariant *arg_eCallStatus,
    GVariant *arg_bCallStatus,
    GVariant *arg_iCallStatus,
    GVariant *arg_sdnStatus,
    guint arg_backupAudioStatus)
{
  g_signal_emit_by_name (object, "notification", arg_eCallStatus, arg_bCallStatus, arg_iCallStatus, arg_sdnStatus, arg_backupAudioStatus);
}

/**
 * com_lge_temil_telematics_rescue_status_call_request:
 * @proxy: A #ComLgeTemilTelematicsRescueStatusProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-lge-temil-Telematics-RescueStatus.Request">Request()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_telematics_rescue_status_call_request_finish() to get the result of the operation.
 *
 * See com_lge_temil_telematics_rescue_status_call_request_sync() for the synchronous, blocking version of this method.
 */
void
com_lge_temil_telematics_rescue_status_call_request (
    ComLgeTemilTelematicsRescueStatus *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Request",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * com_lge_temil_telematics_rescue_status_call_request_finish:
 * @proxy: A #ComLgeTemilTelematicsRescueStatusProxy.
 * @out_eCallStatus: (out): Return location for return parameter or %NULL to ignore.
 * @out_bCallStatus: (out): Return location for return parameter or %NULL to ignore.
 * @out_iCallStatus: (out): Return location for return parameter or %NULL to ignore.
 * @out_sdnStatus: (out): Return location for return parameter or %NULL to ignore.
 * @out_backupAudioStatus: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_telematics_rescue_status_call_request().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with com_lge_temil_telematics_rescue_status_call_request().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_telematics_rescue_status_call_request_finish (
    ComLgeTemilTelematicsRescueStatus *proxy,
    GVariant **out_eCallStatus,
    GVariant **out_bCallStatus,
    GVariant **out_iCallStatus,
    GVariant **out_sdnStatus,
    guint *out_backupAudioStatus,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@(uuuuuuuu)@(uuuuuuuu)@(uuuuuuuu)@(uuuuuuuu)u)",
                 out_eCallStatus,
                 out_bCallStatus,
                 out_iCallStatus,
                 out_sdnStatus,
                 out_backupAudioStatus);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_telematics_rescue_status_call_request_sync:
 * @proxy: A #ComLgeTemilTelematicsRescueStatusProxy.
 * @out_eCallStatus: (out): Return location for return parameter or %NULL to ignore.
 * @out_bCallStatus: (out): Return location for return parameter or %NULL to ignore.
 * @out_iCallStatus: (out): Return location for return parameter or %NULL to ignore.
 * @out_sdnStatus: (out): Return location for return parameter or %NULL to ignore.
 * @out_backupAudioStatus: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-lge-temil-Telematics-RescueStatus.Request">Request()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_telematics_rescue_status_call_request() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_telematics_rescue_status_call_request_sync (
    ComLgeTemilTelematicsRescueStatus *proxy,
    GVariant **out_eCallStatus,
    GVariant **out_bCallStatus,
    GVariant **out_iCallStatus,
    GVariant **out_sdnStatus,
    guint *out_backupAudioStatus,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Request",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@(uuuuuuuu)@(uuuuuuuu)@(uuuuuuuu)@(uuuuuuuu)u)",
                 out_eCallStatus,
                 out_bCallStatus,
                 out_iCallStatus,
                 out_sdnStatus,
                 out_backupAudioStatus);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_telematics_rescue_status_call_notification_request:
 * @proxy: A #ComLgeTemilTelematicsRescueStatusProxy.
 * @arg_subscribed: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-lge-temil-Telematics-RescueStatus.NotificationRequest">NotificationRequest()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_telematics_rescue_status_call_notification_request_finish() to get the result of the operation.
 *
 * See com_lge_temil_telematics_rescue_status_call_notification_request_sync() for the synchronous, blocking version of this method.
 */
void
com_lge_temil_telematics_rescue_status_call_notification_request (
    ComLgeTemilTelematicsRescueStatus *proxy,
    gboolean arg_subscribed,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "NotificationRequest",
    g_variant_new ("(b)",
                   arg_subscribed),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * com_lge_temil_telematics_rescue_status_call_notification_request_finish:
 * @proxy: A #ComLgeTemilTelematicsRescueStatusProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_telematics_rescue_status_call_notification_request().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with com_lge_temil_telematics_rescue_status_call_notification_request().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_telematics_rescue_status_call_notification_request_finish (
    ComLgeTemilTelematicsRescueStatus *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_telematics_rescue_status_call_notification_request_sync:
 * @proxy: A #ComLgeTemilTelematicsRescueStatusProxy.
 * @arg_subscribed: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-lge-temil-Telematics-RescueStatus.NotificationRequest">NotificationRequest()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_telematics_rescue_status_call_notification_request() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_telematics_rescue_status_call_notification_request_sync (
    ComLgeTemilTelematicsRescueStatus *proxy,
    gboolean arg_subscribed,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "NotificationRequest",
    g_variant_new ("(b)",
                   arg_subscribed),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_telematics_rescue_status_complete_request:
 * @object: A #ComLgeTemilTelematicsRescueStatus.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @eCallStatus: Parameter to return.
 * @bCallStatus: Parameter to return.
 * @iCallStatus: Parameter to return.
 * @sdnStatus: Parameter to return.
 * @backupAudioStatus: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-lge-temil-Telematics-RescueStatus.Request">Request()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
com_lge_temil_telematics_rescue_status_complete_request (
    ComLgeTemilTelematicsRescueStatus *object,
    GDBusMethodInvocation *invocation,
    GVariant *eCallStatus,
    GVariant *bCallStatus,
    GVariant *iCallStatus,
    GVariant *sdnStatus,
    guint backupAudioStatus)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(@(uuuuuuuu)@(uuuuuuuu)@(uuuuuuuu)@(uuuuuuuu)u)",
                   eCallStatus,
                   bCallStatus,
                   iCallStatus,
                   sdnStatus,
                   backupAudioStatus));
}

/**
 * com_lge_temil_telematics_rescue_status_complete_notification_request:
 * @object: A #ComLgeTemilTelematicsRescueStatus.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-lge-temil-Telematics-RescueStatus.NotificationRequest">NotificationRequest()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
com_lge_temil_telematics_rescue_status_complete_notification_request (
    ComLgeTemilTelematicsRescueStatus *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * ComLgeTemilTelematicsRescueStatusProxy:
 *
 * The #ComLgeTemilTelematicsRescueStatusProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * ComLgeTemilTelematicsRescueStatusProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #ComLgeTemilTelematicsRescueStatusProxy.
 */

struct _ComLgeTemilTelematicsRescueStatusProxyPrivate
{
  GData *qdata;
};

static void com_lge_temil_telematics_rescue_status_proxy_iface_init (ComLgeTemilTelematicsRescueStatusIface *iface);

G_DEFINE_TYPE_WITH_CODE (ComLgeTemilTelematicsRescueStatusProxy, com_lge_temil_telematics_rescue_status_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (TYPE_COM_LGE_TEMIL_TELEMATICS_RESCUE_STATUS, com_lge_temil_telematics_rescue_status_proxy_iface_init));

static void
com_lge_temil_telematics_rescue_status_proxy_finalize (GObject *object)
{
  ComLgeTemilTelematicsRescueStatusProxy *proxy = COM_LGE_TEMIL_TELEMATICS_RESCUE_STATUS_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (com_lge_temil_telematics_rescue_status_proxy_parent_class)->finalize (object);
}

static void
com_lge_temil_telematics_rescue_status_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec)
{
}

static void
com_lge_temil_telematics_rescue_status_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
}

static void
com_lge_temil_telematics_rescue_status_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_com_lge_temil_telematics_rescue_status_interface_info, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], TYPE_COM_LGE_TEMIL_TELEMATICS_RESCUE_STATUS);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_COM_LGE_TEMIL_TELEMATICS_RESCUE_STATUS);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
com_lge_temil_telematics_rescue_status_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  ComLgeTemilTelematicsRescueStatusProxy *proxy = COM_LGE_TEMIL_TELEMATICS_RESCUE_STATUS_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_telematics_rescue_status_interface_info, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_telematics_rescue_status_interface_info, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static void
com_lge_temil_telematics_rescue_status_proxy_init (ComLgeTemilTelematicsRescueStatusProxy *proxy)
{
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, TYPE_COM_LGE_TEMIL_TELEMATICS_RESCUE_STATUS_PROXY, ComLgeTemilTelematicsRescueStatusProxyPrivate);
  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), com_lge_temil_telematics_rescue_status_interface_info ());
}

static void
com_lge_temil_telematics_rescue_status_proxy_class_init (ComLgeTemilTelematicsRescueStatusProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  g_type_class_add_private (klass, sizeof (ComLgeTemilTelematicsRescueStatusProxyPrivate));

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = com_lge_temil_telematics_rescue_status_proxy_finalize;
  gobject_class->get_property = com_lge_temil_telematics_rescue_status_proxy_get_property;
  gobject_class->set_property = com_lge_temil_telematics_rescue_status_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = com_lge_temil_telematics_rescue_status_proxy_g_signal;
  proxy_class->g_properties_changed = com_lge_temil_telematics_rescue_status_proxy_g_properties_changed;

}

static void
com_lge_temil_telematics_rescue_status_proxy_iface_init (ComLgeTemilTelematicsRescueStatusIface *iface)
{
}

/**
 * com_lge_temil_telematics_rescue_status_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Telematics-RescueStatus.top_of_page">com.lge.temil.Telematics.RescueStatus</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_telematics_rescue_status_proxy_new_finish() to get the result of the operation.
 *
 * See com_lge_temil_telematics_rescue_status_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
com_lge_temil_telematics_rescue_status_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_COM_LGE_TEMIL_TELEMATICS_RESCUE_STATUS_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Telematics.RescueStatus", NULL);
}

/**
 * com_lge_temil_telematics_rescue_status_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_telematics_rescue_status_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with com_lge_temil_telematics_rescue_status_proxy_new().
 *
 * Returns: (transfer full) (type ComLgeTemilTelematicsRescueStatusProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilTelematicsRescueStatus *
com_lge_temil_telematics_rescue_status_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return COM_LGE_TEMIL_TELEMATICS_RESCUE_STATUS (ret);
  else
    return NULL;
}

/**
 * com_lge_temil_telematics_rescue_status_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Telematics-RescueStatus.top_of_page">com.lge.temil.Telematics.RescueStatus</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_telematics_rescue_status_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type ComLgeTemilTelematicsRescueStatusProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilTelematicsRescueStatus *
com_lge_temil_telematics_rescue_status_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_COM_LGE_TEMIL_TELEMATICS_RESCUE_STATUS_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Telematics.RescueStatus", NULL);
  if (ret != NULL)
    return COM_LGE_TEMIL_TELEMATICS_RESCUE_STATUS (ret);
  else
    return NULL;
}


/**
 * com_lge_temil_telematics_rescue_status_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like com_lge_temil_telematics_rescue_status_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_telematics_rescue_status_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See com_lge_temil_telematics_rescue_status_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
com_lge_temil_telematics_rescue_status_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_COM_LGE_TEMIL_TELEMATICS_RESCUE_STATUS_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Telematics.RescueStatus", NULL);
}

/**
 * com_lge_temil_telematics_rescue_status_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_telematics_rescue_status_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with com_lge_temil_telematics_rescue_status_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type ComLgeTemilTelematicsRescueStatusProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilTelematicsRescueStatus *
com_lge_temil_telematics_rescue_status_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return COM_LGE_TEMIL_TELEMATICS_RESCUE_STATUS (ret);
  else
    return NULL;
}

/**
 * com_lge_temil_telematics_rescue_status_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like com_lge_temil_telematics_rescue_status_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_telematics_rescue_status_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type ComLgeTemilTelematicsRescueStatusProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilTelematicsRescueStatus *
com_lge_temil_telematics_rescue_status_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_COM_LGE_TEMIL_TELEMATICS_RESCUE_STATUS_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Telematics.RescueStatus", NULL);
  if (ret != NULL)
    return COM_LGE_TEMIL_TELEMATICS_RESCUE_STATUS (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * ComLgeTemilTelematicsRescueStatusSkeleton:
 *
 * The #ComLgeTemilTelematicsRescueStatusSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * ComLgeTemilTelematicsRescueStatusSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #ComLgeTemilTelematicsRescueStatusSkeleton.
 */

struct _ComLgeTemilTelematicsRescueStatusSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_com_lge_temil_telematics_rescue_status_skeleton_handle_method_call (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  ComLgeTemilTelematicsRescueStatusSkeleton *skeleton = COM_LGE_TEMIL_TELEMATICS_RESCUE_STATUS_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], TYPE_COM_LGE_TEMIL_TELEMATICS_RESCUE_STATUS);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_COM_LGE_TEMIL_TELEMATICS_RESCUE_STATUS);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_com_lge_temil_telematics_rescue_status_skeleton_handle_get_property (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  ComLgeTemilTelematicsRescueStatusSkeleton *skeleton = COM_LGE_TEMIL_TELEMATICS_RESCUE_STATUS_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_telematics_rescue_status_interface_info, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_com_lge_temil_telematics_rescue_status_skeleton_handle_set_property (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  ComLgeTemilTelematicsRescueStatusSkeleton *skeleton = COM_LGE_TEMIL_TELEMATICS_RESCUE_STATUS_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_telematics_rescue_status_interface_info, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _com_lge_temil_telematics_rescue_status_skeleton_vtable =
{
  _com_lge_temil_telematics_rescue_status_skeleton_handle_method_call,
  _com_lge_temil_telematics_rescue_status_skeleton_handle_get_property,
  _com_lge_temil_telematics_rescue_status_skeleton_handle_set_property
};

static GDBusInterfaceInfo *
com_lge_temil_telematics_rescue_status_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton)
{
  return com_lge_temil_telematics_rescue_status_interface_info ();
}

static GDBusInterfaceVTable *
com_lge_temil_telematics_rescue_status_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton)
{
  return (GDBusInterfaceVTable *) &_com_lge_temil_telematics_rescue_status_skeleton_vtable;
}

static GVariant *
com_lge_temil_telematics_rescue_status_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  ComLgeTemilTelematicsRescueStatusSkeleton *skeleton = COM_LGE_TEMIL_TELEMATICS_RESCUE_STATUS_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_com_lge_temil_telematics_rescue_status_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _com_lge_temil_telematics_rescue_status_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _com_lge_temil_telematics_rescue_status_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _com_lge_temil_telematics_rescue_status_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.lge.temil.Telematics.RescueStatus", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static void
com_lge_temil_telematics_rescue_status_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
}

static void
_com_lge_temil_telematics_rescue_status_on_signal_notification (
    ComLgeTemilTelematicsRescueStatus *object,
    GVariant *arg_eCallStatus,
    GVariant *arg_bCallStatus,
    GVariant *arg_iCallStatus,
    GVariant *arg_sdnStatus,
    guint arg_backupAudioStatus)
{
  ComLgeTemilTelematicsRescueStatusSkeleton *skeleton = COM_LGE_TEMIL_TELEMATICS_RESCUE_STATUS_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(@(uuuuuuuu)@(uuuuuuuu)@(uuuuuuuu)@(uuuuuuuu)u)",
                   arg_eCallStatus,
                   arg_bCallStatus,
                   arg_iCallStatus,
                   arg_sdnStatus,
                   arg_backupAudioStatus));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.lge.temil.Telematics.RescueStatus", "Notification",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void com_lge_temil_telematics_rescue_status_skeleton_iface_init (ComLgeTemilTelematicsRescueStatusIface *iface);
G_DEFINE_TYPE_WITH_CODE (ComLgeTemilTelematicsRescueStatusSkeleton, com_lge_temil_telematics_rescue_status_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (TYPE_COM_LGE_TEMIL_TELEMATICS_RESCUE_STATUS, com_lge_temil_telematics_rescue_status_skeleton_iface_init));

static void
com_lge_temil_telematics_rescue_status_skeleton_finalize (GObject *object)
{
  ComLgeTemilTelematicsRescueStatusSkeleton *skeleton = COM_LGE_TEMIL_TELEMATICS_RESCUE_STATUS_SKELETON (object);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (com_lge_temil_telematics_rescue_status_skeleton_parent_class)->finalize (object);
}

static void
com_lge_temil_telematics_rescue_status_skeleton_init (ComLgeTemilTelematicsRescueStatusSkeleton *skeleton)
{
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, TYPE_COM_LGE_TEMIL_TELEMATICS_RESCUE_STATUS_SKELETON, ComLgeTemilTelematicsRescueStatusSkeletonPrivate);
  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
}

static void
com_lge_temil_telematics_rescue_status_skeleton_class_init (ComLgeTemilTelematicsRescueStatusSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  g_type_class_add_private (klass, sizeof (ComLgeTemilTelematicsRescueStatusSkeletonPrivate));

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = com_lge_temil_telematics_rescue_status_skeleton_finalize;

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = com_lge_temil_telematics_rescue_status_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = com_lge_temil_telematics_rescue_status_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = com_lge_temil_telematics_rescue_status_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = com_lge_temil_telematics_rescue_status_skeleton_dbus_interface_get_vtable;
}

static void
com_lge_temil_telematics_rescue_status_skeleton_iface_init (ComLgeTemilTelematicsRescueStatusIface *iface)
{
  iface->notification = _com_lge_temil_telematics_rescue_status_on_signal_notification;
}

/**
 * com_lge_temil_telematics_rescue_status_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Telematics-RescueStatus.top_of_page">com.lge.temil.Telematics.RescueStatus</link>.
 *
 * Returns: (transfer full) (type ComLgeTemilTelematicsRescueStatusSkeleton): The skeleton object.
 */
ComLgeTemilTelematicsRescueStatus *
com_lge_temil_telematics_rescue_status_skeleton_new (void)
{
  return COM_LGE_TEMIL_TELEMATICS_RESCUE_STATUS (g_object_new (TYPE_COM_LGE_TEMIL_TELEMATICS_RESCUE_STATUS_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface com.lge.temil.Telematics.UserPrivacySettings
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:ComLgeTemilTelematicsUserPrivacySettings
 * @title: ComLgeTemilTelematicsUserPrivacySettings
 * @short_description: Generated C code for the com.lge.temil.Telematics.UserPrivacySettings D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-com-lge-temil-Telematics-UserPrivacySettings.top_of_page">com.lge.temil.Telematics.UserPrivacySettings</link> D-Bus interface in C.
 */

/* ---- Introspection data for com.lge.temil.Telematics.UserPrivacySettings ---- */

static const _ExtendedGDBusArgInfo _com_lge_temil_telematics_user_privacy_settings_method_info_set_request_IN_ARG_userPrivacySetting =
{
  {
    -1,
    "userPrivacySetting",
    "(bbbbbb)",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _com_lge_temil_telematics_user_privacy_settings_method_info_set_request_IN_ARG_pointers[] =
{
  &_com_lge_temil_telematics_user_privacy_settings_method_info_set_request_IN_ARG_userPrivacySetting,
  NULL
};

static const _ExtendedGDBusArgInfo _com_lge_temil_telematics_user_privacy_settings_method_info_set_request_OUT_ARG_userPrivacySettingResult =
{
  {
    -1,
    "userPrivacySettingResult",
    "(bbbbbb)",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _com_lge_temil_telematics_user_privacy_settings_method_info_set_request_OUT_ARG_pointers[] =
{
  &_com_lge_temil_telematics_user_privacy_settings_method_info_set_request_OUT_ARG_userPrivacySettingResult,
  NULL
};

static const _ExtendedGDBusMethodInfo _com_lge_temil_telematics_user_privacy_settings_method_info_set_request =
{
  {
    -1,
    "SetRequest",
    (GDBusArgInfo **) &_com_lge_temil_telematics_user_privacy_settings_method_info_set_request_IN_ARG_pointers,
    (GDBusArgInfo **) &_com_lge_temil_telematics_user_privacy_settings_method_info_set_request_OUT_ARG_pointers,
    NULL
  },
  "handle-set-request",
  FALSE
};

static const _ExtendedGDBusArgInfo _com_lge_temil_telematics_user_privacy_settings_method_info_request_OUT_ARG_userPrivacySettingResult =
{
  {
    -1,
    "userPrivacySettingResult",
    "(bbbbbb)",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _com_lge_temil_telematics_user_privacy_settings_method_info_request_OUT_ARG_pointers[] =
{
  &_com_lge_temil_telematics_user_privacy_settings_method_info_request_OUT_ARG_userPrivacySettingResult,
  NULL
};

static const _ExtendedGDBusMethodInfo _com_lge_temil_telematics_user_privacy_settings_method_info_request =
{
  {
    -1,
    "Request",
    NULL,
    (GDBusArgInfo **) &_com_lge_temil_telematics_user_privacy_settings_method_info_request_OUT_ARG_pointers,
    NULL
  },
  "handle-request",
  FALSE
};

static const _ExtendedGDBusArgInfo _com_lge_temil_telematics_user_privacy_settings_method_info_notification_request_IN_ARG_subscribed =
{
  {
    -1,
    "subscribed",
    "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _com_lge_temil_telematics_user_privacy_settings_method_info_notification_request_IN_ARG_pointers[] =
{
  &_com_lge_temil_telematics_user_privacy_settings_method_info_notification_request_IN_ARG_subscribed,
  NULL
};

static const _ExtendedGDBusMethodInfo _com_lge_temil_telematics_user_privacy_settings_method_info_notification_request =
{
  {
    -1,
    "NotificationRequest",
    (GDBusArgInfo **) &_com_lge_temil_telematics_user_privacy_settings_method_info_notification_request_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-notification-request",
  FALSE
};

static const _ExtendedGDBusMethodInfo * const _com_lge_temil_telematics_user_privacy_settings_method_info_pointers[] =
{
  &_com_lge_temil_telematics_user_privacy_settings_method_info_set_request,
  &_com_lge_temil_telematics_user_privacy_settings_method_info_request,
  &_com_lge_temil_telematics_user_privacy_settings_method_info_notification_request,
  NULL
};

static const _ExtendedGDBusArgInfo _com_lge_temil_telematics_user_privacy_settings_signal_info_notification_ARG_userPrivacySettingResult =
{
  {
    -1,
    "userPrivacySettingResult",
    "(bbbbbb)",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _com_lge_temil_telematics_user_privacy_settings_signal_info_notification_ARG_pointers[] =
{
  &_com_lge_temil_telematics_user_privacy_settings_signal_info_notification_ARG_userPrivacySettingResult,
  NULL
};

static const _ExtendedGDBusSignalInfo _com_lge_temil_telematics_user_privacy_settings_signal_info_notification =
{
  {
    -1,
    "Notification",
    (GDBusArgInfo **) &_com_lge_temil_telematics_user_privacy_settings_signal_info_notification_ARG_pointers,
    NULL
  },
  "notification"
};

static const _ExtendedGDBusSignalInfo * const _com_lge_temil_telematics_user_privacy_settings_signal_info_pointers[] =
{
  &_com_lge_temil_telematics_user_privacy_settings_signal_info_notification,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _com_lge_temil_telematics_user_privacy_settings_interface_info =
{
  {
    -1,
    "com.lge.temil.Telematics.UserPrivacySettings",
    (GDBusMethodInfo **) &_com_lge_temil_telematics_user_privacy_settings_method_info_pointers,
    (GDBusSignalInfo **) &_com_lge_temil_telematics_user_privacy_settings_signal_info_pointers,
    NULL,
    NULL
  },
  "com-lge-temil-telematics-user-privacy-settings",
};


/**
 * com_lge_temil_telematics_user_privacy_settings_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-com-lge-temil-Telematics-UserPrivacySettings.top_of_page">com.lge.temil.Telematics.UserPrivacySettings</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
com_lge_temil_telematics_user_privacy_settings_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_com_lge_temil_telematics_user_privacy_settings_interface_info;
}

/**
 * com_lge_temil_telematics_user_privacy_settings_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #ComLgeTemilTelematicsUserPrivacySettings interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
com_lge_temil_telematics_user_privacy_settings_override_properties (GObjectClass *klass, guint property_id_begin)
{
  return property_id_begin - 1;
}



/**
 * ComLgeTemilTelematicsUserPrivacySettings:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Telematics-UserPrivacySettings.top_of_page">com.lge.temil.Telematics.UserPrivacySettings</link>.
 */

/**
 * ComLgeTemilTelematicsUserPrivacySettingsIface:
 * @parent_iface: The parent interface.
 * @handle_notification_request: Handler for the #ComLgeTemilTelematicsUserPrivacySettings::handle-notification-request signal.
 * @handle_request: Handler for the #ComLgeTemilTelematicsUserPrivacySettings::handle-request signal.
 * @handle_set_request: Handler for the #ComLgeTemilTelematicsUserPrivacySettings::handle-set-request signal.
 * @notification: Handler for the #ComLgeTemilTelematicsUserPrivacySettings::notification signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Telematics-UserPrivacySettings.top_of_page">com.lge.temil.Telematics.UserPrivacySettings</link>.
 */

static void
com_lge_temil_telematics_user_privacy_settings_default_init (ComLgeTemilTelematicsUserPrivacySettingsIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * ComLgeTemilTelematicsUserPrivacySettings::handle-set-request:
   * @object: A #ComLgeTemilTelematicsUserPrivacySettings.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_userPrivacySetting: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-lge-temil-Telematics-UserPrivacySettings.SetRequest">SetRequest()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call com_lge_temil_telematics_user_privacy_settings_complete_set_request() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-set-request",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (ComLgeTemilTelematicsUserPrivacySettingsIface, handle_set_request),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_VARIANT);

  /**
   * ComLgeTemilTelematicsUserPrivacySettings::handle-request:
   * @object: A #ComLgeTemilTelematicsUserPrivacySettings.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-lge-temil-Telematics-UserPrivacySettings.Request">Request()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call com_lge_temil_telematics_user_privacy_settings_complete_request() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-request",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (ComLgeTemilTelematicsUserPrivacySettingsIface, handle_request),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * ComLgeTemilTelematicsUserPrivacySettings::handle-notification-request:
   * @object: A #ComLgeTemilTelematicsUserPrivacySettings.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_subscribed: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-lge-temil-Telematics-UserPrivacySettings.NotificationRequest">NotificationRequest()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call com_lge_temil_telematics_user_privacy_settings_complete_notification_request() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-notification-request",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (ComLgeTemilTelematicsUserPrivacySettingsIface, handle_notification_request),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_BOOLEAN);

  /* GObject signals for received D-Bus signals: */
  /**
   * ComLgeTemilTelematicsUserPrivacySettings::notification:
   * @object: A #ComLgeTemilTelematicsUserPrivacySettings.
   * @arg_userPrivacySettingResult: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-lge-temil-Telematics-UserPrivacySettings.Notification">"Notification"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("notification",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (ComLgeTemilTelematicsUserPrivacySettingsIface, notification),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    1, G_TYPE_VARIANT);

}

typedef ComLgeTemilTelematicsUserPrivacySettingsIface ComLgeTemilTelematicsUserPrivacySettingsInterface;
G_DEFINE_INTERFACE (ComLgeTemilTelematicsUserPrivacySettings, com_lge_temil_telematics_user_privacy_settings, G_TYPE_OBJECT);

/**
 * com_lge_temil_telematics_user_privacy_settings_emit_notification:
 * @object: A #ComLgeTemilTelematicsUserPrivacySettings.
 * @arg_userPrivacySettingResult: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-com-lge-temil-Telematics-UserPrivacySettings.Notification">"Notification"</link> D-Bus signal.
 */
void
com_lge_temil_telematics_user_privacy_settings_emit_notification (
    ComLgeTemilTelematicsUserPrivacySettings *object,
    GVariant *arg_userPrivacySettingResult)
{
  g_signal_emit_by_name (object, "notification", arg_userPrivacySettingResult);
}

/**
 * com_lge_temil_telematics_user_privacy_settings_call_set_request:
 * @proxy: A #ComLgeTemilTelematicsUserPrivacySettingsProxy.
 * @arg_userPrivacySetting: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-lge-temil-Telematics-UserPrivacySettings.SetRequest">SetRequest()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_telematics_user_privacy_settings_call_set_request_finish() to get the result of the operation.
 *
 * See com_lge_temil_telematics_user_privacy_settings_call_set_request_sync() for the synchronous, blocking version of this method.
 */
void
com_lge_temil_telematics_user_privacy_settings_call_set_request (
    ComLgeTemilTelematicsUserPrivacySettings *proxy,
    GVariant *arg_userPrivacySetting,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "SetRequest",
    g_variant_new ("(@(bbbbbb))",
                   arg_userPrivacySetting),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * com_lge_temil_telematics_user_privacy_settings_call_set_request_finish:
 * @proxy: A #ComLgeTemilTelematicsUserPrivacySettingsProxy.
 * @out_userPrivacySettingResult: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_telematics_user_privacy_settings_call_set_request().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with com_lge_temil_telematics_user_privacy_settings_call_set_request().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_telematics_user_privacy_settings_call_set_request_finish (
    ComLgeTemilTelematicsUserPrivacySettings *proxy,
    GVariant **out_userPrivacySettingResult,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@(bbbbbb))",
                 out_userPrivacySettingResult);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_telematics_user_privacy_settings_call_set_request_sync:
 * @proxy: A #ComLgeTemilTelematicsUserPrivacySettingsProxy.
 * @arg_userPrivacySetting: Argument to pass with the method invocation.
 * @out_userPrivacySettingResult: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-lge-temil-Telematics-UserPrivacySettings.SetRequest">SetRequest()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_telematics_user_privacy_settings_call_set_request() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_telematics_user_privacy_settings_call_set_request_sync (
    ComLgeTemilTelematicsUserPrivacySettings *proxy,
    GVariant *arg_userPrivacySetting,
    GVariant **out_userPrivacySettingResult,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "SetRequest",
    g_variant_new ("(@(bbbbbb))",
                   arg_userPrivacySetting),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@(bbbbbb))",
                 out_userPrivacySettingResult);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_telematics_user_privacy_settings_call_request:
 * @proxy: A #ComLgeTemilTelematicsUserPrivacySettingsProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-lge-temil-Telematics-UserPrivacySettings.Request">Request()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_telematics_user_privacy_settings_call_request_finish() to get the result of the operation.
 *
 * See com_lge_temil_telematics_user_privacy_settings_call_request_sync() for the synchronous, blocking version of this method.
 */
void
com_lge_temil_telematics_user_privacy_settings_call_request (
    ComLgeTemilTelematicsUserPrivacySettings *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Request",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * com_lge_temil_telematics_user_privacy_settings_call_request_finish:
 * @proxy: A #ComLgeTemilTelematicsUserPrivacySettingsProxy.
 * @out_userPrivacySettingResult: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_telematics_user_privacy_settings_call_request().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with com_lge_temil_telematics_user_privacy_settings_call_request().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_telematics_user_privacy_settings_call_request_finish (
    ComLgeTemilTelematicsUserPrivacySettings *proxy,
    GVariant **out_userPrivacySettingResult,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@(bbbbbb))",
                 out_userPrivacySettingResult);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_telematics_user_privacy_settings_call_request_sync:
 * @proxy: A #ComLgeTemilTelematicsUserPrivacySettingsProxy.
 * @out_userPrivacySettingResult: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-lge-temil-Telematics-UserPrivacySettings.Request">Request()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_telematics_user_privacy_settings_call_request() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_telematics_user_privacy_settings_call_request_sync (
    ComLgeTemilTelematicsUserPrivacySettings *proxy,
    GVariant **out_userPrivacySettingResult,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Request",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@(bbbbbb))",
                 out_userPrivacySettingResult);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_telematics_user_privacy_settings_call_notification_request:
 * @proxy: A #ComLgeTemilTelematicsUserPrivacySettingsProxy.
 * @arg_subscribed: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-lge-temil-Telematics-UserPrivacySettings.NotificationRequest">NotificationRequest()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_telematics_user_privacy_settings_call_notification_request_finish() to get the result of the operation.
 *
 * See com_lge_temil_telematics_user_privacy_settings_call_notification_request_sync() for the synchronous, blocking version of this method.
 */
void
com_lge_temil_telematics_user_privacy_settings_call_notification_request (
    ComLgeTemilTelematicsUserPrivacySettings *proxy,
    gboolean arg_subscribed,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "NotificationRequest",
    g_variant_new ("(b)",
                   arg_subscribed),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * com_lge_temil_telematics_user_privacy_settings_call_notification_request_finish:
 * @proxy: A #ComLgeTemilTelematicsUserPrivacySettingsProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_telematics_user_privacy_settings_call_notification_request().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with com_lge_temil_telematics_user_privacy_settings_call_notification_request().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_telematics_user_privacy_settings_call_notification_request_finish (
    ComLgeTemilTelematicsUserPrivacySettings *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_telematics_user_privacy_settings_call_notification_request_sync:
 * @proxy: A #ComLgeTemilTelematicsUserPrivacySettingsProxy.
 * @arg_subscribed: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-lge-temil-Telematics-UserPrivacySettings.NotificationRequest">NotificationRequest()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_telematics_user_privacy_settings_call_notification_request() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_telematics_user_privacy_settings_call_notification_request_sync (
    ComLgeTemilTelematicsUserPrivacySettings *proxy,
    gboolean arg_subscribed,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "NotificationRequest",
    g_variant_new ("(b)",
                   arg_subscribed),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_telematics_user_privacy_settings_complete_set_request:
 * @object: A #ComLgeTemilTelematicsUserPrivacySettings.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @userPrivacySettingResult: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-lge-temil-Telematics-UserPrivacySettings.SetRequest">SetRequest()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
com_lge_temil_telematics_user_privacy_settings_complete_set_request (
    ComLgeTemilTelematicsUserPrivacySettings *object,
    GDBusMethodInvocation *invocation,
    GVariant *userPrivacySettingResult)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(@(bbbbbb))",
                   userPrivacySettingResult));
}

/**
 * com_lge_temil_telematics_user_privacy_settings_complete_request:
 * @object: A #ComLgeTemilTelematicsUserPrivacySettings.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @userPrivacySettingResult: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-lge-temil-Telematics-UserPrivacySettings.Request">Request()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
com_lge_temil_telematics_user_privacy_settings_complete_request (
    ComLgeTemilTelematicsUserPrivacySettings *object,
    GDBusMethodInvocation *invocation,
    GVariant *userPrivacySettingResult)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(@(bbbbbb))",
                   userPrivacySettingResult));
}

/**
 * com_lge_temil_telematics_user_privacy_settings_complete_notification_request:
 * @object: A #ComLgeTemilTelematicsUserPrivacySettings.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-lge-temil-Telematics-UserPrivacySettings.NotificationRequest">NotificationRequest()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
com_lge_temil_telematics_user_privacy_settings_complete_notification_request (
    ComLgeTemilTelematicsUserPrivacySettings *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * ComLgeTemilTelematicsUserPrivacySettingsProxy:
 *
 * The #ComLgeTemilTelematicsUserPrivacySettingsProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * ComLgeTemilTelematicsUserPrivacySettingsProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #ComLgeTemilTelematicsUserPrivacySettingsProxy.
 */

struct _ComLgeTemilTelematicsUserPrivacySettingsProxyPrivate
{
  GData *qdata;
};

static void com_lge_temil_telematics_user_privacy_settings_proxy_iface_init (ComLgeTemilTelematicsUserPrivacySettingsIface *iface);

G_DEFINE_TYPE_WITH_CODE (ComLgeTemilTelematicsUserPrivacySettingsProxy, com_lge_temil_telematics_user_privacy_settings_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (TYPE_COM_LGE_TEMIL_TELEMATICS_USER_PRIVACY_SETTINGS, com_lge_temil_telematics_user_privacy_settings_proxy_iface_init));

static void
com_lge_temil_telematics_user_privacy_settings_proxy_finalize (GObject *object)
{
  ComLgeTemilTelematicsUserPrivacySettingsProxy *proxy = COM_LGE_TEMIL_TELEMATICS_USER_PRIVACY_SETTINGS_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (com_lge_temil_telematics_user_privacy_settings_proxy_parent_class)->finalize (object);
}

static void
com_lge_temil_telematics_user_privacy_settings_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec)
{
}

static void
com_lge_temil_telematics_user_privacy_settings_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
}

static void
com_lge_temil_telematics_user_privacy_settings_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_com_lge_temil_telematics_user_privacy_settings_interface_info, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], TYPE_COM_LGE_TEMIL_TELEMATICS_USER_PRIVACY_SETTINGS);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_COM_LGE_TEMIL_TELEMATICS_USER_PRIVACY_SETTINGS);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
com_lge_temil_telematics_user_privacy_settings_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  ComLgeTemilTelematicsUserPrivacySettingsProxy *proxy = COM_LGE_TEMIL_TELEMATICS_USER_PRIVACY_SETTINGS_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_telematics_user_privacy_settings_interface_info, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_telematics_user_privacy_settings_interface_info, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static void
com_lge_temil_telematics_user_privacy_settings_proxy_init (ComLgeTemilTelematicsUserPrivacySettingsProxy *proxy)
{
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, TYPE_COM_LGE_TEMIL_TELEMATICS_USER_PRIVACY_SETTINGS_PROXY, ComLgeTemilTelematicsUserPrivacySettingsProxyPrivate);
  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), com_lge_temil_telematics_user_privacy_settings_interface_info ());
}

static void
com_lge_temil_telematics_user_privacy_settings_proxy_class_init (ComLgeTemilTelematicsUserPrivacySettingsProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  g_type_class_add_private (klass, sizeof (ComLgeTemilTelematicsUserPrivacySettingsProxyPrivate));

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = com_lge_temil_telematics_user_privacy_settings_proxy_finalize;
  gobject_class->get_property = com_lge_temil_telematics_user_privacy_settings_proxy_get_property;
  gobject_class->set_property = com_lge_temil_telematics_user_privacy_settings_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = com_lge_temil_telematics_user_privacy_settings_proxy_g_signal;
  proxy_class->g_properties_changed = com_lge_temil_telematics_user_privacy_settings_proxy_g_properties_changed;

}

static void
com_lge_temil_telematics_user_privacy_settings_proxy_iface_init (ComLgeTemilTelematicsUserPrivacySettingsIface *iface)
{
}

/**
 * com_lge_temil_telematics_user_privacy_settings_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Telematics-UserPrivacySettings.top_of_page">com.lge.temil.Telematics.UserPrivacySettings</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_telematics_user_privacy_settings_proxy_new_finish() to get the result of the operation.
 *
 * See com_lge_temil_telematics_user_privacy_settings_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
com_lge_temil_telematics_user_privacy_settings_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_COM_LGE_TEMIL_TELEMATICS_USER_PRIVACY_SETTINGS_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Telematics.UserPrivacySettings", NULL);
}

/**
 * com_lge_temil_telematics_user_privacy_settings_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_telematics_user_privacy_settings_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with com_lge_temil_telematics_user_privacy_settings_proxy_new().
 *
 * Returns: (transfer full) (type ComLgeTemilTelematicsUserPrivacySettingsProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilTelematicsUserPrivacySettings *
com_lge_temil_telematics_user_privacy_settings_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return COM_LGE_TEMIL_TELEMATICS_USER_PRIVACY_SETTINGS (ret);
  else
    return NULL;
}

/**
 * com_lge_temil_telematics_user_privacy_settings_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Telematics-UserPrivacySettings.top_of_page">com.lge.temil.Telematics.UserPrivacySettings</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_telematics_user_privacy_settings_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type ComLgeTemilTelematicsUserPrivacySettingsProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilTelematicsUserPrivacySettings *
com_lge_temil_telematics_user_privacy_settings_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_COM_LGE_TEMIL_TELEMATICS_USER_PRIVACY_SETTINGS_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Telematics.UserPrivacySettings", NULL);
  if (ret != NULL)
    return COM_LGE_TEMIL_TELEMATICS_USER_PRIVACY_SETTINGS (ret);
  else
    return NULL;
}


/**
 * com_lge_temil_telematics_user_privacy_settings_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like com_lge_temil_telematics_user_privacy_settings_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_telematics_user_privacy_settings_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See com_lge_temil_telematics_user_privacy_settings_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
com_lge_temil_telematics_user_privacy_settings_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_COM_LGE_TEMIL_TELEMATICS_USER_PRIVACY_SETTINGS_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Telematics.UserPrivacySettings", NULL);
}

/**
 * com_lge_temil_telematics_user_privacy_settings_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_telematics_user_privacy_settings_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with com_lge_temil_telematics_user_privacy_settings_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type ComLgeTemilTelematicsUserPrivacySettingsProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilTelematicsUserPrivacySettings *
com_lge_temil_telematics_user_privacy_settings_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return COM_LGE_TEMIL_TELEMATICS_USER_PRIVACY_SETTINGS (ret);
  else
    return NULL;
}

/**
 * com_lge_temil_telematics_user_privacy_settings_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like com_lge_temil_telematics_user_privacy_settings_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_telematics_user_privacy_settings_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type ComLgeTemilTelematicsUserPrivacySettingsProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilTelematicsUserPrivacySettings *
com_lge_temil_telematics_user_privacy_settings_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_COM_LGE_TEMIL_TELEMATICS_USER_PRIVACY_SETTINGS_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Telematics.UserPrivacySettings", NULL);
  if (ret != NULL)
    return COM_LGE_TEMIL_TELEMATICS_USER_PRIVACY_SETTINGS (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * ComLgeTemilTelematicsUserPrivacySettingsSkeleton:
 *
 * The #ComLgeTemilTelematicsUserPrivacySettingsSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * ComLgeTemilTelematicsUserPrivacySettingsSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #ComLgeTemilTelematicsUserPrivacySettingsSkeleton.
 */

struct _ComLgeTemilTelematicsUserPrivacySettingsSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_com_lge_temil_telematics_user_privacy_settings_skeleton_handle_method_call (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  ComLgeTemilTelematicsUserPrivacySettingsSkeleton *skeleton = COM_LGE_TEMIL_TELEMATICS_USER_PRIVACY_SETTINGS_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], TYPE_COM_LGE_TEMIL_TELEMATICS_USER_PRIVACY_SETTINGS);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_COM_LGE_TEMIL_TELEMATICS_USER_PRIVACY_SETTINGS);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_com_lge_temil_telematics_user_privacy_settings_skeleton_handle_get_property (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  ComLgeTemilTelematicsUserPrivacySettingsSkeleton *skeleton = COM_LGE_TEMIL_TELEMATICS_USER_PRIVACY_SETTINGS_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_telematics_user_privacy_settings_interface_info, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_com_lge_temil_telematics_user_privacy_settings_skeleton_handle_set_property (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  ComLgeTemilTelematicsUserPrivacySettingsSkeleton *skeleton = COM_LGE_TEMIL_TELEMATICS_USER_PRIVACY_SETTINGS_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_telematics_user_privacy_settings_interface_info, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _com_lge_temil_telematics_user_privacy_settings_skeleton_vtable =
{
  _com_lge_temil_telematics_user_privacy_settings_skeleton_handle_method_call,
  _com_lge_temil_telematics_user_privacy_settings_skeleton_handle_get_property,
  _com_lge_temil_telematics_user_privacy_settings_skeleton_handle_set_property
};

static GDBusInterfaceInfo *
com_lge_temil_telematics_user_privacy_settings_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton)
{
  return com_lge_temil_telematics_user_privacy_settings_interface_info ();
}

static GDBusInterfaceVTable *
com_lge_temil_telematics_user_privacy_settings_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton)
{
  return (GDBusInterfaceVTable *) &_com_lge_temil_telematics_user_privacy_settings_skeleton_vtable;
}

static GVariant *
com_lge_temil_telematics_user_privacy_settings_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  ComLgeTemilTelematicsUserPrivacySettingsSkeleton *skeleton = COM_LGE_TEMIL_TELEMATICS_USER_PRIVACY_SETTINGS_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_com_lge_temil_telematics_user_privacy_settings_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _com_lge_temil_telematics_user_privacy_settings_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _com_lge_temil_telematics_user_privacy_settings_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _com_lge_temil_telematics_user_privacy_settings_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.lge.temil.Telematics.UserPrivacySettings", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static void
com_lge_temil_telematics_user_privacy_settings_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
}

static void
_com_lge_temil_telematics_user_privacy_settings_on_signal_notification (
    ComLgeTemilTelematicsUserPrivacySettings *object,
    GVariant *arg_userPrivacySettingResult)
{
  ComLgeTemilTelematicsUserPrivacySettingsSkeleton *skeleton = COM_LGE_TEMIL_TELEMATICS_USER_PRIVACY_SETTINGS_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(@(bbbbbb))",
                   arg_userPrivacySettingResult));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.lge.temil.Telematics.UserPrivacySettings", "Notification",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void com_lge_temil_telematics_user_privacy_settings_skeleton_iface_init (ComLgeTemilTelematicsUserPrivacySettingsIface *iface);
G_DEFINE_TYPE_WITH_CODE (ComLgeTemilTelematicsUserPrivacySettingsSkeleton, com_lge_temil_telematics_user_privacy_settings_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (TYPE_COM_LGE_TEMIL_TELEMATICS_USER_PRIVACY_SETTINGS, com_lge_temil_telematics_user_privacy_settings_skeleton_iface_init));

static void
com_lge_temil_telematics_user_privacy_settings_skeleton_finalize (GObject *object)
{
  ComLgeTemilTelematicsUserPrivacySettingsSkeleton *skeleton = COM_LGE_TEMIL_TELEMATICS_USER_PRIVACY_SETTINGS_SKELETON (object);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (com_lge_temil_telematics_user_privacy_settings_skeleton_parent_class)->finalize (object);
}

static void
com_lge_temil_telematics_user_privacy_settings_skeleton_init (ComLgeTemilTelematicsUserPrivacySettingsSkeleton *skeleton)
{
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, TYPE_COM_LGE_TEMIL_TELEMATICS_USER_PRIVACY_SETTINGS_SKELETON, ComLgeTemilTelematicsUserPrivacySettingsSkeletonPrivate);
  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
}

static void
com_lge_temil_telematics_user_privacy_settings_skeleton_class_init (ComLgeTemilTelematicsUserPrivacySettingsSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  g_type_class_add_private (klass, sizeof (ComLgeTemilTelematicsUserPrivacySettingsSkeletonPrivate));

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = com_lge_temil_telematics_user_privacy_settings_skeleton_finalize;

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = com_lge_temil_telematics_user_privacy_settings_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = com_lge_temil_telematics_user_privacy_settings_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = com_lge_temil_telematics_user_privacy_settings_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = com_lge_temil_telematics_user_privacy_settings_skeleton_dbus_interface_get_vtable;
}

static void
com_lge_temil_telematics_user_privacy_settings_skeleton_iface_init (ComLgeTemilTelematicsUserPrivacySettingsIface *iface)
{
  iface->notification = _com_lge_temil_telematics_user_privacy_settings_on_signal_notification;
}

/**
 * com_lge_temil_telematics_user_privacy_settings_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Telematics-UserPrivacySettings.top_of_page">com.lge.temil.Telematics.UserPrivacySettings</link>.
 *
 * Returns: (transfer full) (type ComLgeTemilTelematicsUserPrivacySettingsSkeleton): The skeleton object.
 */
ComLgeTemilTelematicsUserPrivacySettings *
com_lge_temil_telematics_user_privacy_settings_skeleton_new (void)
{
  return COM_LGE_TEMIL_TELEMATICS_USER_PRIVACY_SETTINGS (g_object_new (TYPE_COM_LGE_TEMIL_TELEMATICS_USER_PRIVACY_SETTINGS_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface com.lge.temil.Telematics.SendToCarConfirmation
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:ComLgeTemilTelematicsSendToCarConfirmation
 * @title: ComLgeTemilTelematicsSendToCarConfirmation
 * @short_description: Generated C code for the com.lge.temil.Telematics.SendToCarConfirmation D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-com-lge-temil-Telematics-SendToCarConfirmation.top_of_page">com.lge.temil.Telematics.SendToCarConfirmation</link> D-Bus interface in C.
 */

/* ---- Introspection data for com.lge.temil.Telematics.SendToCarConfirmation ---- */

static const _ExtendedGDBusArgInfo _com_lge_temil_telematics_send_to_car_confirmation_method_info_set_request_IN_ARG_confirmation =
{
  {
    -1,
    "confirmation",
    "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _com_lge_temil_telematics_send_to_car_confirmation_method_info_set_request_IN_ARG_confirmedId =
{
  {
    -1,
    "confirmedId",
    "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _com_lge_temil_telematics_send_to_car_confirmation_method_info_set_request_IN_ARG_pointers[] =
{
  &_com_lge_temil_telematics_send_to_car_confirmation_method_info_set_request_IN_ARG_confirmation,
  &_com_lge_temil_telematics_send_to_car_confirmation_method_info_set_request_IN_ARG_confirmedId,
  NULL
};

static const _ExtendedGDBusArgInfo _com_lge_temil_telematics_send_to_car_confirmation_method_info_set_request_OUT_ARG_confirmationResult =
{
  {
    -1,
    "confirmationResult",
    "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _com_lge_temil_telematics_send_to_car_confirmation_method_info_set_request_OUT_ARG_confirmedIdResult =
{
  {
    -1,
    "confirmedIdResult",
    "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _com_lge_temil_telematics_send_to_car_confirmation_method_info_set_request_OUT_ARG_pointers[] =
{
  &_com_lge_temil_telematics_send_to_car_confirmation_method_info_set_request_OUT_ARG_confirmationResult,
  &_com_lge_temil_telematics_send_to_car_confirmation_method_info_set_request_OUT_ARG_confirmedIdResult,
  NULL
};

static const _ExtendedGDBusMethodInfo _com_lge_temil_telematics_send_to_car_confirmation_method_info_set_request =
{
  {
    -1,
    "SetRequest",
    (GDBusArgInfo **) &_com_lge_temil_telematics_send_to_car_confirmation_method_info_set_request_IN_ARG_pointers,
    (GDBusArgInfo **) &_com_lge_temil_telematics_send_to_car_confirmation_method_info_set_request_OUT_ARG_pointers,
    NULL
  },
  "handle-set-request",
  FALSE
};

static const _ExtendedGDBusArgInfo _com_lge_temil_telematics_send_to_car_confirmation_method_info_request_OUT_ARG_confirmation =
{
  {
    -1,
    "confirmation",
    "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _com_lge_temil_telematics_send_to_car_confirmation_method_info_request_OUT_ARG_confirmedId =
{
  {
    -1,
    "confirmedId",
    "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _com_lge_temil_telematics_send_to_car_confirmation_method_info_request_OUT_ARG_pointers[] =
{
  &_com_lge_temil_telematics_send_to_car_confirmation_method_info_request_OUT_ARG_confirmation,
  &_com_lge_temil_telematics_send_to_car_confirmation_method_info_request_OUT_ARG_confirmedId,
  NULL
};

static const _ExtendedGDBusMethodInfo _com_lge_temil_telematics_send_to_car_confirmation_method_info_request =
{
  {
    -1,
    "Request",
    NULL,
    (GDBusArgInfo **) &_com_lge_temil_telematics_send_to_car_confirmation_method_info_request_OUT_ARG_pointers,
    NULL
  },
  "handle-request",
  FALSE
};

static const _ExtendedGDBusMethodInfo * const _com_lge_temil_telematics_send_to_car_confirmation_method_info_pointers[] =
{
  &_com_lge_temil_telematics_send_to_car_confirmation_method_info_set_request,
  &_com_lge_temil_telematics_send_to_car_confirmation_method_info_request,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _com_lge_temil_telematics_send_to_car_confirmation_interface_info =
{
  {
    -1,
    "com.lge.temil.Telematics.SendToCarConfirmation",
    (GDBusMethodInfo **) &_com_lge_temil_telematics_send_to_car_confirmation_method_info_pointers,
    NULL,
    NULL,
    NULL
  },
  "com-lge-temil-telematics-send-to-car-confirmation",
};


/**
 * com_lge_temil_telematics_send_to_car_confirmation_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-com-lge-temil-Telematics-SendToCarConfirmation.top_of_page">com.lge.temil.Telematics.SendToCarConfirmation</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
com_lge_temil_telematics_send_to_car_confirmation_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_com_lge_temil_telematics_send_to_car_confirmation_interface_info;
}

/**
 * com_lge_temil_telematics_send_to_car_confirmation_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #ComLgeTemilTelematicsSendToCarConfirmation interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
com_lge_temil_telematics_send_to_car_confirmation_override_properties (GObjectClass *klass, guint property_id_begin)
{
  return property_id_begin - 1;
}



/**
 * ComLgeTemilTelematicsSendToCarConfirmation:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Telematics-SendToCarConfirmation.top_of_page">com.lge.temil.Telematics.SendToCarConfirmation</link>.
 */

/**
 * ComLgeTemilTelematicsSendToCarConfirmationIface:
 * @parent_iface: The parent interface.
 * @handle_request: Handler for the #ComLgeTemilTelematicsSendToCarConfirmation::handle-request signal.
 * @handle_set_request: Handler for the #ComLgeTemilTelematicsSendToCarConfirmation::handle-set-request signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Telematics-SendToCarConfirmation.top_of_page">com.lge.temil.Telematics.SendToCarConfirmation</link>.
 */

static void
com_lge_temil_telematics_send_to_car_confirmation_default_init (ComLgeTemilTelematicsSendToCarConfirmationIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * ComLgeTemilTelematicsSendToCarConfirmation::handle-set-request:
   * @object: A #ComLgeTemilTelematicsSendToCarConfirmation.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_confirmation: Argument passed by remote caller.
   * @arg_confirmedId: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-lge-temil-Telematics-SendToCarConfirmation.SetRequest">SetRequest()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call com_lge_temil_telematics_send_to_car_confirmation_complete_set_request() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-set-request",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (ComLgeTemilTelematicsSendToCarConfirmationIface, handle_set_request),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_UINT, G_TYPE_UINT);

  /**
   * ComLgeTemilTelematicsSendToCarConfirmation::handle-request:
   * @object: A #ComLgeTemilTelematicsSendToCarConfirmation.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-lge-temil-Telematics-SendToCarConfirmation.Request">Request()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call com_lge_temil_telematics_send_to_car_confirmation_complete_request() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-request",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (ComLgeTemilTelematicsSendToCarConfirmationIface, handle_request),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

}

typedef ComLgeTemilTelematicsSendToCarConfirmationIface ComLgeTemilTelematicsSendToCarConfirmationInterface;
G_DEFINE_INTERFACE (ComLgeTemilTelematicsSendToCarConfirmation, com_lge_temil_telematics_send_to_car_confirmation, G_TYPE_OBJECT);

/**
 * com_lge_temil_telematics_send_to_car_confirmation_call_set_request:
 * @proxy: A #ComLgeTemilTelematicsSendToCarConfirmationProxy.
 * @arg_confirmation: Argument to pass with the method invocation.
 * @arg_confirmedId: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-lge-temil-Telematics-SendToCarConfirmation.SetRequest">SetRequest()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_telematics_send_to_car_confirmation_call_set_request_finish() to get the result of the operation.
 *
 * See com_lge_temil_telematics_send_to_car_confirmation_call_set_request_sync() for the synchronous, blocking version of this method.
 */
void
com_lge_temil_telematics_send_to_car_confirmation_call_set_request (
    ComLgeTemilTelematicsSendToCarConfirmation *proxy,
    guint arg_confirmation,
    guint arg_confirmedId,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "SetRequest",
    g_variant_new ("(uu)",
                   arg_confirmation,
                   arg_confirmedId),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * com_lge_temil_telematics_send_to_car_confirmation_call_set_request_finish:
 * @proxy: A #ComLgeTemilTelematicsSendToCarConfirmationProxy.
 * @out_confirmationResult: (out): Return location for return parameter or %NULL to ignore.
 * @out_confirmedIdResult: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_telematics_send_to_car_confirmation_call_set_request().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with com_lge_temil_telematics_send_to_car_confirmation_call_set_request().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_telematics_send_to_car_confirmation_call_set_request_finish (
    ComLgeTemilTelematicsSendToCarConfirmation *proxy,
    guint *out_confirmationResult,
    guint *out_confirmedIdResult,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(uu)",
                 out_confirmationResult,
                 out_confirmedIdResult);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_telematics_send_to_car_confirmation_call_set_request_sync:
 * @proxy: A #ComLgeTemilTelematicsSendToCarConfirmationProxy.
 * @arg_confirmation: Argument to pass with the method invocation.
 * @arg_confirmedId: Argument to pass with the method invocation.
 * @out_confirmationResult: (out): Return location for return parameter or %NULL to ignore.
 * @out_confirmedIdResult: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-lge-temil-Telematics-SendToCarConfirmation.SetRequest">SetRequest()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_telematics_send_to_car_confirmation_call_set_request() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_telematics_send_to_car_confirmation_call_set_request_sync (
    ComLgeTemilTelematicsSendToCarConfirmation *proxy,
    guint arg_confirmation,
    guint arg_confirmedId,
    guint *out_confirmationResult,
    guint *out_confirmedIdResult,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "SetRequest",
    g_variant_new ("(uu)",
                   arg_confirmation,
                   arg_confirmedId),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(uu)",
                 out_confirmationResult,
                 out_confirmedIdResult);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_telematics_send_to_car_confirmation_call_request:
 * @proxy: A #ComLgeTemilTelematicsSendToCarConfirmationProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-lge-temil-Telematics-SendToCarConfirmation.Request">Request()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_telematics_send_to_car_confirmation_call_request_finish() to get the result of the operation.
 *
 * See com_lge_temil_telematics_send_to_car_confirmation_call_request_sync() for the synchronous, blocking version of this method.
 */
void
com_lge_temil_telematics_send_to_car_confirmation_call_request (
    ComLgeTemilTelematicsSendToCarConfirmation *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Request",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * com_lge_temil_telematics_send_to_car_confirmation_call_request_finish:
 * @proxy: A #ComLgeTemilTelematicsSendToCarConfirmationProxy.
 * @out_confirmation: (out): Return location for return parameter or %NULL to ignore.
 * @out_confirmedId: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_telematics_send_to_car_confirmation_call_request().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with com_lge_temil_telematics_send_to_car_confirmation_call_request().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_telematics_send_to_car_confirmation_call_request_finish (
    ComLgeTemilTelematicsSendToCarConfirmation *proxy,
    guint *out_confirmation,
    guint *out_confirmedId,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(uu)",
                 out_confirmation,
                 out_confirmedId);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_telematics_send_to_car_confirmation_call_request_sync:
 * @proxy: A #ComLgeTemilTelematicsSendToCarConfirmationProxy.
 * @out_confirmation: (out): Return location for return parameter or %NULL to ignore.
 * @out_confirmedId: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-lge-temil-Telematics-SendToCarConfirmation.Request">Request()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_telematics_send_to_car_confirmation_call_request() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
com_lge_temil_telematics_send_to_car_confirmation_call_request_sync (
    ComLgeTemilTelematicsSendToCarConfirmation *proxy,
    guint *out_confirmation,
    guint *out_confirmedId,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Request",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(uu)",
                 out_confirmation,
                 out_confirmedId);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * com_lge_temil_telematics_send_to_car_confirmation_complete_set_request:
 * @object: A #ComLgeTemilTelematicsSendToCarConfirmation.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @confirmationResult: Parameter to return.
 * @confirmedIdResult: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-lge-temil-Telematics-SendToCarConfirmation.SetRequest">SetRequest()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
com_lge_temil_telematics_send_to_car_confirmation_complete_set_request (
    ComLgeTemilTelematicsSendToCarConfirmation *object,
    GDBusMethodInvocation *invocation,
    guint confirmationResult,
    guint confirmedIdResult)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(uu)",
                   confirmationResult,
                   confirmedIdResult));
}

/**
 * com_lge_temil_telematics_send_to_car_confirmation_complete_request:
 * @object: A #ComLgeTemilTelematicsSendToCarConfirmation.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @confirmation: Parameter to return.
 * @confirmedId: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-lge-temil-Telematics-SendToCarConfirmation.Request">Request()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
com_lge_temil_telematics_send_to_car_confirmation_complete_request (
    ComLgeTemilTelematicsSendToCarConfirmation *object,
    GDBusMethodInvocation *invocation,
    guint confirmation,
    guint confirmedId)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(uu)",
                   confirmation,
                   confirmedId));
}

/* ------------------------------------------------------------------------ */

/**
 * ComLgeTemilTelematicsSendToCarConfirmationProxy:
 *
 * The #ComLgeTemilTelematicsSendToCarConfirmationProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * ComLgeTemilTelematicsSendToCarConfirmationProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #ComLgeTemilTelematicsSendToCarConfirmationProxy.
 */

struct _ComLgeTemilTelematicsSendToCarConfirmationProxyPrivate
{
  GData *qdata;
};

static void com_lge_temil_telematics_send_to_car_confirmation_proxy_iface_init (ComLgeTemilTelematicsSendToCarConfirmationIface *iface);

G_DEFINE_TYPE_WITH_CODE (ComLgeTemilTelematicsSendToCarConfirmationProxy, com_lge_temil_telematics_send_to_car_confirmation_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (TYPE_COM_LGE_TEMIL_TELEMATICS_SEND_TO_CAR_CONFIRMATION, com_lge_temil_telematics_send_to_car_confirmation_proxy_iface_init));

static void
com_lge_temil_telematics_send_to_car_confirmation_proxy_finalize (GObject *object)
{
  ComLgeTemilTelematicsSendToCarConfirmationProxy *proxy = COM_LGE_TEMIL_TELEMATICS_SEND_TO_CAR_CONFIRMATION_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (com_lge_temil_telematics_send_to_car_confirmation_proxy_parent_class)->finalize (object);
}

static void
com_lge_temil_telematics_send_to_car_confirmation_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec)
{
}

static void
com_lge_temil_telematics_send_to_car_confirmation_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
}

static void
com_lge_temil_telematics_send_to_car_confirmation_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_com_lge_temil_telematics_send_to_car_confirmation_interface_info, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], TYPE_COM_LGE_TEMIL_TELEMATICS_SEND_TO_CAR_CONFIRMATION);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_COM_LGE_TEMIL_TELEMATICS_SEND_TO_CAR_CONFIRMATION);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
com_lge_temil_telematics_send_to_car_confirmation_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  ComLgeTemilTelematicsSendToCarConfirmationProxy *proxy = COM_LGE_TEMIL_TELEMATICS_SEND_TO_CAR_CONFIRMATION_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_telematics_send_to_car_confirmation_interface_info, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_telematics_send_to_car_confirmation_interface_info, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static void
com_lge_temil_telematics_send_to_car_confirmation_proxy_init (ComLgeTemilTelematicsSendToCarConfirmationProxy *proxy)
{
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, TYPE_COM_LGE_TEMIL_TELEMATICS_SEND_TO_CAR_CONFIRMATION_PROXY, ComLgeTemilTelematicsSendToCarConfirmationProxyPrivate);
  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), com_lge_temil_telematics_send_to_car_confirmation_interface_info ());
}

static void
com_lge_temil_telematics_send_to_car_confirmation_proxy_class_init (ComLgeTemilTelematicsSendToCarConfirmationProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  g_type_class_add_private (klass, sizeof (ComLgeTemilTelematicsSendToCarConfirmationProxyPrivate));

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = com_lge_temil_telematics_send_to_car_confirmation_proxy_finalize;
  gobject_class->get_property = com_lge_temil_telematics_send_to_car_confirmation_proxy_get_property;
  gobject_class->set_property = com_lge_temil_telematics_send_to_car_confirmation_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = com_lge_temil_telematics_send_to_car_confirmation_proxy_g_signal;
  proxy_class->g_properties_changed = com_lge_temil_telematics_send_to_car_confirmation_proxy_g_properties_changed;

}

static void
com_lge_temil_telematics_send_to_car_confirmation_proxy_iface_init (ComLgeTemilTelematicsSendToCarConfirmationIface *iface)
{
}

/**
 * com_lge_temil_telematics_send_to_car_confirmation_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Telematics-SendToCarConfirmation.top_of_page">com.lge.temil.Telematics.SendToCarConfirmation</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_telematics_send_to_car_confirmation_proxy_new_finish() to get the result of the operation.
 *
 * See com_lge_temil_telematics_send_to_car_confirmation_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
com_lge_temil_telematics_send_to_car_confirmation_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_COM_LGE_TEMIL_TELEMATICS_SEND_TO_CAR_CONFIRMATION_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Telematics.SendToCarConfirmation", NULL);
}

/**
 * com_lge_temil_telematics_send_to_car_confirmation_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_telematics_send_to_car_confirmation_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with com_lge_temil_telematics_send_to_car_confirmation_proxy_new().
 *
 * Returns: (transfer full) (type ComLgeTemilTelematicsSendToCarConfirmationProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilTelematicsSendToCarConfirmation *
com_lge_temil_telematics_send_to_car_confirmation_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return COM_LGE_TEMIL_TELEMATICS_SEND_TO_CAR_CONFIRMATION (ret);
  else
    return NULL;
}

/**
 * com_lge_temil_telematics_send_to_car_confirmation_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Telematics-SendToCarConfirmation.top_of_page">com.lge.temil.Telematics.SendToCarConfirmation</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_telematics_send_to_car_confirmation_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type ComLgeTemilTelematicsSendToCarConfirmationProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilTelematicsSendToCarConfirmation *
com_lge_temil_telematics_send_to_car_confirmation_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_COM_LGE_TEMIL_TELEMATICS_SEND_TO_CAR_CONFIRMATION_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Telematics.SendToCarConfirmation", NULL);
  if (ret != NULL)
    return COM_LGE_TEMIL_TELEMATICS_SEND_TO_CAR_CONFIRMATION (ret);
  else
    return NULL;
}


/**
 * com_lge_temil_telematics_send_to_car_confirmation_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like com_lge_temil_telematics_send_to_car_confirmation_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call com_lge_temil_telematics_send_to_car_confirmation_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See com_lge_temil_telematics_send_to_car_confirmation_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
com_lge_temil_telematics_send_to_car_confirmation_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_COM_LGE_TEMIL_TELEMATICS_SEND_TO_CAR_CONFIRMATION_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Telematics.SendToCarConfirmation", NULL);
}

/**
 * com_lge_temil_telematics_send_to_car_confirmation_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to com_lge_temil_telematics_send_to_car_confirmation_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with com_lge_temil_telematics_send_to_car_confirmation_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type ComLgeTemilTelematicsSendToCarConfirmationProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilTelematicsSendToCarConfirmation *
com_lge_temil_telematics_send_to_car_confirmation_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return COM_LGE_TEMIL_TELEMATICS_SEND_TO_CAR_CONFIRMATION (ret);
  else
    return NULL;
}

/**
 * com_lge_temil_telematics_send_to_car_confirmation_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like com_lge_temil_telematics_send_to_car_confirmation_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See com_lge_temil_telematics_send_to_car_confirmation_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type ComLgeTemilTelematicsSendToCarConfirmationProxy): The constructed proxy object or %NULL if @error is set.
 */
ComLgeTemilTelematicsSendToCarConfirmation *
com_lge_temil_telematics_send_to_car_confirmation_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_COM_LGE_TEMIL_TELEMATICS_SEND_TO_CAR_CONFIRMATION_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.lge.temil.Telematics.SendToCarConfirmation", NULL);
  if (ret != NULL)
    return COM_LGE_TEMIL_TELEMATICS_SEND_TO_CAR_CONFIRMATION (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * ComLgeTemilTelematicsSendToCarConfirmationSkeleton:
 *
 * The #ComLgeTemilTelematicsSendToCarConfirmationSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * ComLgeTemilTelematicsSendToCarConfirmationSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #ComLgeTemilTelematicsSendToCarConfirmationSkeleton.
 */

struct _ComLgeTemilTelematicsSendToCarConfirmationSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_com_lge_temil_telematics_send_to_car_confirmation_skeleton_handle_method_call (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  ComLgeTemilTelematicsSendToCarConfirmationSkeleton *skeleton = COM_LGE_TEMIL_TELEMATICS_SEND_TO_CAR_CONFIRMATION_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], TYPE_COM_LGE_TEMIL_TELEMATICS_SEND_TO_CAR_CONFIRMATION);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_COM_LGE_TEMIL_TELEMATICS_SEND_TO_CAR_CONFIRMATION);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_com_lge_temil_telematics_send_to_car_confirmation_skeleton_handle_get_property (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  ComLgeTemilTelematicsSendToCarConfirmationSkeleton *skeleton = COM_LGE_TEMIL_TELEMATICS_SEND_TO_CAR_CONFIRMATION_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_telematics_send_to_car_confirmation_interface_info, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_com_lge_temil_telematics_send_to_car_confirmation_skeleton_handle_set_property (
  GDBusConnection *connection,
  const gchar *sender,
  const gchar *object_path,
  const gchar *interface_name,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  ComLgeTemilTelematicsSendToCarConfirmationSkeleton *skeleton = COM_LGE_TEMIL_TELEMATICS_SEND_TO_CAR_CONFIRMATION_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_com_lge_temil_telematics_send_to_car_confirmation_interface_info, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _com_lge_temil_telematics_send_to_car_confirmation_skeleton_vtable =
{
  _com_lge_temil_telematics_send_to_car_confirmation_skeleton_handle_method_call,
  _com_lge_temil_telematics_send_to_car_confirmation_skeleton_handle_get_property,
  _com_lge_temil_telematics_send_to_car_confirmation_skeleton_handle_set_property
};

static GDBusInterfaceInfo *
com_lge_temil_telematics_send_to_car_confirmation_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton)
{
  return com_lge_temil_telematics_send_to_car_confirmation_interface_info ();
}

static GDBusInterfaceVTable *
com_lge_temil_telematics_send_to_car_confirmation_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton)
{
  return (GDBusInterfaceVTable *) &_com_lge_temil_telematics_send_to_car_confirmation_skeleton_vtable;
}

static GVariant *
com_lge_temil_telematics_send_to_car_confirmation_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  ComLgeTemilTelematicsSendToCarConfirmationSkeleton *skeleton = COM_LGE_TEMIL_TELEMATICS_SEND_TO_CAR_CONFIRMATION_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_com_lge_temil_telematics_send_to_car_confirmation_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _com_lge_temil_telematics_send_to_car_confirmation_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _com_lge_temil_telematics_send_to_car_confirmation_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _com_lge_temil_telematics_send_to_car_confirmation_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.lge.temil.Telematics.SendToCarConfirmation", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static void
com_lge_temil_telematics_send_to_car_confirmation_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
}

static void com_lge_temil_telematics_send_to_car_confirmation_skeleton_iface_init (ComLgeTemilTelematicsSendToCarConfirmationIface *iface);
G_DEFINE_TYPE_WITH_CODE (ComLgeTemilTelematicsSendToCarConfirmationSkeleton, com_lge_temil_telematics_send_to_car_confirmation_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (TYPE_COM_LGE_TEMIL_TELEMATICS_SEND_TO_CAR_CONFIRMATION, com_lge_temil_telematics_send_to_car_confirmation_skeleton_iface_init));

static void
com_lge_temil_telematics_send_to_car_confirmation_skeleton_finalize (GObject *object)
{
  ComLgeTemilTelematicsSendToCarConfirmationSkeleton *skeleton = COM_LGE_TEMIL_TELEMATICS_SEND_TO_CAR_CONFIRMATION_SKELETON (object);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (com_lge_temil_telematics_send_to_car_confirmation_skeleton_parent_class)->finalize (object);
}

static void
com_lge_temil_telematics_send_to_car_confirmation_skeleton_init (ComLgeTemilTelematicsSendToCarConfirmationSkeleton *skeleton)
{
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, TYPE_COM_LGE_TEMIL_TELEMATICS_SEND_TO_CAR_CONFIRMATION_SKELETON, ComLgeTemilTelematicsSendToCarConfirmationSkeletonPrivate);
  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
}

static void
com_lge_temil_telematics_send_to_car_confirmation_skeleton_class_init (ComLgeTemilTelematicsSendToCarConfirmationSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  g_type_class_add_private (klass, sizeof (ComLgeTemilTelematicsSendToCarConfirmationSkeletonPrivate));

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = com_lge_temil_telematics_send_to_car_confirmation_skeleton_finalize;

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = com_lge_temil_telematics_send_to_car_confirmation_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = com_lge_temil_telematics_send_to_car_confirmation_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = com_lge_temil_telematics_send_to_car_confirmation_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = com_lge_temil_telematics_send_to_car_confirmation_skeleton_dbus_interface_get_vtable;
}

static void
com_lge_temil_telematics_send_to_car_confirmation_skeleton_iface_init (ComLgeTemilTelematicsSendToCarConfirmationIface *iface)
{
}

/**
 * com_lge_temil_telematics_send_to_car_confirmation_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-com-lge-temil-Telematics-SendToCarConfirmation.top_of_page">com.lge.temil.Telematics.SendToCarConfirmation</link>.
 *
 * Returns: (transfer full) (type ComLgeTemilTelematicsSendToCarConfirmationSkeleton): The skeleton object.
 */
ComLgeTemilTelematicsSendToCarConfirmation *
com_lge_temil_telematics_send_to_car_confirmation_skeleton_new (void)
{
  return COM_LGE_TEMIL_TELEMATICS_SEND_TO_CAR_CONFIRMATION (g_object_new (TYPE_COM_LGE_TEMIL_TELEMATICS_SEND_TO_CAR_CONFIRMATION_SKELETON, NULL));
}

